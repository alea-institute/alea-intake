---
phase: 06-legal-research-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/mcp/__init__.py
  - backend/app/services/mcp/folio_mcp_client.py
  - backend/app/services/research/citation_normalizer.py
  - backend/tests/test_folio_mcp_client.py
  - backend/tests/test_citation_normalizer.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - INTEGRATE-05
  - RESEARCH-07

must_haves:
  truths:
    - "FolioMCPClient connects to folio-mcp as a long-running subprocess and exposes all 12 tools"
    - "FolioMCPClient is a singleton started at application lifespan, not per-request"
    - "CitationNormalizer parses citation strings via eyecite and produces canonical forms"
    - "Citation normalization deduplicates alternate citations for the same authority"
    - "mcp and eyecite packages are installed as new dependencies"
  artifacts:
    - path: "backend/app/services/mcp/folio_mcp_client.py"
      provides: "FolioMCPClient singleton wrapping mcp SDK for folio-mcp subprocess"
      exports: ["FolioMCPClient"]
    - path: "backend/app/services/research/citation_normalizer.py"
      provides: "CitationNormalizer using eyecite for Bluebook citation parsing"
      exports: ["CitationNormalizer", "NormalizedCitation"]
  key_links:
    - from: "backend/app/services/mcp/folio_mcp_client.py"
      to: "folio-mcp subprocess"
      via: "mcp SDK StdioServerParameters + ClientSession"
      pattern: "stdio_client.*folio-mcp"
    - from: "backend/app/services/research/citation_normalizer.py"
      to: "eyecite library"
      via: "eyecite.get_citations for citation parsing"
      pattern: "get_citations"
---

<objective>
Install new dependencies (mcp, eyecite) and build two independent infrastructure components: (1) FolioMCPClient -- a singleton MCP client that connects to folio-mcp as a long-running subprocess for LLM agent tool-use during analysis, exposing all 12 FOLIO ontology tools; (2) CitationNormalizer -- an eyecite-based citation parser that normalizes legal citations to canonical Bluebook forms for deduplication and verification.

Purpose: These are foundational utilities consumed by Plans 03 and 05. folio-mcp integration fulfills INTEGRATE-05 (LLM agent tool-use). Citation normalization is critical for D-05/D-15 deduplication and verification. Per D-10, folio-mcp is used during issue-spotting, exploration, and research stages.

Output: FolioMCPClient, CitationNormalizer, new dependency installation, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-legal-research-verification/06-CONTEXT.md
@.planning/phases/06-legal-research-verification/06-RESEARCH.md

<interfaces>
<!-- folio-mcp tool reference (12 tools) -->
Tool                   Parameters              Purpose
search_concepts        query, limit=10         Fuzzy concept name search
search_definitions     query, limit=10         Search by definition content
query_concepts         text/structural filters Advanced multi-filter query
query_properties       label, domain, range    Search OWL object properties
get_concept            iri                     Full concept details
export_concept         iri, format             Export as markdown/JSON-LD/OWL
list_branches          (none)                  List all 24 taxonomy branches
get_taxonomy_branch    branch_name, max_depth  Extract branch concepts
get_children           iri, max_depth          Subordinate concepts
get_parents            iri, max_depth          Parent concepts
get_properties         (none)                  List all OWL properties
find_connections       subject, property, obj  Semantic triple lookup

<!-- MCP SDK pattern (from 06-RESEARCH.md) -->
```python
from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client

async with stdio_client(server_params) as (read, write):
    async with ClientSession(read, write) as session:
        await session.initialize()
        result = await session.call_tool("search_concepts", {"query": "negligence", "limit": 5})
```

<!-- eyecite pattern (from 06-RESEARCH.md) -->
```python
from eyecite import get_citations
citations = get_citations("See Smith v. Jones, 123 F.3d 456, 789 (9th Cir. 2020).")
for cite in citations:
    # cite.corrected_citation() -> normalized form
    pass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies + FolioMCPClient singleton</name>
  <files>
    backend/app/services/mcp/__init__.py,
    backend/app/services/mcp/folio_mcp_client.py,
    backend/tests/test_folio_mcp_client.py,
    backend/pyproject.toml
  </files>
  <read_first>
    backend/pyproject.toml,
    backend/app/main.py,
    backend/app/services/folio/loader.py,
    backend/tests/conftest.py,
    .planning/phases/06-legal-research-verification/06-RESEARCH.md (lines 275-323)
  </read_first>
  <behavior>
    - Test 1: FolioMCPClient.__init__ accepts mode param ("api" default, "local" for local OWL)
    - Test 2: FolioMCPClient.connect() creates StdioServerParameters with command="uvx" args=["folio-mcp"]
    - Test 3: FolioMCPClient.connect() in local mode adds "--local" arg
    - Test 4: search_concepts(query, limit) calls session.call_tool("search_concepts", ...) and returns list[dict]
    - Test 5: get_concept(iri) calls session.call_tool("get_concept", {"iri": iri})
    - Test 6: get_taxonomy_branch(branch, max_depth) calls session.call_tool("get_taxonomy_branch", ...)
    - Test 7: get_children(iri, max_depth) calls session.call_tool("get_children", ...)
    - Test 8: get_parents(iri, max_depth) calls session.call_tool("get_parents", ...)
    - Test 9: list_branches() calls session.call_tool("list_branches", {})
    - Test 10: find_connections(subject, property, obj) calls session.call_tool("find_connections", ...)
    - Test 11: close() properly cleans up session and subprocess resources
    - Test 12: FolioMCPClient is usable as async context manager (__aenter__/__aexit__)
    Note: All tests mock the mcp SDK (StdioServerParameters, ClientSession, stdio_client) since folio-mcp subprocess is not available in test env. Tests verify correct tool names and argument shapes are passed.
  </behavior>
  <action>
    First, add mcp>=1.27.0 and eyecite>=2.7.6 to backend/pyproject.toml dependencies. Run pip install to verify.

    Per D-10 and Pitfall 2, create FolioMCPClient as a singleton long-running subprocess manager:
    - __init__(mode="api"): store mode, init _session/_read/_write to None
    - async connect(): Create StdioServerParameters(command="uvx", args=["folio-mcp"] + ["--local"] if local mode). Use mcp SDK's stdio_client to start subprocess. Create ClientSession, call initialize(). Store references.
    - Wrapper methods for all 12 tools (search_concepts, search_definitions, query_concepts, query_properties, get_concept, export_concept, list_branches, get_taxonomy_branch, get_children, get_parents, get_properties, find_connections). Each wraps session.call_tool() and returns parsed content.
    - async close(): Properly close session, read/write streams, terminate subprocess.
    - __aenter__/__aexit__ for async context manager support.
    - Class-level singleton pattern matching EmbeddingService (get_instance/reset_instance with threading.Lock).

    Per Pitfall 2: Register cleanup in a way compatible with FastAPI lifespan. The lifespan integration itself happens in Plan 05 when wiring the orchestrator.

    Tests must mock the mcp SDK entirely (unittest.mock.AsyncMock for session, MagicMock for stdio_client). Verify correct tool names, argument shapes, and cleanup behavior. Do NOT attempt to start an actual folio-mcp subprocess in tests.
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/alea-intake/backend && python -m pytest tests/test_folio_mcp_client.py -x --timeout=30</automated>
  </verify>
  <done>
    mcp and eyecite installed in pyproject.toml. FolioMCPClient singleton wraps all 12 folio-mcp tools via mcp SDK. Async context manager support. Tests mock the MCP SDK and verify correct tool calls. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CitationNormalizer with eyecite-based parsing</name>
  <files>
    backend/app/services/research/citation_normalizer.py,
    backend/tests/test_citation_normalizer.py
  </files>
  <read_first>
    backend/pyproject.toml,
    .planning/phases/06-legal-research-verification/06-RESEARCH.md (lines 456-468)
  </read_first>
  <behavior>
    - Test 1: normalize("123 F.3d 456") returns NormalizedCitation with volume=123, reporter="F.3d", page=456
    - Test 2: normalize("Smith v. Jones, 123 F.3d 456 (9th Cir. 2020)") extracts citation from context text
    - Test 3: extract_all("See 123 F.3d 456 and 789 U.S. 101.") returns list of 2 NormalizedCitation objects
    - Test 4: are_same_authority("123 F.3d 456", "123 F. 3d 456") returns True (whitespace normalization)
    - Test 5: are_same_authority("123 F.3d 456", "789 U.S. 101") returns False (different authorities)
    - Test 6: normalize handles common Bluebook formats: U.S. Reports, Federal Reporter, S.Ct., state reporters
    - Test 7: normalize returns None for unparseable strings (graceful degradation)
    - Test 8: compute_query_hash(query, tool_name, jurisdiction) returns deterministic SHA-256 hash for cache key per D-19
    - Test 9: deduplicate_results(results) removes duplicate ResearchResults by normalized citation per D-15
  </behavior>
  <action>
    Per D-15 (deduplication by citation string normalization) and D-05 (verification pipeline), create CitationNormalizer using eyecite:

    NormalizedCitation Pydantic model:
    - raw: str (original citation string)
    - normalized: str (canonical form from eyecite)
    - volume: int | None
    - reporter: str | None
    - page: int | None
    - pin_cite: str | None (specific page within opinion)
    - court: str | None
    - year: int | None

    CitationNormalizer class:
    - normalize(citation_str) -> NormalizedCitation | None: Parse using eyecite.get_citations(), extract first FullCitation, build NormalizedCitation. Return None for unparseable.
    - extract_all(text) -> list[NormalizedCitation]: Extract all citations from a block of text.
    - are_same_authority(cite_a, cite_b) -> bool: Normalize both, compare by (volume, reporter, page) tuple.
    - compute_query_hash(query, tool_name, jurisdiction) -> str: SHA-256 of normalized inputs for D-19 TTL cache key.
    - deduplicate_results(results: list[ResearchResult]) -> list[ResearchResult]: Group by normalized citation, keep highest-scored per D-15.

    Use eyecite.get_citations() for parsing. Handle eyecite's FullCitation, ShortCaseCitation, SupraCitation types. For non-case citations (statutes, regulations), fall back to string normalization (strip whitespace, lowercase, canonical spacing).

    Note: Import ResearchResult from adapters/base.py. If Plan 01 hasn't run yet in this executor's context, use a forward reference or TYPE_CHECKING import.
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/alea-intake/backend && python -m pytest tests/test_citation_normalizer.py -x --timeout=30</automated>
  </verify>
  <done>
    CitationNormalizer parses Bluebook citations via eyecite, produces NormalizedCitation objects, deduplicates ResearchResults by normalized citation, and computes deterministic cache keys. Graceful None return for unparseable strings. All tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/damienriehl/Coding\ Projects/alea-intake/backend && python -m pytest tests/test_folio_mcp_client.py tests/test_citation_normalizer.py -x --timeout=30
```
</verification>

<success_criteria>
- mcp>=1.27.0 and eyecite>=2.7.6 in pyproject.toml and installable
- FolioMCPClient singleton wraps all 12 folio-mcp tools via mcp SDK per D-10/INTEGRATE-05
- FolioMCPClient supports async context manager and proper subprocess cleanup per Pitfall 2
- CitationNormalizer uses eyecite for Bluebook parsing per D-15
- NormalizedCitation schema captures volume, reporter, page, court, year
- Deduplication by normalized citation works correctly
- Cache key computation is deterministic per D-19
- All tests green with mocked MCP SDK (no real subprocess)
</success_criteria>

<output>
After completion, create `.planning/phases/06-legal-research-verification/06-02-SUMMARY.md`
</output>
