---
phase: 12-open-source-public-release
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - README.md
  - docs/images/
autonomous: false

requirements: []

must_haves:
  truths:
    - "README includes visible UI screenshots referenced from docs/images/"
    - "Screenshots show at minimum: login page, chat interface, dashboard, admin page, and one visualization view"
    - "All screenshot image files exist in docs/images/ and are referenced with relative paths in README"
    - ".planning/ directory passes PII/secrets sanity check (D-24a)"
    - "Final README is factually accurate — no overstatement of capabilities"
  artifacts:
    - path: "docs/images/"
      provides: "UI screenshots captured from running dev servers"
    - path: "README.md"
      provides: "Final complete README with screenshot references embedded"
  key_links:
    - from: "README.md"
      to: "docs/images/*.png"
      via: "Markdown image references"
      pattern: "docs/images/"
---

<objective>
Capture UI screenshots from the running application using MCP chrome-devtools, embed them in the README, verify the .planning/ directory has no secrets (D-24a), and perform a final human verification of the complete README.

Purpose: Visual assets make the project approachable. A legal aid program leader seeing actual screenshots of the chat interface, dashboard, and admin page can evaluate the system far more effectively than reading text alone. The final checkpoint ensures the README is accurate before the repo goes public.

Output: docs/images/ populated with screenshots, README.md updated with image references, .planning/ verified clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-open-source-public-release-mit-license-comprehensive-readme-/12-CONTEXT.md
@.planning/phases/12-open-source-public-release-mit-license-comprehensive-readme-/12-04-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture UI screenshots and embed in README + .planning/ sanity check</name>
  <files>docs/images/login.png, docs/images/chat.png, docs/images/dashboard.png, docs/images/admin.png, docs/images/visualization.png, README.md</files>
  <read_first>
    - README.md (current state — find the Screenshots section placeholder from Plan 03)
    - .planning/phases/12-open-source-public-release-mit-license-comprehensive-readme-/12-CONTEXT.md (D-23b screenshot requirements, D-24a .planning/ sanity check)
    - frontend/package.json (dev script command)
    - backend/pyproject.toml (uvicorn command)
  </read_first>
  <action>
    **A. Start dev servers:**
    1. Start the backend: `cd backend && uvicorn app.main:app --port 8000 &` (or use the project's standard port). If backend fails to start due to missing DB or config, proceed with frontend-only screenshots (the frontend has enough UI to capture).
    2. Start the frontend: `cd frontend && pnpm dev --port 8923 &` (port 8923 per project convention).
    3. Wait for both servers to be ready.

    If the dev servers cannot start (missing dependencies, DB issues), use an alternative approach:
    - Build the frontend: `cd frontend && pnpm build`
    - Serve statically: `cd frontend && pnpm preview --port 8923`
    - Capture whatever UI is available (login page, static components)

    **B. Capture screenshots using MCP chrome-devtools (D-23b):**
    Use chrome-devtools MCP tools to navigate to pages and capture screenshots.

    Required screenshots (at minimum per D-23b):
    1. **Login page** — `http://localhost:8923/login` or the landing page
       Save to: `docs/images/login.png`
    2. **Chat interface** — `http://localhost:8923/chat` or `/intake`
       Save to: `docs/images/chat.png`
    3. **Dashboard** — `http://localhost:8923/dashboard` or `/`
       Save to: `docs/images/dashboard.png`
    4. **Admin configuration** — `http://localhost:8923/admin`
       Save to: `docs/images/admin.png`
    5. **Visualization view** — `http://localhost:8923/visualization` or similar
       Save to: `docs/images/visualization.png`

    For each screenshot:
    - Navigate to the URL using chrome-devtools `navigate_page`
    - Wait for content to load using `wait_for`
    - Take screenshot to `$HOME/screenshot-{name}.png` using `take_screenshot`
    - Copy to `docs/images/{name}.png`
    - Clean up the `$HOME/screenshot-{name}.png` file

    If chrome-devtools MCP is unavailable or pages fail to load (auth required, backend not running), create placeholder text files in docs/images/ explaining what each screenshot should show, and add a note in the README: "Screenshots will be added once the development environment is fully configured."

    **C. Update README.md Screenshots section:**
    Find the Screenshots section (created by Plan 03) and replace the placeholder content with actual image references:
    ```markdown
    ## Screenshots

    ### Login
    ![Login page](docs/images/login.png)

    ### Chat Interface
    ![Chat interface for intake conversation](docs/images/chat.png)

    ### Intake Dashboard
    ![Dashboard showing intake list and status](docs/images/dashboard.png)

    ### Admin Configuration
    ![Admin settings panel](docs/images/admin.png)

    ### Analysis Visualization
    ![Visualization of fact-claim-element relationships](docs/images/visualization.png)
    ```

    **D. .planning/ sanity check (D-24a):**
    Scan the .planning/ directory for accidentally committed secrets or internal-only references:
    ```bash
    grep -rn "AKIA\|sk-\|ghp_\|password=\|secret=" .planning/ | grep -v "change-me\|example\|placeholder\|ALEA_SECRET_KEY\|ALEA_DB_PASSWORD"
    grep -rn "slack.com\|notion.so\|atlassian.net\|jira\." .planning/
    grep -rn "@[a-z]*\.[a-z]" .planning/ | grep -v "@example\|@test\|@localhost\|@anthropic\|@gmail\|@github"
    ```
    Record findings. If any real secrets are found in .planning/, they must be removed before the repo goes public.

    **E. Stop dev servers:**
    Kill any background processes started in step A.
  </action>
  <verify>
    <automated>ls docs/images/*.png 2>/dev/null | wc -l && grep -c "docs/images/" README.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/images/ directory contains at least 3 image files (png or placeholder text files)
    - README.md contains "docs/images/" references (at least 3 image links)
    - README.md ## Screenshots section contains actual image markdown syntax `![` or explicit placeholder note
    - .planning/ sanity check completed with findings recorded (zero real secrets expected)
    - Dev servers stopped (no orphaned background processes)
  </acceptance_criteria>
  <done>Screenshots captured (or placeholders created) and embedded in README. .planning/ directory verified clean for public release per D-24a.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final README and release files verification</name>
  <files>README.md, LICENSE, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, THIRD_PARTY_LICENSES.md</files>
  <action>
    Present the complete set of release artifacts to the user for final review before the repository goes public. This is a human verification checkpoint — no automated changes are made.

    What was built across all Phase 12 plans:
    - Updated LICENSE with "Copyright (c) 2026 Damien Riehl and ALEA Institute"
    - Created SECURITY.md with GitHub private vulnerability reporting
    - Created CONTRIBUTING.md with PR guidelines (no CLA)
    - Created CODE_OF_CONDUCT.md with Contributor Covenant 2.1
    - Created THIRD_PARTY_LICENSES.md with all dependency attributions
    - Comprehensive README.md (~1500-2500 lines) with:
      - Architecture diagram (Mermaid)
      - All 12 use cases (4 core + 8 specialty)
      - Multi-language, autonomy modes, ephemeral/privacy, FOLIO grounding sections
      - Complete security documentation (AES-256-GCM, JWT, audit, consent, right-to-delete, tenant isolation, etc.)
      - Full configuration reference (every ALEA_* env var + org-level fields)
      - 5 scenario walkthroughs with concrete .env examples
      - 4 deployment topology diagrams (Mermaid)
      - Data flow / security model diagram (Mermaid)
      - UI screenshots
      - Forward-looking roadmap
    - Pre-flight audits completed: secret scan, dependency licenses, PII scrub, infra linting
    - .planning/ directory verified clean

    Verification steps for the human:
    1. Open README.md and scroll through the entire document:
       - Does the tagline read "Open-source, privacy-first legal intake for access to justice"?
       - Does the Table of Contents link to all sections?
       - Do the Mermaid diagrams render correctly in GitHub preview? (gh repo view --web or GitHub.com)
       - Are the use cases written in plain language that a non-technical reader can follow?
       - Does the security section accurately describe the encryption, auth, and privacy features?
       - Does Cloud KMS say "Planned" or "roadmap" (not "supported")?
       - Does the configuration reference table include every env var you expect?
       - Do the scenario walkthroughs make sense for their target audiences?
    2. Open LICENSE and confirm the copyright line.
    3. Open SECURITY.md and confirm GitHub private vulnerability reporting is the disclosure channel.
    4. Open CONTRIBUTING.md and confirm it covers: bugs, features, dev setup, PR process, commit conventions.
    5. Open CODE_OF_CONDUCT.md and confirm it is the Contributor Covenant 2.1.
    6. Open THIRD_PARTY_LICENSES.md and confirm dependencies are listed with SPDX identifiers.
    7. Check docs/images/ for screenshots (or placeholders if screenshots could not be captured).

    Resume signal: Type "approved" to finalize Phase 12, or describe any issues to address.
  </action>
  <verify>
    <automated>test -f README.md && test -f LICENSE && test -f SECURITY.md && test -f CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md && test -f THIRD_PARTY_LICENSES.md && echo "all files exist"</automated>
  </verify>
  <acceptance_criteria>
    - Human has reviewed and approved all release artifacts
    - All 6 files exist: README.md, LICENSE, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, THIRD_PARTY_LICENSES.md
    - docs/images/ contains screenshot files or documented placeholders
    - No blocking issues identified by human reviewer
  </acceptance_criteria>
  <done>Human approved the final state of all release artifacts. Phase 12 is complete. Repository is ready for public release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ -> public repo | Planning history becomes world-readable; must not contain secrets |
| docs/images/ -> public repo | Screenshots must not show real PII or internal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | Information Disclosure | .planning/ directory | mitigate | Grep scan for secrets/PII in Task 1D; findings block release |
| T-12-16 | Information Disclosure | Screenshots | mitigate | Screenshots taken from dev environment with test data only; no real PII visible |
| T-12-17 | Tampering | README accuracy | mitigate | Human checkpoint (Task 2) verifies all claims before public release |
</threat_model>

<verification>
1. `ls docs/images/` -- screenshot files exist
2. `grep "docs/images/" README.md` -- README references screenshots
3. `grep "Copyright (c) 2026 Damien Riehl and ALEA Institute" LICENSE` -- correct copyright
4. `test -f SECURITY.md && test -f CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md && test -f THIRD_PARTY_LICENSES.md` -- all community files exist
5. Human approved final review
</verification>

<success_criteria>
All release artifacts are complete: LICENSE, README.md, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, THIRD_PARTY_LICENSES.md, docs/images/. The README is factually accurate, comprehensive, and written for legal aid / court program leaders. All pre-flight audits passed. Human has approved the final state for public release.
</success_criteria>

<output>
After completion, create `.planning/phases/12-open-source-public-release-mit-license-comprehensive-readme-/12-05-SUMMARY.md`
</output>
