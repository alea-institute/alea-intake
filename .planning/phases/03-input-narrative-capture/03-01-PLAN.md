---
phase: 03-input-narrative-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/intake.py
  - backend/app/models/audio.py
  - backend/app/models/document.py
  - backend/app/models/fact.py
  - backend/app/models/__init__.py
  - backend/app/config.py
  - backend/app/services/intake/__init__.py
  - backend/app/services/intake/session_service.py
  - backend/app/services/intake/conversation.py
  - backend/app/services/intake/message_pipeline.py
  - backend/app/routers/intake.py
  - backend/tests/test_intake_chat.py
  - backend/tests/test_message_pipeline.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-05

must_haves:
  truths:
    - "Consumer can type a message via WebSocket and receive acknowledgment with message_id and sequence_number"
    - "System LLM-generated question is sent back through the same WebSocket connection"
    - "All chat messages are normalized into a common NormalizedContent representation with source spans"
    - "Multi-party intakes track per-party message attribution"
    - "Sessions support pause/resume for multi-session mode"
  artifacts:
    - path: "backend/app/models/intake.py"
      provides: "Intake, IntakeParty, IntakeSession, Message models"
      contains: "class Intake(TenantBase)"
    - path: "backend/app/models/audio.py"
      provides: "AudioRecording, Transcript models"
      contains: "class AudioRecording(TenantBase)"
    - path: "backend/app/models/document.py"
      provides: "UploadedDocument, DocumentExtraction models"
      contains: "class UploadedDocument(TenantBase)"
    - path: "backend/app/models/fact.py"
      provides: "ExtractedFact, FactSourceSpan models"
      contains: "class ExtractedFact(TenantBase)"
    - path: "backend/app/services/intake/message_pipeline.py"
      provides: "Unified normalization pipeline for all modalities"
      contains: "async def process_message"
    - path: "backend/app/routers/intake.py"
      provides: "WebSocket chat endpoint and REST intake endpoints"
      contains: "async def intake_websocket"
  key_links:
    - from: "backend/app/routers/intake.py"
      to: "backend/app/services/intake/session_service.py"
      via: "IntakeSessionService for session lifecycle"
      pattern: "IntakeSessionService"
    - from: "backend/app/routers/intake.py"
      to: "backend/app/services/intake/message_pipeline.py"
      via: "process_message for normalizing incoming messages"
      pattern: "process_message"
    - from: "backend/app/services/intake/session_service.py"
      to: "backend/app/services/intake/conversation.py"
      via: "ConversationService for LLM question generation"
      pattern: "ConversationService"
---

<objective>
Build the intake data layer (all DB models for intake, audio, documents, facts), the unified message normalization pipeline, and the real-time WebSocket chat system with LLM-guided conversation.

Purpose: Establishes the foundational data models and chat infrastructure that all other Phase 3 plans build upon. Voice/ASR (Plan 02), document processing (Plan 02), professional mode (Plan 03), and fact extraction (Plan 03) all depend on the models and pipeline created here.

Output: Working WebSocket chat with LLM-generated follow-up questions, message normalization pipeline, and all Phase 3 DB models.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-input-narrative-capture/03-CONTEXT.md
@.planning/phases/03-input-narrative-capture/03-RESEARCH.md
@.planning/phases/03-input-narrative-capture/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts from existing codebase that executor needs. -->

From backend/app/db/base.py:
```python
class TenantBase(DeclarativeBase):
    """Base class for models that live in per-tenant schemas."""
    metadata = tenant_metadata  # MetaData(schema="tenant", naming_convention=convention)

class SharedBase(DeclarativeBase):
    """Base class for models that live in the shared schema."""
    metadata = shared_metadata
```

From backend/app/config.py:
```python
class Settings(BaseSettings):
    # ... existing fields ...
    model_config = SettingsConfigDict(env_file=".env", env_prefix="ALEA_")
```

From backend/app/models/shared.py:
```python
class Organization(SharedBase):
    __tablename__ = "organizations"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    slug: Mapped[str] = mapped_column(String(100), unique=True, nullable=False)
    settings: Mapped[dict | None] = mapped_column(JSON, nullable=True, default=dict)
    auth_mode: Mapped[str] = mapped_column(String(20), default="email_password")
    llm_data_policy: Mapped[str] = mapped_column(String(20), default="cloud_optout")
    # ...
```

From backend/app/services/llm_service.py:
```python
_PROVIDER_MODEL_MAP: dict[str, type] = { "openai": ..., "anthropic": ..., "google": ..., "vllm": ... }

class LLMService:
    def __init__(self, org_config: OrganizationConfig | None = None, ...): ...
    def get_client_config(self) -> dict[str, Any]: ...
    async def check_connection(self) -> dict[str, str]: ...

def get_llm_service(org_config: OrganizationConfig | None = None, ...) -> LLMService: ...
```

From backend/app/main.py:
```python
# Router registration pattern:
app.include_router(auth_router)
app.include_router(organizations_router)
# ...

# Lifespan pattern:
@asynccontextmanager
async def lifespan(app: FastAPI):
    settings = get_settings()
    loop = asyncio.get_event_loop()
    # ... init services ...
    yield
    # ... cleanup ...
```

From backend/tests/conftest.py:
```python
def get_test_settings() -> Settings:
    return Settings(
        secret_key="test-secret-key-for-testing-only-not-production",
        database_backend="sqlite",
        sqlite_path=":memory:",
        debug=False,
        cors_origins=["http://localhost:5173"],
    )
```

From backend/app/models/__init__.py:
```python
# Re-export all models for convenient importing
from app.models.audit import AuditLog
from app.models.consent import ConsentRecord, ConsentTemplate
# ... (all models listed in __all__)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Intake DB models, config settings, and message normalization pipeline</name>
  <files>
    backend/app/models/intake.py
    backend/app/models/audio.py
    backend/app/models/document.py
    backend/app/models/fact.py
    backend/app/models/__init__.py
    backend/app/config.py
    backend/app/services/intake/__init__.py
    backend/app/services/intake/message_pipeline.py
    backend/tests/test_message_pipeline.py
  </files>
  <read_first>
    backend/app/db/base.py
    backend/app/models/shared.py
    backend/app/models/__init__.py
    backend/app/models/folio_concepts.py
    backend/app/config.py
    backend/tests/conftest.py
    .planning/phases/03-input-narrative-capture/03-RESEARCH.md
    .planning/phases/03-input-narrative-capture/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Intake model has columns: id (PK), org_id (int), status (String, default "active"), created_by_user_id (nullable int), session_mode (String, default "multi_session"), metadata_json (JSON nullable), created_at, updated_at
    - Test 2: IntakeParty model has columns: id (PK), intake_id (FK intakes.id), user_id (nullable int), role_in_intake (String, default "primary"), label (nullable String), created_at
    - Test 3: IntakeSession model has columns: id (PK), intake_id (FK intakes.id), status (String, default "active"), started_at, ended_at (nullable)
    - Test 4: Message model has columns: id (PK), session_id (FK intake_sessions.id), party_id (nullable FK intake_parties.id), sender_type (String: "consumer"/"professional"/"system"), modality (String: "text"/"voice"/"document"/"professional_note"), content_encrypted (LargeBinary nullable), normalized_text (LargeBinary nullable), metadata_json (JSON nullable), sequence_number (int), created_at
    - Test 5: AudioRecording model has columns: id (PK), message_id (FK messages.id), intake_id (FK intakes.id), file_path_encrypted (LargeBinary nullable), original_format (String), duration_seconds (Float nullable), file_size_bytes (int nullable), storage_policy (String, default "store_both"), created_at
    - Test 6: Transcript model has columns: id (PK), recording_id (FK audio_recordings.id), text_encrypted (LargeBinary nullable), status (String, default "pending_review"), asr_provider (String), segments_json (JSON nullable), language (String nullable), confidence (Float nullable), created_at, reviewed_at (nullable)
    - Test 7: UploadedDocument model has columns: id (PK), message_id (FK messages.id), intake_id (FK intakes.id), file_path_encrypted (LargeBinary nullable), original_filename (String), mime_type (String), file_size_bytes (int), page_count (int nullable), extraction_status (String, default "pending"), created_at
    - Test 8: DocumentExtraction model has columns: id (PK), document_id (FK uploaded_documents.id), full_text_encrypted (LargeBinary nullable), elements_json (JSON nullable), extraction_method (String), created_at
    - Test 9: ExtractedFact model has columns: id (PK), intake_id (FK intakes.id), message_id (FK messages.id), party_id (nullable FK intake_parties.id), assertion_text (Text), fact_type (String), entity_type (String nullable), confidence (Float), is_active (Boolean, default True), superseded_by_id (int nullable), visibility (String, default "internal"), metadata_json (JSON nullable), created_at
    - Test 10: FactSourceSpan model has columns: id (PK), fact_id (FK extracted_facts.id), message_id (FK messages.id), start_char (int), end_char (int), timestamp_start_sec (Float nullable), timestamp_end_sec (Float nullable), page_number (int nullable), paragraph_index (int nullable)
    - Test 11: process_message("text", content) returns NormalizedContent with text, source_type="chat", source_spans list
    - Test 12: process_message("professional_note", content) returns NormalizedContent with source_type="professional_note"
    - Test 13: NormalizedContent dataclass has fields: text (str), elements (list), source_type (str), source_id (str), source_spans (list), party_id (int|None)
  </behavior>
  <action>
    **1. Create backend/app/models/intake.py** with these exact models (all extend TenantBase):
    - `Intake`: __tablename__ = "intakes". Columns: id (Integer PK autoincrement), org_id (Integer not null), status (String(20) default "active"), created_by_user_id (Integer nullable), session_mode (String(20) default "multi_session"), metadata_json (JSON nullable), created_at (server_default=func.now()), updated_at (server_default=func.now(), onupdate=func.now()).
    - `IntakeParty`: __tablename__ = "intake_parties". Columns: id (Integer PK autoincrement), intake_id (Integer FK "intakes.id" not null), user_id (Integer nullable), role_in_intake (String(50) default "primary"), label (String(255) nullable), created_at (server_default=func.now()).
    - `IntakeSession`: __tablename__ = "intake_sessions". Columns: id (Integer PK autoincrement), intake_id (Integer FK "intakes.id" not null), status (String(20) default "active"), started_at (server_default=func.now()), ended_at (datetime nullable).
    - `Message`: __tablename__ = "messages". Columns: id (Integer PK autoincrement), session_id (Integer FK "intake_sessions.id" not null), party_id (Integer FK "intake_parties.id" nullable), sender_type (String(20) not null), modality (String(20) not null), content_encrypted (LargeBinary nullable), normalized_text (LargeBinary nullable), metadata_json (JSON nullable), sequence_number (Integer not null), created_at (server_default=func.now()).

    **2. Create backend/app/models/audio.py** with models (all TenantBase):
    - `AudioRecording`: __tablename__ = "audio_recordings". Columns: id (Integer PK autoincrement), message_id (Integer FK "messages.id" not null), intake_id (Integer FK "intakes.id" not null), file_path_encrypted (LargeBinary nullable), original_format (String(20) not null), duration_seconds (Float nullable), file_size_bytes (Integer nullable), storage_policy (String(20) default "store_both"), created_at (server_default=func.now()).
    - `Transcript`: __tablename__ = "transcripts". Columns: id (Integer PK autoincrement), recording_id (Integer FK "audio_recordings.id" not null), text_encrypted (LargeBinary nullable), status (String(20) default "pending_review"), asr_provider (String(50) not null), segments_json (JSON nullable), language (String(10) nullable), confidence (Float nullable), created_at (server_default=func.now()), reviewed_at (datetime nullable).

    **3. Create backend/app/models/document.py** with models (all TenantBase):
    - `UploadedDocument`: __tablename__ = "uploaded_documents". Columns: id (Integer PK autoincrement), message_id (Integer FK "messages.id" not null), intake_id (Integer FK "intakes.id" not null), file_path_encrypted (LargeBinary nullable), original_filename (String(255) not null), mime_type (String(100) not null), file_size_bytes (Integer not null), page_count (Integer nullable), extraction_status (String(20) default "pending"), created_at (server_default=func.now()).
    - `DocumentExtraction`: __tablename__ = "document_extractions". Columns: id (Integer PK autoincrement), document_id (Integer FK "uploaded_documents.id" not null), full_text_encrypted (LargeBinary nullable), elements_json (JSON nullable), extraction_method (String(50) not null), created_at (server_default=func.now()).

    **4. Create backend/app/models/fact.py** with models (all TenantBase):
    - `ExtractedFact`: __tablename__ = "extracted_facts". Columns: id (Integer PK autoincrement), intake_id (Integer FK "intakes.id" not null), message_id (Integer FK "messages.id" not null), party_id (Integer FK "intake_parties.id" nullable), assertion_text (Text not null), fact_type (String(50) not null), entity_type (String(50) nullable), confidence (Float not null), is_active (Boolean default True), superseded_by_id (Integer nullable), visibility (String(20) default "internal"), metadata_json (JSON nullable), created_at (server_default=func.now()).
    - `FactSourceSpan`: __tablename__ = "fact_source_spans". Columns: id (Integer PK autoincrement), fact_id (Integer FK "extracted_facts.id" not null), message_id (Integer FK "messages.id" not null), start_char (Integer not null), end_char (Integer not null), timestamp_start_sec (Float nullable), timestamp_end_sec (Float nullable), page_number (Integer nullable), paragraph_index (Integer nullable).

    **5. Update backend/app/models/__init__.py** to import and re-export all new models:
    ```python
    from app.models.intake import Intake, IntakeParty, IntakeSession, Message
    from app.models.audio import AudioRecording, Transcript
    from app.models.document import UploadedDocument, DocumentExtraction
    from app.models.fact import ExtractedFact, FactSourceSpan
    ```
    Add all 10 new model names to `__all__`.

    **6. Update backend/app/config.py** -- add these settings to the Settings class:
    ```python
    # Intake
    intake_upload_dir: str = "./data/uploads"
    intake_max_file_size_mb: int = 50
    intake_max_page_count: int = 200
    intake_max_recording_duration_sec: int = 900  # 15 minutes
    intake_default_session_mode: str = "multi_session"
    intake_fact_visibility: str = "internal"  # internal or consumer_visible

    # ASR
    asr_default_provider: str = "whisper"
    whisper_endpoint: str = "http://localhost:8790"
    asr_audio_storage_policy: str = "store_both"  # store_both, transcript_only, ephemeral
    ```

    **7. Create backend/app/services/intake/__init__.py** as empty init file.

    **8. Create backend/app/services/intake/message_pipeline.py** with:
    - `@dataclass TextElement`: text (str), element_type (str: "paragraph"/"heading"/"table"/"list_item"), page (int|None = None), section_path (str|None = None)
    - `@dataclass SourceSpan`: start_char (int), end_char (int), source_message_id (int|None = None), source_timestamp_start (float|None = None), source_timestamp_end (float|None = None), source_page (int|None = None), source_paragraph (int|None = None)
    - `@dataclass NormalizedContent`: text (str), elements (list[TextElement]), source_type (str), source_id (str), source_spans (list[SourceSpan]), party_id (int|None = None)
    - `def normalize_text(content: str, message_id: int, party_id: int | None = None) -> NormalizedContent`: Creates NormalizedContent with source_type="chat", single paragraph TextElement, single SourceSpan covering full text.
    - `def normalize_professional_note(content: str, message_id: int, party_id: int | None = None) -> NormalizedContent`: Same as text but source_type="professional_note".
    - `async def process_message(modality: str, content: str, message_id: int, party_id: int | None = None, **kwargs) -> NormalizedContent`: Routes to appropriate normalizer based on modality ("text" -> normalize_text, "professional_note" -> normalize_professional_note). For "voice" and "document" modalities, raise NotImplementedError("Voice/document normalization handled by respective services") -- these are wired in Plan 02.

    **9. Create backend/tests/test_message_pipeline.py** with tests for:
    - normalize_text returns NormalizedContent with correct text, source_type="chat", SourceSpan with start_char=0, end_char=len(content)
    - normalize_professional_note returns NormalizedContent with source_type="professional_note"
    - process_message("text", ...) delegates to normalize_text
    - process_message("professional_note", ...) delegates to normalize_professional_note
    - NormalizedContent dataclass has all required fields
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/alea-intake/backend && python -m pytest tests/test_message_pipeline.py -x --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/intake.py contains `class Intake(TenantBase)` and `class IntakeParty(TenantBase)` and `class IntakeSession(TenantBase)` and `class Message(TenantBase)`
    - backend/app/models/audio.py contains `class AudioRecording(TenantBase)` and `class Transcript(TenantBase)`
    - backend/app/models/document.py contains `class UploadedDocument(TenantBase)` and `class DocumentExtraction(TenantBase)`
    - backend/app/models/fact.py contains `class ExtractedFact(TenantBase)` and `class FactSourceSpan(TenantBase)`
    - backend/app/models/__init__.py contains imports for all 10 new model classes
    - backend/app/config.py contains `intake_upload_dir` and `intake_max_file_size_mb` and `asr_default_provider` and `whisper_endpoint`
    - backend/app/services/intake/message_pipeline.py contains `class NormalizedContent` and `async def process_message` and `def normalize_text`
    - backend/tests/test_message_pipeline.py exits 0
    - `cd backend && python -m pytest tests/ -x --timeout=30` exits 0 (no regressions)
  </acceptance_criteria>
  <done>All 10 intake DB models created with correct column types. Config settings added for intake, ASR, and file storage. Message normalization pipeline handles text and professional_note modalities with NormalizedContent dataclass. All existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebSocket chat system with session lifecycle and LLM conversation</name>
  <files>
    backend/app/services/intake/session_service.py
    backend/app/services/intake/conversation.py
    backend/app/routers/intake.py
    backend/tests/test_intake_chat.py
  </files>
  <read_first>
    backend/app/models/intake.py
    backend/app/services/intake/message_pipeline.py
    backend/app/services/llm_service.py
    backend/app/routers/auth.py
    backend/app/routers/folio_admin.py
    backend/app/core/exceptions.py
    backend/app/middleware/tenant.py
    backend/app/db/engine.py
    backend/tests/conftest.py
    .planning/phases/03-input-narrative-capture/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: POST /api/v1/intake creates an Intake record with status="active" and returns {id, status, session_mode, created_at}
    - Test 2: POST /api/v1/intake/{intake_id}/session creates an IntakeSession with status="active"
    - Test 3: POST /api/v1/intake/{intake_id}/party creates an IntakeParty with label and role_in_intake
    - Test 4: WebSocket connect to /api/ws/intake/{session_id}?token={jwt} accepts connection when JWT is valid
    - Test 5: WebSocket connect with invalid/missing token closes with code 4001
    - Test 6: Sending text_message via WebSocket stores Message in DB with modality="text", returns message_ack with message_id and sequence_number
    - Test 7: After text_message, server sends system_message (LLM response) or llm_stream tokens
    - Test 8: Sending session_pause via WebSocket updates IntakeSession.status to "paused"
    - Test 9: GET /api/v1/intake/{intake_id}/messages returns paginated message history
    - Test 10: GET /api/v1/intake lists intakes for the authenticated user's tenant
  </behavior>
  <action>
    **1. Create backend/app/services/intake/session_service.py** with `IntakeSessionService`:
    - `__init__(self, session: AsyncSession)`: Stores DB session.
    - `async def create_intake(self, org_id: int, user_id: int | None, session_mode: str = "multi_session") -> Intake`: Creates Intake record with status="active", returns it.
    - `async def create_session(self, intake_id: int) -> IntakeSession`: Creates IntakeSession with status="active" for the intake.
    - `async def add_party(self, intake_id: int, user_id: int | None, role_in_intake: str = "primary", label: str | None = None) -> IntakeParty`: Creates IntakeParty.
    - `async def get_next_sequence(self, session_id: int) -> int`: SELECT MAX(sequence_number) FROM messages WHERE session_id=X, returns max+1 or 1.
    - `async def store_message(self, session_id: int, sender_type: str, modality: str, content: str, party_id: int | None = None, metadata_json: dict | None = None) -> Message`: Creates Message with content_encrypted set to content.encode() (actual encryption deferred to EncryptionContext integration), sequence_number from get_next_sequence.
    - `async def pause_session(self, session_id: int) -> IntakeSession`: Updates session status to "paused", sets ended_at.
    - `async def resume_session(self, session_id: int) -> IntakeSession`: Updates session status to "active", clears ended_at.
    - `async def list_intakes(self, org_id: int) -> list[Intake]`: SELECT * FROM intakes WHERE org_id=X ORDER BY created_at DESC.
    - `async def get_messages(self, session_id: int, limit: int = 50, offset: int = 0) -> list[Message]`: SELECT messages ORDER BY sequence_number with pagination.

    **2. Create backend/app/services/intake/conversation.py** with `ConversationService`:
    - `__init__(self, llm_service: LLMService)`: Stores LLM service reference.
    - `async def generate_response(self, messages: list[dict], system_prompt: str | None = None) -> str`: Calls llm_service to generate a conversational response. Uses default system prompt from INTAKE_SYSTEM_PROMPT if none provided.
    - `INTAKE_SYSTEM_PROMPT`: Module-level constant with the prompt from 03-UI-SPEC.md: "Tell me about your legal situation in your own words. You can type, record your voice, or upload documents -- whatever is easiest for you." The system prompt instructs the LLM to ask clarifying follow-up questions about the consumer's legal situation, one topic at a time, using plain language.
    - `async def generate_welcome_message(self, session_mode: str, is_professional: bool = False) -> str`: Returns the appropriate welcome message from UI-SPEC: consumer welcome or professional welcome.

    **3. Create backend/app/routers/intake.py** with:
    - `router = APIRouter(prefix="/api/v1/intake", tags=["intake"])`
    - `IntakeConnectionManager` class: manages active WebSocket connections per session_id. Methods: connect(ws, session_id), disconnect(ws, session_id), send_to_session(session_id, message_dict).
    - Module-level `manager = IntakeConnectionManager()`.
    - `POST /` -- Create intake. Requires authentication (get_current_active_user). Gets org_id from request.state.tenant_slug via DB lookup or from the user's org. Creates intake + primary party + first session. Returns {id, status, session_mode, created_at, session_id, party_id}.
    - `GET /` -- List intakes for the tenant. Returns list of intake summaries.
    - `GET /{intake_id}/messages` -- Get message history. Query params: session_id (required), limit (default 50), offset (default 0).
    - `POST /{intake_id}/party` -- Add a party to the intake. Body: {role_in_intake, label, user_id?}.
    - `POST /{intake_id}/session` -- Create a new session for the intake (multi-session resume).
    - WebSocket endpoint `@router.websocket("/ws/{session_id}")`:
      1. Extract token from query param `?token=`.
      2. Validate JWT using same logic as auth dependency (decode with secret_key, verify exp). If invalid, close with code 4001. If user lacks access to session, close with code 4003.
      3. Accept connection. Add to manager.
      4. Send session_state message with current facts_count and messages_count.
      5. Message loop: receive_json, route by type field:
         - `text_message`: Store message via IntakeSessionService.store_message. Run normalize_text from message_pipeline. Send message_ack {type: "message_ack", message_id, sequence_number}. Generate LLM response via ConversationService (mock/stub if LLM not available). Send system_message {type: "system_message", content, message_id}.
         - `session_pause`: Call IntakeSessionService.pause_session. Send session_state.
         - `typing_indicator`: Broadcast to other connections on same session.
      6. On WebSocketDisconnect: remove from manager.

    **4. Create backend/tests/test_intake_chat.py** with:
    - Test fixtures that create test intake, session, party in DB.
    - `test_create_intake`: POST /api/v1/intake with auth returns 201 with {id, status, session_mode}.
    - `test_list_intakes`: GET /api/v1/intake returns list.
    - `test_websocket_auth_reject`: Connect to /api/ws/intake/{session_id} without token, assert close code 4001.
    - `test_websocket_text_message`: Connect with valid token, send text_message, receive message_ack with message_id and sequence_number.
    - `test_websocket_session_pause`: Send session_pause, verify IntakeSession.status becomes "paused".
    - `test_get_messages`: After sending messages, GET /api/v1/intake/{id}/messages returns them ordered by sequence_number.
    - Use `httpx.AsyncClient` for REST and `TestClient` WebSocket context manager for WebSocket tests.
    - Mock the ConversationService.generate_response to return a fixed string to avoid LLM dependency in tests.
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/alea-intake/backend && python -m pytest tests/test_intake_chat.py -x --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/intake/session_service.py contains `class IntakeSessionService` with methods `create_intake`, `store_message`, `pause_session`, `get_next_sequence`
    - backend/app/services/intake/conversation.py contains `class ConversationService` with `async def generate_response` and `INTAKE_SYSTEM_PROMPT`
    - backend/app/routers/intake.py contains `class IntakeConnectionManager` and `async def intake_websocket` and `router = APIRouter(prefix="/api/v1/intake"`
    - backend/app/routers/intake.py contains WebSocket auth validation with close code 4001
    - backend/tests/test_intake_chat.py contains `test_create_intake` and `test_websocket_text_message` and `test_websocket_auth_reject`
    - backend/tests/test_intake_chat.py exits 0
    - `cd backend && python -m pytest tests/ -x --timeout=30` exits 0 (no regressions)
  </acceptance_criteria>
  <done>WebSocket chat endpoint accepts authenticated connections, receives text messages, stores them in the DB with sequence numbers, normalizes via message pipeline, generates LLM follow-up questions, and returns message_ack + system_message. Session pause/resume works. REST endpoints for intake CRUD and message history functional. All tests pass.</done>
</task>

</tasks>

<verification>
1. All 10 DB models (Intake, IntakeParty, IntakeSession, Message, AudioRecording, Transcript, UploadedDocument, DocumentExtraction, ExtractedFact, FactSourceSpan) exist with correct columns and TenantBase inheritance
2. Config settings for intake, ASR, and file storage are present
3. Message normalization pipeline handles text and professional_note modalities
4. WebSocket chat connects with JWT auth, sends/receives messages, stores in DB
5. Session lifecycle (create, pause, resume) works correctly
6. REST endpoints for intake CRUD and message history exist
7. All existing Phase 1 and Phase 2 tests still pass
</verification>

<success_criteria>
- `cd backend && python -m pytest tests/test_message_pipeline.py tests/test_intake_chat.py -x --timeout=30` exits 0
- `cd backend && python -m pytest tests/ -x --timeout=60` exits 0 (full suite, no regressions)
- WebSocket endpoint at /api/ws/intake/{session_id} authenticates via JWT and handles text_message, session_pause, typing_indicator
- All models importable via `from app.models import Intake, IntakeParty, IntakeSession, Message, AudioRecording, Transcript, UploadedDocument, DocumentExtraction, ExtractedFact, FactSourceSpan`
</success_criteria>

<output>
After completion, create `.planning/phases/03-input-narrative-capture/03-01-SUMMARY.md`
</output>
