---
phase: 14-railway-dev-server-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: []   # No source edits — git merge + push only
autonomous: false    # Merge to master + push (substantive; pre-authorized but a release-shaping action)
requirements: []

must_haves:
  truths:
    - "master contains the Phase 13 practice-area feature, the $PORT deploy fix, the uv-from-astral fix, and the Phase 14 asyncpg fix from plan 14-01"
    - "demo/practice-customization fast-forwards cleanly onto master (no merge commit, no divergence)"
    - "origin/master is updated and matches local master"
  artifacts: []
  key_links:
    - from: "demo/practice-customization"
      to: "master"
      via: "fast-forward merge then push"
      pattern: "git merge --ff-only|git push origin master"
---

<objective>
Land all in-flight code on `master` so the dev server has something correct to deploy.
Fast-forward merge `demo/practice-customization` -> `master` (CONTEXT verified clean FF:
master tip `ebd6de0` is the merge-base, demo branch is ~10 commits ahead, zero
divergence) and push `master`. This brings: the Phase 13 practice-area feature, the
`$PORT` Railway deploy fix, the uv-from-astral Dockerfile fix, and the Phase 14 asyncpg
fix (committed by plan 14-01).

Purpose: The dev server will track `master` (locked decision). The asyncpg fix MUST be
on master before the dev redeploy in plan 14-03, so this merge depends on 14-01.
Output: `master` updated locally and on origin, fast-forward only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md
@.planning/phases/14-railway-dev-server-consolidation/14-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm fast-forward is still clean before merging</name>
  <read_first>
    - .planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md ("Merge is a clean fast-forward" finding)
  </read_first>
  <what-built>
    Re-verify the merge is still a clean fast-forward now that plan 14-01 has added the
    asyncpg-fix commit(s) to demo/practice-customization.
  </what-built>
  <how-to-verify>
    Run, from /home/damienriehl/Coding Projects/alea-intake:
    1. `git fetch origin`
    2. `git rev-parse master` and `git merge-base master demo/practice-customization`
       — these MUST be equal (master tip is the merge-base => no divergence => FF possible).
    3. `git log --oneline master..demo/practice-customization` — review the commits about
       to land (should include the practice-area feature, $PORT fix, uv-from-astral fix,
       and the 14-01 asyncpg fix).
    Confirm the two SHAs match and the commit list looks right.
  </how-to-verify>
  <acceptance_criteria>
    - `git rev-parse master` equals `git merge-base master demo/practice-customization` (clean FF; no divergence).
    - The `master..demo/practice-customization` commit list includes the 14-01 asyncpg-fix commit(s).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the fast-forward merge and push, or describe any divergence found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fast-forward merge to master and push</name>
  <read_first>
    - .planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md (constraint: pushes pre-authorized for this repo)
  </read_first>
  <action>
    Switch to `master` (`git checkout master`), then fast-forward merge the feature
    branch with `git merge --ff-only demo/practice-customization`. The `--ff-only` flag
    guarantees no merge commit is created and FAILS LOUDLY if a fast-forward is not
    possible (treat any failure as a stop — do not create a merge commit, re-raise to
    the user). After a successful FF, push with `git push origin master`. Pushes are
    pre-authorized for this repo per CONTEXT. Do not force-push. Do not rebase. Leave
    `demo/practice-customization` intact (retiring branches is out of scope here).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake" && git rev-parse master && git rev-parse demo/practice-customization && git rev-parse origin/master && [ "$(git rev-parse master)" = "$(git rev-parse demo/practice-customization)" ] && [ "$(git rev-parse master)" = "$(git rev-parse origin/master)" ] && echo "FF MERGE + PUSH OK"</automated>
  </verify>
  <acceptance_criteria>
    - `git merge --ff-only demo/practice-customization` succeeded with no merge commit.
    - `git rev-parse master` == `git rev-parse demo/practice-customization` (FF landed all commits).
    - `git rev-parse origin/master` == `git rev-parse master` (push succeeded).
    - `git log -1 master` shows the demo branch tip; `master` now contains `backend/app/db/engine.py` with `pool_pre_ping=True` (`git show master:backend/app/db/engine.py | grep pool_pre_ping`).
  </acceptance_criteria>
  <done>master fast-forwarded to the feature tip and pushed to origin; asyncpg fix + practice-area feature + deploy fixes are all on master.</done>
</task>

</tasks>

<verification>
- `master`, `demo/practice-customization`, and `origin/master` all point at the same SHA.
- `git show master:backend/app/db/engine.py` contains `pool_pre_ping=True`.
- No merge commit was created (FF only).
</verification>

<success_criteria>
master holds the complete, correct code (practice-area feature + asyncpg fix + deploy
fixes) and is pushed to origin, ready for the dev server to track and deploy.
</success_criteria>

<output>
Create `.planning/phases/14-railway-dev-server-consolidation/14-02-SUMMARY.md` when done.
Record the merged SHA and confirmation that the FF was clean and origin/master matches.
</output>
