---
phase: 14-railway-dev-server-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified: []   # Railway config only — no repo source edits
autonomous: false    # Requires interactive `railway login` (CLI currently Unauthorized) + dashboard/CLI config
requirements: []

must_haves:
  truths:
    - "alea-intake-dev is connected to GitHub and auto-deploys on every push to master"
    - "alea-intake-dev runs with ALEA_DATABASE_BACKEND=postgresql and correct ALEA_DB_* wiring and ALEA_SKIP_MIGRATIONS=true"
    - "A redeploy from the current master tip succeeds and the new build (with the asyncpg fix + practice-area feature) is live"
  artifacts: []
  key_links:
    - from: "GitHub master branch"
      to: "alea-intake-dev Railway service"
      via: "GitHub-connected auto-deploy on push"
      pattern: "railway"
---

<objective>
Make `alea-intake-dev` track `master` and deploy the just-merged code. Connect the
`alea-intake-dev` Railway service to GitHub on branch `master` (so every master push
auto-deploys), confirm/repair its environment variables, and trigger a redeploy from the
current master tip.

Railway project: `alea-tools` (id `357ab7e1-d7cd-465e-b22b-74d6d2c4ea2e`).
Dev service public URL: https://alea-intake-dev-production.up.railway.app

Purpose: The locked decision is that dev tracks master on PostgreSQL. This is the step
that puts the corrected code (asyncpg fix + practice-area feature) onto the live dev box.
Output: dev service GitHub-connected to master, env confirmed, redeployed from master tip.

BLOCKER: The Railway CLI is currently `Unauthorized`. This plan cannot run autonomously —
the user must run `railway login` interactively first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md
@.planning/phases/14-railway-dev-server-consolidation/14-02-SUMMARY.md
# The Phase 13 runbook documents the GraphQL deploy-mutation pattern and the project/env ids.
@.planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md
@railway.toml
@entrypoint.sh
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Authenticate the Railway CLI</name>
  <read_first>
    - .planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md ("Railway CLI is Unauthorized" blocker)
  </read_first>
  <what-built>
    Nothing automated yet — `railway login` requires an interactive browser/device flow
    that Claude cannot perform. The user must authenticate the CLI so the subsequent
    config and deploy steps can run.
  </what-built>
  <how-to-verify>
    User runs, in their terminal:
    1. `railway login` (complete the browser/device auth)
    2. `railway whoami` — confirms an authenticated account
    3. `railway link` (or confirm) the `alea-tools` project (id 357ab7e1-d7cd-465e-b22b-74d6d2c4ea2e)
    Confirm `railway whoami` returns the authenticated user (not `Unauthorized`).
  </how-to-verify>
  <acceptance_criteria>
    - `railway whoami` returns an authenticated user (no `Unauthorized` error).
    - The CLI is linked to / can see the `alea-tools` project.
  </acceptance_criteria>
  <resume-signal>Type "authenticated" once `railway whoami` succeeds, or paste the error if login failed.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Connect alea-intake-dev to GitHub master and confirm env vars</name>
  <read_first>
    - .planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md (deploy-mutation + ids pattern)
    - .planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md (required env vars)
    - railway.toml (healthcheckPath=/health, healthcheckTimeout=300)
  </read_first>
  <decision>
    Wire `alea-intake-dev` to auto-deploy from GitHub `master`, and confirm/repair its
    environment variables, using the now-authenticated Railway CLI (or the Railway
    dashboard if a setting is not CLI-exposable). The exact GitHub-connect mechanism
    (CLI vs dashboard "Connect Repo -> branch master -> enable auto-deploy") is the user's
    call; record which path was used.
  </decision>
  <context>
    On the `alea-intake-dev` service (in project `alea-tools`,
    id 357ab7e1-d7cd-465e-b22b-74d6d2c4ea2e), perform two things:

    (a) GitHub auto-deploy: connect the service to the GitHub repo and set the deploy
        branch to `master` with auto-deploy on push enabled. (The demo service tracked
        `demo/practice-customization` on SQLite; dev must track `master` on Postgres.)

    (b) Confirm/set environment variables on the dev service:
        - `ALEA_DATABASE_BACKEND=postgresql`
        - `ALEA_DB_HOST`, `ALEA_DB_PORT`, `ALEA_DB_NAME`, `ALEA_DB_USER`, `ALEA_DB_PASSWORD`
          wired to the dev's PostgreSQL service (use Railway service-reference variables
          where possible so they track the DB service).
        - `ALEA_SKIP_MIGRATIONS=true` (create_all on startup stays; Alembic wiring is
          out of scope per CONTEXT).
        Verify the dev service actually has a PostgreSQL database attached (pgvector
        preferred to exercise the pgvector path); if it points at a missing/dormant DB,
        attach/repair it.

    Record the chosen connect path (CLI vs dashboard) and the final env-var values
    (mask the password) in the SUMMARY.
  </context>
  <options>
    <option id="cli">
      <name>Railway CLI (railway variables / railway service settings)</name>
      <pros>Scriptable, reproducible, recordable in the runbook.</pros>
      <cons>GitHub repo connection + branch selection may not be fully CLI-exposable; may still need dashboard for the repo link.</cons>
    </option>
    <option id="dashboard">
      <name>Railway dashboard (Service -> Settings -> Connect Repo + Variables)</name>
      <pros>Definitive for the GitHub connect + branch + auto-deploy toggle.</pros>
      <cons>Manual; capture screenshots/notes for the runbook so it is reproducible.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `alea-intake-dev` is connected to the GitHub repo with deploy branch = `master` and auto-deploy enabled (confirmed in dashboard or `railway status`).
    - The dev service env shows `ALEA_DATABASE_BACKEND=postgresql`, all five `ALEA_DB_*` vars set, and `ALEA_SKIP_MIGRATIONS=true`.
    - The dev service has a live PostgreSQL database attached (`pg_isready` or a successful connection from the service).
    - SUMMARY records the connect path and the (password-masked) env values.
  </acceptance_criteria>
  <resume-signal>Select: cli or dashboard — and confirm the GitHub-master connection + env vars are set.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Redeploy dev from the current master tip and confirm build success</name>
  <read_first>
    - .planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md (manual GraphQL serviceInstanceDeployV2 mutation pattern + measured ~2-3 min redeploy timing)
    - railway.toml (healthcheckPath=/health, healthcheckTimeout=300 — the deploy gates on /health)
  </read_first>
  <what-built>
    Trigger a redeploy of `alea-intake-dev` from the current `master` tip (so the asyncpg
    fix + practice-area feature go live) and confirm the build reaches SUCCESS. With
    GitHub auto-deploy now wired, the merge push from 14-02 may already have triggered a
    deploy; if so, confirm that deploy is from the master tip and succeeded. Otherwise
    trigger one — either `railway up` / redeploy via CLI, or the GraphQL
    `serviceInstanceDeployV2` mutation pattern documented in the Phase 13 runbook (use
    the alea-intake-dev service id + its environment id + the master tip commit SHA).
  </what-built>
  <how-to-verify>
    1. Identify the dev service deploy and confirm its commit SHA == current `master` tip
       (`git rev-parse master`).
    2. `railway logs --service alea-intake-dev` (or dashboard) shows the build completing
       and uvicorn starting; healthcheck on `/health` passes (healthcheckTimeout 300s).
    3. Confirm the deploy status is SUCCESS / ACTIVE (not crashed/restart-looping).
    Expected redeploy time ~2-3 min per the Phase 13 measured timings.
  </how-to-verify>
  <acceptance_criteria>
    - The active `alea-intake-dev` deploy's commit SHA equals the current `master` tip.
    - The deploy status is SUCCESS/ACTIVE and is not in a restart loop.
    - Build logs show uvicorn started and the `/health` healthcheck passed.
    - SUMMARY records the deploy SHA, the trigger method (auto vs manual mutation/CLI), and the observed redeploy duration.
  </acceptance_criteria>
  <resume-signal>Type "deployed" once the dev deploy from master tip reaches SUCCESS, or paste failing build logs.</resume-signal>
</task>

</tasks>

<verification>
- `alea-intake-dev` GitHub-connected to `master` with auto-deploy on.
- Env: `ALEA_DATABASE_BACKEND=postgresql`, `ALEA_DB_*` wired, `ALEA_SKIP_MIGRATIONS=true`.
- Active deploy SHA == master tip; build SUCCESS; `/health` healthcheck passed during deploy.
</verification>

<success_criteria>
The dev service tracks master on PostgreSQL and has redeployed the corrected code,
setting up the verification in plan 14-04.
</success_criteria>

<output>
Create `.planning/phases/14-railway-dev-server-consolidation/14-03-SUMMARY.md` when done.
Record: connect path, masked env values, deploy SHA, trigger method, redeploy duration.
</output>
