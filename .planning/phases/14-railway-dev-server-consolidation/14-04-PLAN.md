---
phase: 14-railway-dev-server-consolidation
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified: []   # Verification only — no edits
autonomous: true     # curl + chrome-devtools MCP against the live dev URL

must_haves:
  truths:
    - "GET /health on the dev URL returns status healthy with database.status up (the asyncpg fix proven live on Postgres)"
    - "GET /api/practice-areas on the dev URL returns the personal_injury practice area (not SPA HTML)"
    - "The intake landing page on the dev URL shows the practice-area chip-row and swaps to the PI welcome copy when Personal Injury is selected"
  artifacts: []
  key_links:
    - from: "https://alea-intake-dev-production.up.railway.app/health"
      to: "Postgres SELECT 1"
      via: "live health check"
      pattern: "database.status.*up"
    - from: "https://alea-intake-dev-production.up.railway.app/api/practice-areas"
      to: "practice-area registry"
      via: "public read-only route"
      pattern: "personal_injury"
---

<objective>
Prove `alea-intake-dev` is now the working canonical test server. Against the live dev
URL (https://alea-intake-dev-production.up.railway.app):
1. `/health` returns `healthy` with `database.status: up` (asyncpg fix live on Postgres).
2. `/api/practice-areas` returns the `personal_injury` area (currently returns SPA HTML
   because the route isn't deployed on the old build — so this also confirms the new
   build shipped).
3. Browser smoke test: the intake landing page shows the practice-area chip-row, and
   selecting "Personal Injury" swaps the welcome copy to the PI text.

Purpose: This is the acceptance gate for the whole phase — it confirms the locked goal
("healthy on PostgreSQL ... with the practice-area feature live and verified") is met
before we document and tear down the demo service.
Output: Verified-live evidence (health JSON, practice-areas JSON, browser screenshot).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md
@.planning/phases/14-railway-dev-server-consolidation/14-03-SUMMARY.md
# Frontend contract for /api/practice-areas (id, display_name, welcome_message_*, disclaimer)
@backend/app/routers/practice_areas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify /health and /api/practice-areas on the live dev URL</name>
  <read_first>
    - backend/app/observability/health.py (the shape: status, database.status, version)
    - backend/app/routers/practice_areas.py (the {"practice_areas":[{"id":...}]} contract)
  </read_first>
  <action>
    Curl the live dev endpoints and assert the responses with `jq -e`.
    (a) `curl -fsS https://alea-intake-dev-production.up.railway.app/health` and assert
        `.status == "healthy"` and `.database.status == "up"`. (Degraded is NOT acceptable
        here — the whole point of the phase is database up on Postgres.)
    (b) `curl -fsS https://alea-intake-dev-production.up.railway.app/api/practice-areas`
        and assert the JSON contains a practice area with `.id == "personal_injury"`. If
        the response is SPA HTML rather than JSON, the new build did not ship — stop and
        flag for re-deploy (loop back to 14-03), do not paper over it.
    Capture both raw responses into the SUMMARY.
  </action>
  <verify>
    <automated>curl -fsS https://alea-intake-dev-production.up.railway.app/health | jq -e '.status=="healthy" and .database.status=="up"' && curl -fsS https://alea-intake-dev-production.up.railway.app/api/practice-areas | jq -e '[.practice_areas[].id] | index("personal_injury") != null'</automated>
  </verify>
  <acceptance_criteria>
    - `curl .../health | jq -e '.status=="healthy"'` is true (exit 0).
    - `curl .../health | jq -e '.database.status=="up"'` is true (exit 0).
    - `curl .../api/practice-areas | jq -e '[.practice_areas[].id] | index("personal_injury") != null'` is true (exit 0) — i.e. JSON, not SPA HTML, and PI present.
    - SUMMARY contains the raw `/health` and `/api/practice-areas` responses.
  </acceptance_criteria>
  <done>Live dev /health is healthy with DB up, and /api/practice-areas returns personal_injury as JSON — the new build with the asyncpg fix is confirmed live on Postgres.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser smoke test of the chip-row + PI welcome swap (chrome-devtools MCP)</name>
  <read_first>
    - .planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md (verification step 4: chip-row + PI welcome swap)
    - .planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md (the chip-row UX: Generic + Personal Injury chips, welcome swap on click)
  </read_first>
  <what-built>
    The intake landing page on the live dev URL renders a practice-area chip-row and
    swaps the welcome copy in place when a practice area is selected. Verify it visually
    via the chrome-devtools MCP (per the global browser-testing rule — no headless CLI
    fallback).
  </what-built>
  <how-to-verify>
    Using chrome-devtools MCP (write any screenshot to $HOME, read it, then clean it up):
    1. `navigate_page` to https://alea-intake-dev-production.up.railway.app (reach the
       intake landing / New intake view; register a fresh name@example.com user if the
       landing requires auth, per the Phase 13 runbook).
    2. `wait_for` the chip-row to render; `take_screenshot` and inspect: confirm the
       chip-row is visible with at least `Generic` and `Personal Injury` chips.
    3. Click the `Personal Injury` chip; `wait_for` the welcome copy to update;
       `take_screenshot` and confirm the welcome text changed to PI-specific copy
       (distinct from the generic welcome).
    Report PASS/FAIL with a one-line description of what each screenshot showed.
  </how-to-verify>
  <acceptance_criteria>
    - Screenshot 1 shows the chip-row with `Generic` and `Personal Injury` chips on the live dev URL.
    - Screenshot 2 shows the welcome copy changed to Personal-Injury-specific text after selecting the PI chip.
    - Verification used chrome-devtools MCP (not headless Chromium CLI).
    - SUMMARY records PASS/FAIL and a one-line description per screenshot; temporary screenshot files were cleaned up.
  </acceptance_criteria>
  <resume-signal>Type "verified" if the chip-row and PI welcome swap render correctly on the live dev URL, or describe what was wrong.</resume-signal>
</task>

</tasks>

<verification>
- `/health` on dev URL: `status=healthy`, `database.status=up`.
- `/api/practice-areas` on dev URL: JSON containing `personal_injury`.
- Browser (chrome-devtools MCP): chip-row visible; PI chip swaps welcome copy.
</verification>

<success_criteria>
`alea-intake-dev` is verified as a healthy PostgreSQL test server with the practice-area
feature live — the phase goal is met and the demo service can be retired.
</success_criteria>

<output>
Create `.planning/phases/14-railway-dev-server-consolidation/14-04-SUMMARY.md` when done.
Record the raw endpoint responses and the per-screenshot PASS/FAIL notes.
</output>
