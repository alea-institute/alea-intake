---
phase: 14-railway-dev-server-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/engine.py
  - backend/app/services/analysis/orchestrator.py
autonomous: true
requirements: []   # Operational/infra phase — no product requirement IDs (ROADMAP: "no new product requirements")

must_haves:
  truths:
    - "The Postgres engine validates and recycles connections before reuse (pool_pre_ping + pool_recycle)"
    - "Parallel per-jurisdiction analysis branches no longer share one AsyncSession/asyncpg connection"
    - "A real local PostgreSQL exercises the health check and a multi-jurisdiction analysis path without raising asyncpg 'another operation is in progress'"
  artifacts:
    - path: "backend/app/db/engine.py"
      provides: "Postgres engine with pool_pre_ping=True and pool_recycle"
      contains: "pool_pre_ping=True"
    - path: "backend/app/services/analysis/orchestrator.py"
      provides: "Per-branch session isolation for asyncio.gather jurisdiction branches"
      contains: "_run_parallel_jurisdictions"
  key_links:
    - from: "backend/app/services/analysis/orchestrator.py"
      to: "backend/app/db/engine.py"
      via: "per-branch session created from engine (not shared self._session)"
      pattern: "get_engine|AsyncSession\\("
---

<objective>
Fix the asyncpg "another operation is in progress" bug that takes the dev Postgres
down (it fires even on the health-check SELECT 1). Two-part fix, verified locally
against a real PostgreSQL before any Railway work:

1. Add `pool_pre_ping=True` + `pool_recycle=1800` to the Postgres branch of the
   engine factory so poisoned/dead pooled connections are validated and recycled
   before reuse (addresses the symptom and stops the pool from re-handing-out a
   broken connection).
2. Root-cause fix: `_run_parallel_jurisdictions` runs multiple branches via
   `asyncio.gather`, and every branch uses the SHARED `self._session` (one asyncpg
   connection). asyncpg connections are NOT concurrency-safe, so concurrent stage
   execution on one session poisons the connection. Give each parallel branch its
   OWN session bound to its OWN connection.

Purpose: A poisoned shared connection persists in the pool and breaks unrelated
requests (including /health), so this is the root blocker for making dev healthy on Postgres.
Output: Engine factory hardened; orchestrator parallel branches isolated; both
verified against a real local PostgreSQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md

<interfaces>
<!-- Current engine factory — Postgres branch has pool_size/max_overflow but NO pool_pre_ping/pool_recycle -->
backend/app/db/engine.py:
  create_engine() -> AsyncEngine
  get_engine() -> AsyncEngine   # cached singleton
  dispose_engine() -> None

<!-- Session factories — both create a fresh connection per request via engine.connect() + schema_translate_map -->
backend/app/db/session.py:
  get_tenant_session(request) -> AsyncGenerator[AsyncSession]   # schema_map = {"tenant": tenant_schema}
  get_shared_session() -> AsyncGenerator[AsyncSession]
  pattern: async with engine.connect() as conn:
             conn = await conn.execution_options(schema_translate_map=schema_map)
             async with AsyncSession(bind=conn, expire_on_commit=False) as session: ...

<!-- The bug site — orchestrator constructed with one db_session, branches share it -->
backend/app/services/analysis/orchestrator.py:
  AnalysisOrchestrator.__init__(self, db_session, llm_service, folio=None, embedding_service=None, org_config=None, max_iterations=10, autonomy_interceptor=None)
    self._session = db_session
  _run_parallel_jurisdictions(jurisdictions, run, iteration, ws_manager)
    # async def _jurisdiction_branch(j): await self._execute_stage("fact_map", ...); await self._execute_stage("gap_analyze", ...)
    # await asyncio.gather(*[_jurisdiction_branch(j) for j in jurisdictions])   <-- all branches use self._session
  _execute_stage -> _execute_stage_inner   # uses self._session for all loads, adds, flush; instantiates stages with db_session=self._session
  _get_stage_instance(stage_name) -> Stage(db_session=self._session, ...)

<!-- Stages take db_session in __init__ and use self._session.execute / .add / .flush throughout -->
backend/app/services/analysis/stages/fact_map.py: FactMapStage(llm_service, db_session, folio, embedding_service)
backend/app/services/analysis/stages/gap_analyze.py: GapAnalyzeStage(llm_service, db_session)
</interfaces>

# Real Postgres for local verification lives in docker-compose.multi.yml (pgvector/pgvector:pg17),
# NOT the default docker-compose.yml (which is SQLite). CONTEXT.md said "docker-compose.yml" —
# the actual Postgres service is in docker-compose.multi.yml. Use that one.
@docker-compose.multi.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the Postgres engine with pool_pre_ping and pool_recycle</name>
  <read_first>
    - backend/app/db/engine.py (the factory being modified)
    - backend/app/config.py (confirm DatabaseBackend enum + db_* settings names referenced in create_engine)
  </read_first>
  <behavior>
    - The PostgreSQL branch of create_engine() passes pool_pre_ping=True and pool_recycle=1800 to create_async_engine, in addition to the existing pool_size=20 and max_overflow=10.
    - The SQLite branch is unchanged (no pool_pre_ping/pool_recycle added there).
    - get_engine() / dispose_engine() behavior is unchanged.
  </behavior>
  <action>
    In backend/app/db/engine.py, in the `settings.database_backend == DatabaseBackend.POSTGRESQL`
    branch of create_engine(), add two keyword arguments to the create_async_engine() call:
    `pool_pre_ping=True` and `pool_recycle=1800`. Keep the existing `pool_size=20`,
    `max_overflow=10`, and `echo=settings.debug`. Do not touch the SQLite branch.
    Rationale to record in a brief code comment: pre_ping validates a pooled connection
    with a lightweight check before handing it out (drops dead/poisoned asyncpg connections),
    and recycle (1800s) caps connection lifetime so long-lived poisoned connections rotate out.
  </action>
  <verify>
    <automated>cd backend && grep -n "pool_pre_ping=True" app/db/engine.py && grep -n "pool_recycle=1800" app/db/engine.py && python -c "import ast; ast.parse(open('app/db/engine.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/engine.py` contains `pool_pre_ping=True` inside the PostgreSQL branch.
    - `backend/app/db/engine.py` contains `pool_recycle=1800` inside the PostgreSQL branch.
    - The SQLite branch contains neither `pool_pre_ping` nor `pool_recycle`.
    - `python -c "import ast; ast.parse(open('app/db/engine.py').read())"` exits 0 (file parses).
  </acceptance_criteria>
  <done>Postgres engine validates and recycles pooled connections; SQLite path untouched; file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate per-jurisdiction parallel branches onto their own sessions</name>
  <read_first>
    - backend/app/services/analysis/orchestrator.py (_run_parallel_jurisdictions, _execute_stage, _execute_stage_inner, _get_stage_instance — the shared-session bug)
    - backend/app/db/session.py (the engine.connect() + schema_translate_map + AsyncSession pattern to replicate per branch)
    - backend/app/services/analysis/stages/fact_map.py (confirms each stage takes db_session and drives self._session.execute/add/flush)
  </read_first>
  <action>
    Root-cause fix for asyncpg "another operation is in progress": in
    backend/app/services/analysis/orchestrator.py, change `_run_parallel_jurisdictions`
    so each `asyncio.gather` branch operates on its OWN AsyncSession bound to its OWN
    connection, instead of the shared `self._session`. asyncpg connections are not
    concurrency-safe; concurrent stage execution on one session is what poisons the
    pooled connection.

    Approach: capture the schema_translate_map already in use on `self._session`'s bind
    (so tenant routing is preserved) by reading it from the session's connection
    execution options; for each jurisdiction branch, open a fresh connection from
    `get_engine()` (import get_engine from app.db.engine), apply that same
    schema_translate_map via `conn.execution_options(...)`, create a new
    `AsyncSession(bind=conn, expire_on_commit=False)`, run that branch's fact_map +
    gap_analyze against the new session, then commit (rollback on exception) and close
    the connection — mirror the `async with engine.connect()` / `async with AsyncSession`
    pattern from backend/app/db/session.py. Each branch must construct its stage
    instances bound to its own session (do not reuse the orchestrator's shared stages).

    To make stages run against a per-branch session without rewriting every call site,
    add an optional `session` parameter to the internal stage-execution path
    (`_execute_stage` / `_execute_stage_inner`) that, when provided, is used in place of
    `self._session` for all loads, adds, flushes, AND for `_get_stage_instance(...)`
    construction; when not provided, behavior is identical to today (uses self._session).
    The sequential (single-jurisdiction) path must remain unchanged and keep using
    self._session.

    Do NOT change the public method signatures of run(), resume(), override_convergence(),
    or AnalysisOrchestrator.__init__. Do NOT introduce nested transactions on the shared
    session. Keep all six STAGES names and stage argument shapes identical.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; ast.parse(open('app/services/analysis/orchestrator.py').read())" && grep -n "get_engine" app/services/analysis/orchestrator.py && grep -n "asyncio.gather" app/services/analysis/orchestrator.py && python -m pytest tests/ -k "orchestrator or jurisdiction or parallel" -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `orchestrator.py` imports `get_engine` from `app.db.engine`.
    - `_run_parallel_jurisdictions` no longer passes `self._session` into the gathered branches; each branch creates a fresh `AsyncSession(bind=conn, ...)` from a new `get_engine().connect()`.
    - The schema_translate_map applied to each per-branch connection matches the one on the orchestrator's existing session (tenant routing preserved).
    - The single-jurisdiction (sequential) code path is byte-for-byte equivalent in behavior to before (still uses `self._session`).
    - `python -c "import ast; ast.parse(...)"` exits 0.
    - Existing analysis/orchestrator tests still pass: `python -m pytest tests/ -k "orchestrator or jurisdiction or parallel" -q` exits 0 (if no such tests exist, the run reports 0 selected and exits 0 — record that in the SUMMARY).
  </acceptance_criteria>
  <done>Parallel jurisdiction branches each own a session/connection; sequential path unchanged; public API unchanged; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the fix against a real local PostgreSQL</name>
  <read_first>
    - docker-compose.multi.yml (the pgvector/pgvector:pg17 Postgres service + ALEA_DB_* env wiring)
    - backend/app/observability/health.py (the SELECT 1 health check that currently reports database down)
    - entrypoint.sh (ALEA_SKIP_MIGRATIONS gate + uvicorn start; relevant if you bring the stack up via compose)
  </read_first>
  <action>
    Stand up a real PostgreSQL locally and prove the asyncpg bug is gone. Bring up the
    Postgres service from docker-compose.multi.yml (image pgvector/pgvector:pg17). Run
    the backend against it with `ALEA_DATABASE_BACKEND=postgresql` and the matching
    `ALEA_DB_HOST/PORT/NAME/USER/PASSWORD` (defaults in docker-compose.multi.yml:
    db_name alea_intake, user alea, password changeme, host db/localhost, port 5432) and
    `ALEA_SKIP_MIGRATIONS=true` (create_all on startup stays per CONTEXT out-of-scope note).

    Then exercise two paths against the live Postgres:
    (a) Hit the health check (call `app.observability.health.check_health` or curl
        `GET /health` against the locally running backend) and confirm `database.status`
        is `up` — i.e. SELECT 1 succeeds with no asyncpg error.
    (b) Exercise a concurrent / multi-jurisdiction analysis path so the
        `_run_parallel_jurisdictions` branches run under real asyncpg concurrency, and
        confirm NO "another operation is in progress" error is raised. If a focused
        test for the parallel path exists, run it against Postgres; otherwise write a
        minimal async smoke script that opens the engine and runs two `engine.connect()`
        sessions concurrently via `asyncio.gather` doing `SELECT 1` each, proving the
        engine + per-connection isolation pattern survives concurrency. Save such a
        script under backend/scripts/ if you create one.

    Tear down the compose Postgres when done. Record the exact commands and observed
    output (health JSON + concurrency result) in the plan SUMMARY.

    This is the gate CONTEXT.md requires: "Verify locally against a real Postgres before
    touching Railway." Do NOT proceed to Railway in this plan.
  </action>
  <verify>
    <automated>cd backend && ALEA_DATABASE_BACKEND=postgresql python -c "import asyncio, os; from app.db.engine import get_engine, dispose_engine; from sqlalchemy import text; \nasync def m():\n  e=get_engine()\n  async def q():\n    async with e.connect() as c: await c.execute(text('SELECT 1'))\n  await asyncio.gather(q(), q(), q())\n  await dispose_engine()\nasyncio.run(m()); print('OK no asyncpg concurrency error')" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - A real PostgreSQL (pgvector/pgvector:pg17 from docker-compose.multi.yml) was running during verification (record container id/name + `pg_isready` output in SUMMARY).
    - Health check against the live Postgres returns `database.status: up` (no asyncpg error). Record the `/health` JSON (or check_health() dict) in the SUMMARY.
    - A concurrent path (multi-jurisdiction analysis test OR the concurrent SELECT 1 smoke) runs against Postgres and raises NO `another operation is in progress` error. The verify command prints `OK no asyncpg concurrency error` and exits 0.
    - SUMMARY documents the exact commands used and the observed output.
  </acceptance_criteria>
  <done>Real-Postgres health check is up and concurrent sessions run clean — the asyncpg bug is verified fixed locally before any Railway change.</done>
</task>

</tasks>

<verification>
- `backend/app/db/engine.py` Postgres branch has both `pool_pre_ping=True` and `pool_recycle=1800`.
- `_run_parallel_jurisdictions` branches no longer share `self._session`.
- Against a real local PostgreSQL: `/health` reports `database.status: up`, and concurrent sessions raise no asyncpg "another operation is in progress".
- Existing test suite (or the targeted subset) is green.
</verification>

<success_criteria>
The asyncpg root cause is fixed in code and proven against a real PostgreSQL locally,
clearing the blocker that keeps `alea-intake-dev` unhealthy on Postgres — without yet
touching Railway.
</success_criteria>

<output>
Create `.planning/phases/14-railway-dev-server-consolidation/14-01-SUMMARY.md` when done.
Record: engine diff, orchestrator session-isolation approach, the exact local-Postgres
verification commands, the `/health` JSON observed, and the concurrency smoke result.
</output>
