---
phase: 14-railway-dev-server-consolidation
plan: 05
type: execute
wave: 5
depends_on: ["14-04"]
files_modified:
  - docs/DEV-DEPLOY-RUNBOOK.md
autonomous: false   # Retiring the alea-intake-demo Railway service needs CLI auth / dashboard
requirements: []

must_haves:
  truths:
    - "A dev deploy runbook exists documenting how alea-intake-dev tracks master, its env vars, and how to redeploy/diagnose"
    - "The redundant alea-intake-demo Railway service is retired (deleted or paused) so it no longer auto-deploys or accrues spend"
  artifacts:
    - path: "docs/DEV-DEPLOY-RUNBOOK.md"
      provides: "Canonical dev deploy + ops runbook for alea-intake-dev"
      min_lines: 30
  key_links:
    - from: "docs/DEV-DEPLOY-RUNBOOK.md"
      to: "alea-intake-dev service"
      via: "documented redeploy + smoke-test commands"
      pattern: "alea-intake-dev"
---

<objective>
Make the consolidation durable. Two deliverables:
1. Write a concise dev deploy runbook (docs/DEV-DEPLOY-RUNBOOK.md) mirroring the style of
   the Phase 13 DEMO-RUNBOOK.md: how alea-intake-dev tracks master, its env vars, the
   redeploy + smoke-test commands, and failure-mode diagnostics.
2. Retire the now-redundant alea-intake-demo Railway service (the 2026-05-06 talk is past)
   so it stops auto-deploying demo/practice-customization and stops accruing spend.

Purpose: Closes the phase goal ("retire the redundant alea-intake-demo service") and
leaves a single, documented canonical dev server.
Output: A committed runbook and a retired demo service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-railway-dev-server-consolidation/14-CONTEXT.md
@.planning/phases/14-railway-dev-server-consolidation/14-03-SUMMARY.md
@.planning/phases/14-railway-dev-server-consolidation/14-04-SUMMARY.md
# Style/structure model + the demo service id to retire (alea-intake-demo id 86f796a8-b8a7-49fa-830d-b410bbfa8937)
@.planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md
@railway.toml
@entrypoint.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the dev deploy runbook</name>
  <read_first>
    - .planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md (style/structure to mirror: header block, command cheat-sheet, failure-modes table)
    - .planning/phases/14-railway-dev-server-consolidation/14-03-SUMMARY.md (the actual env wiring + connect path used)
    - .planning/phases/14-railway-dev-server-consolidation/14-04-SUMMARY.md (the verified smoke-test commands + responses)
    - railway.toml (healthcheckPath, timeout, restart policy)
    - entrypoint.sh ($PORT, ALEA_SKIP_MIGRATIONS gate)
  </read_first>
  <action>
    Create docs/DEV-DEPLOY-RUNBOOK.md mirroring the Phase 13 DEMO-RUNBOOK.md structure but
    for the dev server. Include at minimum: a header block naming service alea-intake-dev,
    public URL https://alea-intake-dev-production.up.railway.app, Railway project alea-tools
    (id 357ab7e1-d7cd-465e-b22b-74d6d2c4ea2e), tracked branch master, DB backend PostgreSQL
    (pgvector), and the env vars ALEA_DATABASE_BACKEND=postgresql, the ALEA_DB_* set, and
    ALEA_SKIP_MIGRATIONS=true (pull exact values from the 14-03 SUMMARY, password masked).
    Add a "How deploys happen" section (push to master -> GitHub auto-deploy -> Railway
    Dockerfile build -> /health healthcheck; note the ~2-3 min redeploy timing from the
    Phase 13 measurements). Add a command cheat-sheet: railway logs --service
    alea-intake-dev, the manual serviceInstanceDeployV2 GraphQL redeploy mutation adapted
    from the Phase 13 runbook (using the alea-intake-dev service id + its environment id),
    and the smoke tests (curl .../health piped to jq, curl .../api/practice-areas piped to
    jq for .practice_areas[].id). Add a failure-modes table covering build fails,
    healthcheck timeout, DB down (reference the asyncpg fix: pool_pre_ping + per-branch
    sessions), and restart loop, plus the SQLite-on-volume FALLBACK that CONTEXT documents
    as a net (not the plan). Keep it concise (a working ops doc). Never paste a real secret:
    write the DB password as a placeholder only.
  </action>
  <verify>
    <automated>test -f docs/DEV-DEPLOY-RUNBOOK.md && [ "$(grep -vc '^[[:space:]]*$' docs/DEV-DEPLOY-RUNBOOK.md)" -ge 30 ] && grep -q "alea-intake-dev" docs/DEV-DEPLOY-RUNBOOK.md && grep -q "master" docs/DEV-DEPLOY-RUNBOOK.md && grep -q "ALEA_DATABASE_BACKEND" docs/DEV-DEPLOY-RUNBOOK.md && grep -q "/api/practice-areas" docs/DEV-DEPLOY-RUNBOOK.md && ! grep -q "changeme" docs/DEV-DEPLOY-RUNBOOK.md && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/DEV-DEPLOY-RUNBOOK.md exists with >= 30 non-blank lines.
    - It names alea-intake-dev, the public URL, the alea-tools project id, tracked branch master, and the PostgreSQL backend.
    - It documents the env vars ALEA_DATABASE_BACKEND, ALEA_DB_*, and ALEA_SKIP_MIGRATIONS.
    - It includes a redeploy command, a railway logs --service alea-intake-dev line, and the /health + /api/practice-areas smoke tests.
    - It contains NO literal DB password (no changeme, no real secret) — password is masked/placeholdered.
  </acceptance_criteria>
  <done>A concise, secret-free dev deploy runbook exists at docs/DEV-DEPLOY-RUNBOOK.md mirroring the Phase 13 runbook style.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Retire the redundant alea-intake-demo Railway service</name>
  <read_first>
    - .planning/phases/13-practice-area-customization-demo/DEMO-RUNBOOK.md (demo service id 86f796a8-b8a7-49fa-830d-b410bbfa8937; its DB service alea-intake-demo-db)
    - .planning/phases/14-railway-dev-server-consolidation/14-04-SUMMARY.md (confirms dev is verified-live BEFORE we tear down demo)
  </read_first>
  <what-built>
    Tear down the now-redundant alea-intake-demo service so it stops auto-deploying
    demo/practice-customization and stops accruing spend. This requires the authenticated
    Railway CLI (from 14-03) or the Railway dashboard — Claude cannot delete a Railway
    service without that interactive auth, so this is a human-action checkpoint.
    GATE: do NOT retire demo until plan 14-04 confirmed alea-intake-dev is verified-live
    (healthy + practice-areas + chip-row). Dev must be proven first.
  </what-built>
  <how-to-verify>
    1. Confirm 14-04 SUMMARY shows dev verified-live (health up + personal_injury + chip-row PASS).
    2. In the Railway dashboard (or CLI) for project alea-tools, delete or pause the
       alea-intake-demo service (id 86f796a8-b8a7-49fa-830d-b410bbfa8937). Decide whether to
       also remove its dormant DB service alea-intake-demo-db and its Volume — note the
       decision in the SUMMARY.
    3. Confirm retirement: curl -fsS https://alea-intake-demo-production.up.railway.app/health
       no longer returns a healthy app (connection refused / 404 / service-not-found is the
       expected post-retirement signal), AND the demo service no longer appears as active in
       railway status / the dashboard.
  </how-to-verify>
  <acceptance_criteria>
    - 14-04 SUMMARY confirms dev is verified-live before this step ran (gate respected).
    - The alea-intake-demo service is deleted or paused in the alea-tools project (confirmed in dashboard or railway status).
    - curl https://alea-intake-demo-production.up.railway.app/health no longer returns a healthy running app.
    - SUMMARY records what was retired (service only, or service + DB + volume) and the post-teardown curl result.
  </acceptance_criteria>
  <resume-signal>Type "retired" once alea-intake-demo is deleted/paused and the URL no longer serves a healthy app, or describe why it was kept.</resume-signal>
</task>

</tasks>

<verification>
- docs/DEV-DEPLOY-RUNBOOK.md exists, >= 30 non-blank lines, names alea-intake-dev / master / Postgres env / smoke tests, contains no secret.
- alea-intake-demo Railway service is retired (deleted/paused); its URL no longer serves a healthy app.
- The demo teardown happened only AFTER 14-04 verified dev is live.
</verification>

<success_criteria>
A single canonical, documented dev server (alea-intake-dev on master + Postgres) remains,
the redundant demo service is retired, and the consolidation is captured in a runbook —
the Phase 14 goal is fully met.
</success_criteria>

<output>
Create `.planning/phases/14-railway-dev-server-consolidation/14-05-SUMMARY.md` when done.
Record the runbook path, what was retired (service / DB / volume), and the post-teardown
curl result.
</output>
