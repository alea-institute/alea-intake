---
phase: 10-autonomy-orchestration-modes
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - frontend/src/features/autonomy/types.ts
  - frontend/src/features/autonomy/api.ts
  - frontend/src/features/autonomy/hooks.ts
  - frontend/src/features/admin/components/AutonomySettings.tsx
  - frontend/src/features/admin/components/AutonomySettings.test.tsx
  - frontend/src/features/admin/components/AdminTabs.tsx
  - frontend/src/features/chat/types.ts
  - frontend/src/features/chat/components/ApprovalCard.tsx
  - frontend/src/features/chat/components/ApprovalCard.test.tsx
  - frontend/src/features/chat/components/ReviewStatus.tsx
  - frontend/src/features/chat/components/ReviewStatus.test.tsx
  - frontend/src/features/chat/hooks/useWebSocket.ts
  - frontend/src/locales/en/admin.json
  - frontend/src/locales/en/chat.json
autonomous: true
requirements:
  - AUTONOMY-04
  - AUTONOMY-05

must_haves:
  truths:
    - "Admin sees Autonomy tab with per-stage checkpoint toggles, timeout config, and mode preview"
    - "Admin can select a preset (chatbot/professional/agent) and customize individual stage toggles"
    - "Admin can configure timeout duration and timeout behavior"
    - "Consumer sees mode-appropriate status when pipeline is waiting for professional review"
    - "Professional sees approval card with Approve/Edit/Reject buttons for pending stages"
  artifacts:
    - path: "frontend/src/features/admin/components/AutonomySettings.tsx"
      provides: "Autonomy admin tab with stage toggles, timeout config, mode preview"
      min_lines: 80
    - path: "frontend/src/features/chat/components/ApprovalCard.tsx"
      provides: "Inline approval card for professional view"
      min_lines: 50
    - path: "frontend/src/features/chat/components/ReviewStatus.tsx"
      provides: "Consumer-facing waiting-for-review indicator"
      min_lines: 30
    - path: "frontend/src/features/autonomy/types.ts"
      provides: "TypeScript types for autonomy config, approval requests"
      exports: ["AutonomyConfig", "StageCheckpoint", "ApprovalRequest", "ApprovalAction"]
    - path: "frontend/src/features/autonomy/hooks.ts"
      provides: "React Query hooks for autonomy config and approval queue"
      exports: ["useAutonomyConfig", "useUpdateAutonomyConfig", "usePendingApprovals"]
  key_links:
    - from: "frontend/src/features/admin/components/AutonomySettings.tsx"
      to: "frontend/src/features/autonomy/hooks.ts"
      via: "useAutonomyConfig and useUpdateAutonomyConfig hooks"
      pattern: "useAutonomyConfig|useUpdateAutonomyConfig"
    - from: "frontend/src/features/chat/components/ApprovalCard.tsx"
      to: "frontend/src/features/autonomy/api.ts"
      via: "approveStage, rejectStage, editStage API calls"
      pattern: "approveStage|rejectStage|editStage"
    - from: "frontend/src/features/admin/components/AdminTabs.tsx"
      to: "frontend/src/features/admin/components/AutonomySettings.tsx"
      via: "New Autonomy tab in admin tabs"
      pattern: "AutonomySettings"
---

<objective>
Build the frontend autonomy experience: Autonomy admin settings tab with per-stage checkpoint toggles, preset selection, timeout configuration, and mode preview; approval card component for professionals; review status indicator for consumers; and WebSocket event handling for autonomy status updates.

Purpose: Admins need to configure their org's autonomy level (AUTONOMY-04, AUTONOMY-05 via D-06), professionals need to approve/reject/edit stage output (D-07), and consumers need to see mode-appropriate status labels when analysis is paused for review (D-11).
Output: Complete frontend autonomy UI wired to the backend API from Plans 01 and 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-autonomy-orchestration-modes/10-CONTEXT.md
@.planning/phases/10-autonomy-orchestration-modes/10-RESEARCH.md
@.planning/phases/10-autonomy-orchestration-modes/10-01-SUMMARY.md
@.planning/phases/10-autonomy-orchestration-modes/10-02-SUMMARY.md

<interfaces>
<!-- Existing frontend contracts the executor needs -->

From frontend/src/features/admin/components/AdminTabs.tsx:
```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
// 7 existing tabs: organization, research, kb, protocols, profiles, users, usage
// Most tabs are stubs with muted-foreground text
// Pattern: <TabsTrigger value="name"> + <TabsContent value="name">
```

From frontend/src/features/chat/types.ts:
```typescript
export type WSEvent =
  | { type: 'message_ack'; client_id: string; id: string; timestamp: string }
  | { type: 'llm_stream'; message_id: string; token: string; done: boolean }
  | { type: 'analysis_progress'; data: AnalysisProgress }
  | { type: 'safety_alert'; tier: SafetyAlert['tier']; payload: SafetyAlert }
  | { type: 'fact_extracted'; count: number }
  | { type: 'error'; code: string; message: string }
// Need to add: approval_pending, approval_resolved, review_status
```

From frontend/src/features/chat/hooks/useWebSocket.ts:
```typescript
// WebSocket hook with reconnect, backoff, jitter
// Handles WSEvent dispatch in onMessage handler
```

Backend API contract (from Plan 02):
```
GET  /api/v1/autonomy/pending                     -> ApprovalRequest[]
POST /api/v1/autonomy/requests/{id}/approve        -> { status: "approved" }
POST /api/v1/autonomy/requests/{id}/reject         -> { status: "rejected" }
POST /api/v1/autonomy/requests/{id}/edit           -> { status: "edited" }
POST /api/v1/autonomy/runs/{id}/switch-mode        -> { status: "switched" }
GET  /api/v1/autonomy/admin/config                 -> AutonomyConfig
PUT  /api/v1/autonomy/admin/config                 -> AutonomyConfig
GET  /api/v1/autonomy/admin/stages                 -> string[]
GET  /api/v1/autonomy/admin/presets                -> Record<string, AutonomyConfig>
```

From frontend/src/shared/api/apiFetch.ts:
```typescript
export async function apiFetch<T>(url: string, options?: RequestInit): Promise<T>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Autonomy types, API layer, hooks, and admin AutonomySettings tab</name>
  <files>
    frontend/src/features/autonomy/types.ts,
    frontend/src/features/autonomy/api.ts,
    frontend/src/features/autonomy/hooks.ts,
    frontend/src/features/admin/components/AutonomySettings.tsx,
    frontend/src/features/admin/components/AutonomySettings.test.tsx,
    frontend/src/features/admin/components/AdminTabs.tsx,
    frontend/src/locales/en/admin.json
  </files>
  <read_first>
    frontend/src/features/admin/components/AdminTabs.tsx (full file),
    frontend/src/features/admin/components/OrgProfileForm.tsx (for form pattern),
    frontend/src/shared/api/apiFetch.ts (for apiFetch usage),
    frontend/src/locales/en/admin.json (for i18n key pattern),
    frontend/src/features/chat/store.ts (for Zustand pattern),
    frontend/src/components/ui/tabs.tsx (for Tabs component API),
    frontend/src/components/ui/switch.tsx or similar toggle component (if exists)
  </read_first>
  <behavior>
    - Test: AutonomySettings renders 6 stage toggle switches (one per stage from /admin/stages)
    - Test: AutonomySettings renders timeout number input with min=60
    - Test: AutonomySettings renders timeout behavior dropdown with 3 options
    - Test: AutonomySettings renders safety behavior radio group (strict vs professional) per D-12
    - Test: Selecting "Chatbot" preset sets all toggles to AUTO
    - Test: Selecting "Professional" preset sets all toggles to CHECKPOINT
    - Test: Selecting "Agent" preset sets only question_gen to CHECKPOINT
    - Test: Save button calls PUT /api/v1/autonomy/admin/config with current config
    - Test: AdminTabs includes "Autonomy" tab trigger and content
  </behavior>
  <action>
    1. Create frontend/src/features/autonomy/types.ts:
       - StageCheckpoint = "auto" | "checkpoint"
       - TimeoutBehavior = "auto_proceed" | "queue_next" | "pause_until"
       - SafetyBehavior = "strict" | "professional"
       - AutonomyConfig interface: stage_checkpoints (Record<string, StageCheckpoint>), timeout_seconds (number), timeout_behavior (TimeoutBehavior), safety_behavior (SafetyBehavior), notify_websocket (boolean), notify_email (boolean), labels (Record<string, string>)
       - ApprovalRequest interface: id, run_id, iteration_id, stage_name, status, safety_triggered, is_rerun, rerun_attempt, guidance_text, stage_output_json, created_at, resolved_at
       - ApprovalAction interface: decision ("approve"|"reject"|"edit"), guidance_text?, edits?, actor_id?
       - AutonomyPresets type: Record<string, AutonomyConfig>

    2. Create frontend/src/features/autonomy/api.ts:
       - fetchAutonomyConfig(): apiFetch<AutonomyConfig>("/api/v1/autonomy/admin/config")
       - updateAutonomyConfig(config: AutonomyConfig): apiFetch<AutonomyConfig>("/api/v1/autonomy/admin/config", { method: "PUT", body: JSON.stringify(config) })
       - fetchStages(): apiFetch<string[]>("/api/v1/autonomy/admin/stages")
       - fetchPresets(): apiFetch<AutonomyPresets>("/api/v1/autonomy/admin/presets")
       - fetchPendingApprovals(): apiFetch<ApprovalRequest[]>("/api/v1/autonomy/pending")
       - approveStage(requestId: number): apiFetch("/api/v1/autonomy/requests/" + requestId + "/approve", { method: "POST" })
       - rejectStage(requestId: number, guidanceText: string): apiFetch("/api/v1/autonomy/requests/" + requestId + "/reject", { method: "POST", body: JSON.stringify({ guidance_text: guidanceText }) })
       - editStage(requestId: number, edits: Record<string, unknown>): apiFetch("/api/v1/autonomy/requests/" + requestId + "/edit", { method: "POST", body: JSON.stringify({ edits }) })
       - switchMode(runId: number, config: AutonomyConfig, reason: string): apiFetch("/api/v1/autonomy/runs/" + runId + "/switch-mode", { method: "POST", body: JSON.stringify({ config, reason }) })

    3. Create frontend/src/features/autonomy/hooks.ts:
       - useAutonomyConfig(): useQuery wrapping fetchAutonomyConfig
       - useUpdateAutonomyConfig(): useMutation wrapping updateAutonomyConfig, invalidates config query on success
       - useStages(): useQuery wrapping fetchStages
       - usePresets(): useQuery wrapping fetchPresets
       - usePendingApprovals(): useQuery wrapping fetchPendingApprovals, refetchInterval: 5000

    4. Create frontend/src/features/admin/components/AutonomySettings.tsx (D-06):
       - Uses useAutonomyConfig, useUpdateAutonomyConfig, useStages, usePresets hooks
       - Preset buttons row: "Chatbot", "Professional", "Agent" -- clicking applies preset via update mutation
       - Per-stage toggle section: renders one Switch per stage from useStages(). Toggle between AUTO/CHECKPOINT. Stage names formatted as human-readable labels (issue_spot -> "Issue Spotting")
       - Timeout config section: number input (min 60, step 60) for timeout_seconds, Select for timeout_behavior (3 options), labels per D-04
       - Safety behavior section: RadioGroup with "Strict" (D-12 Option A) and "Professional" (D-12 Option B) options, descriptions explaining each
       - Notification section: Switch for notify_websocket (always on, disabled), Switch for notify_email (toggle, shows warning if SMTP not configured -- Pitfall 7)
       - Mode preview panel: shows consumer-facing labels for current config state. E.g., "In this configuration, consumers will see: [AI Assistant] on system messages. When analysis pauses, they'll see: [Analysis paused for review]." Updates live as admin changes toggles.
       - Save button: calls useUpdateAutonomyConfig with current state
       - Use i18n keys from admin namespace for all labels

    5. Add Autonomy tab to AdminTabs.tsx:
       - Add TabsTrigger value="autonomy" with label t('tabs.autonomy', 'Autonomy')
       - Add TabsContent value="autonomy" rendering <AutonomySettings />
       - Import AutonomySettings (lazy if pattern supports it, otherwise direct)

    6. Add i18n keys to frontend/src/locales/en/admin.json:
       - tabs.autonomy: "Autonomy"
       - autonomy.presets.title: "Mode Presets"
       - autonomy.presets.chatbot: "Chatbot (Fully Autonomous)"
       - autonomy.presets.professional: "Professional (Full Oversight)"
       - autonomy.presets.agent: "Agent (Selective Checkpoints)"
       - autonomy.stages.title: "Stage Checkpoints"
       - autonomy.stages.issue_spot: "Issue Spotting"
       - autonomy.stages.explore: "Exploration"
       - autonomy.stages.research: "Legal Research"
       - autonomy.stages.fact_map: "Fact Mapping"
       - autonomy.stages.gap_analyze: "Gap Analysis"
       - autonomy.stages.question_gen: "Question Generation"
       - autonomy.timeout.title: "Timeout Configuration"
       - autonomy.timeout.duration: "Timeout Duration (seconds)"
       - autonomy.timeout.behavior: "On Timeout"
       - autonomy.timeout.auto_proceed: "Auto-proceed with audit note"
       - autonomy.timeout.queue_next: "Queue for next available professional"
       - autonomy.timeout.pause_until: "Pause until approved"
       - autonomy.safety.title: "Safety Behavior"
       - autonomy.safety.strict: "Strict (all critical + elevated mandatory)"
       - autonomy.safety.professional: "Professional (critical mandatory, elevated silenceable)"
       - autonomy.preview.title: "Consumer Experience Preview"
       - autonomy.save: "Save Configuration"

    7. Write tests in frontend/src/features/admin/components/AutonomySettings.test.tsx:
       - Mock API calls with MSW or vi.mock
       - Test preset buttons change toggle states
       - Test save button calls update mutation
       - Test stage toggles render for all stages
       - Test timeout input validation (min 60)
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/frontend" && npx vitest run src/features/admin/components/AutonomySettings.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AutonomySettings tab renders with preset buttons, per-stage toggles, timeout config, safety behavior, and preview panel (D-06)
    - Preset selection correctly applies chatbot/professional/agent configs (AUTONOMY-01/02/03)
    - Save persists to backend via PUT /api/v1/autonomy/admin/config (AUTONOMY-04, AUTONOMY-05)
    - AdminTabs includes the Autonomy tab
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ApprovalCard, ReviewStatus, and WebSocket autonomy event handling</name>
  <files>
    frontend/src/features/chat/types.ts,
    frontend/src/features/chat/components/ApprovalCard.tsx,
    frontend/src/features/chat/components/ApprovalCard.test.tsx,
    frontend/src/features/chat/components/ReviewStatus.tsx,
    frontend/src/features/chat/components/ReviewStatus.test.tsx,
    frontend/src/features/chat/hooks/useWebSocket.ts,
    frontend/src/locales/en/chat.json
  </files>
  <read_first>
    frontend/src/features/chat/types.ts (full file for WSEvent union),
    frontend/src/features/chat/hooks/useWebSocket.ts (full file for event dispatch pattern),
    frontend/src/features/chat/components/AnalysisProgressPanel.tsx (for progress component pattern),
    frontend/src/features/chat/components/ChatMessage.tsx (for message component pattern),
    frontend/src/features/chat/store.ts (for Zustand store pattern),
    frontend/src/locales/en/chat.json (for i18n keys),
    frontend/src/features/chat/ChatPage.tsx (for component composition pattern)
  </read_first>
  <behavior>
    - Test: ApprovalCard renders stage name, Approve/Reject/Edit buttons
    - Test: ApprovalCard Approve button calls approveStage API
    - Test: ApprovalCard Reject button shows guidance text input, then calls rejectStage API
    - Test: ApprovalCard Edit button shows editable output, then calls editStage API
    - Test: ApprovalCard shows safety badge when safety_triggered=true
    - Test: ReviewStatus shows "Legal professional is reviewing" when status="reviewing" (D-11)
    - Test: ReviewStatus shows "Analysis paused for review" when status="paused" (D-11)
    - Test: ReviewStatus is hidden when status="proceeding"
    - Test: WSEvent type includes approval_pending, approval_resolved, review_status variants
  </behavior>
  <action>
    1. Extend frontend/src/features/chat/types.ts WSEvent union:
       Add three new event types to the WSEvent union:
       ```typescript
       | { type: 'approval_pending'; request_id: number; stage: string; safety: boolean }
       | { type: 'approval_resolved'; request_id: number; decision: string }
       | { type: 'review_status'; status: 'reviewing' | 'paused' | 'proceeding'; label: string }
       ```
       Also add ReviewStatusState type: { status: 'reviewing' | 'paused' | 'proceeding' | 'idle'; label: string; requestId?: number; stageName?: string; safetyTriggered?: boolean }

    2. Create frontend/src/features/chat/components/ApprovalCard.tsx:
       - Props: request (ApprovalRequest from autonomy types), onAction (callback after action)
       - Shows card with: stage name (human-readable from i18n), safety badge (if safety_triggered), stage output preview (JSON pretty-print or summary)
       - Three action buttons: "Approve" (primary), "Reject" (destructive), "Edit" (secondary)
       - Reject flow: clicking Reject shows a textarea for guidance_text input + "Submit Rejection" button. Calls rejectStage(request.id, guidanceText).
       - Edit flow: clicking Edit shows the stage_output_json in an editable textarea (JSON). "Save Edits" button calls editStage(request.id, parsedEdits).
       - Approve flow: clicking Approve calls approveStage(request.id) directly.
       - After any action, call onAction() to refetch pending list.
       - Loading states on buttons during API calls.
       - Use i18n keys from chat namespace.

    3. Create frontend/src/features/chat/components/ReviewStatus.tsx:
       - Props: reviewStatus (ReviewStatusState)
       - Renders a banner/indicator when status !== "idle":
         - "reviewing": shows pulsing dot + label text (default "Legal professional is reviewing" per D-11)
         - "paused": shows pause icon + label text (default "Analysis paused for review" per D-11)
         - "proceeding": hidden (or brief "Proceeding..." flash)
       - Label text comes from reviewStatus.label (server-sent, org-configurable per D-11)
       - Positioned within the chat area, above the input, below messages
       - Uses appropriate ARIA role="status" for accessibility

    4. Update frontend/src/features/chat/hooks/useWebSocket.ts:
       - In the onMessage handler where WSEvent is dispatched, add cases for:
         - 'approval_pending': update review status state to { status: 'paused' or 'reviewing', label, requestId, stageName, safetyTriggered }. If user is professional, add to pending approvals list. If user is consumer, show ReviewStatus.
         - 'approval_resolved': update review status to { status: 'proceeding' }. Remove from pending list.
         - 'review_status': update review status directly from server label.
       - The review status state can be stored in the existing chat Zustand store or a new slice. Prefer the existing store to avoid new state management.

    5. Add i18n keys to frontend/src/locales/en/chat.json:
       - approval.title: "Stage Review"
       - approval.approve: "Approve"
       - approval.reject: "Reject"
       - approval.edit: "Edit Output"
       - approval.guidance_placeholder: "Provide guidance for re-running this stage..."
       - approval.submit_rejection: "Submit Rejection"
       - approval.save_edits: "Save Edits"
       - approval.safety_badge: "Safety Alert"
       - approval.stage.issue_spot: "Issue Spotting"
       - approval.stage.explore: "Exploration"
       - approval.stage.research: "Legal Research"
       - approval.stage.fact_map: "Fact Mapping"
       - approval.stage.gap_analyze: "Gap Analysis"
       - approval.stage.question_gen: "Question Generation"
       - review.reviewing: "Legal professional is reviewing"
       - review.paused: "Analysis paused for review"
       - review.proceeding: "Proceeding with analysis"

    6. Write tests:
       - frontend/src/features/chat/components/ApprovalCard.test.tsx: renders buttons, calls API on click, shows guidance input on reject, shows edit textarea on edit, shows safety badge
       - frontend/src/features/chat/components/ReviewStatus.test.tsx: renders correct text per status, hidden when idle, ARIA attributes
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/frontend" && npx vitest run src/features/chat/components/ApprovalCard.test.tsx src/features/chat/components/ReviewStatus.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ApprovalCard renders with Approve/Reject/Edit buttons and calls backend API (D-07, D-08, D-09)
    - ReviewStatus shows mode-appropriate labels when pipeline is paused for review (D-11)
    - WSEvent union extended with approval_pending, approval_resolved, review_status variants
    - WebSocket hook handles new autonomy events and updates chat state
    - All tests pass
  </done>
</task>

</tasks>

<verification>
cd "/home/damienriehl/Coding Projects/alea-intake/frontend" && npx vitest run src/features/admin/components/AutonomySettings.test.tsx src/features/chat/components/ApprovalCard.test.tsx src/features/chat/components/ReviewStatus.test.tsx --reporter=verbose 2>&1 | tail -30
</verification>

<success_criteria>
- Autonomy tab visible in admin settings with per-stage toggles (AUTONOMY-05)
- Preset buttons apply chatbot/professional/agent configs (AUTONOMY-01/02/03)
- Config saved to backend via API (AUTONOMY-04)
- ApprovalCard shows in professional view with Approve/Reject/Edit actions (D-07)
- ReviewStatus shows consumer-facing labels during pipeline pauses (D-11)
- WebSocket events handled for real-time approval status
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-autonomy-orchestration-modes/10-03-SUMMARY.md`
</output>
