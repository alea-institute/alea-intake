---
phase: 11-integration-production-deployment
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - LICENSE
  - Dockerfile
  - docker-compose.yml
  - docker-compose.multi.yml
  - helm/alea-intake/Chart.yaml
  - helm/alea-intake/values.yaml
  - helm/alea-intake/templates/_helpers.tpl
  - helm/alea-intake/templates/deployment.yaml
  - helm/alea-intake/templates/service.yaml
  - helm/alea-intake/templates/ingress.yaml
  - helm/alea-intake/templates/configmap.yaml
  - helm/alea-intake/templates/secret.yaml
  - scripts/install.sh
  - backend/app/skills/__init__.py
  - backend/app/skills/registry.py
  - backend/app/skills/marketplace.py
  - backend/app/main.py
  - backend/app/models/__init__.py
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-03
must_haves:
  truths:
    - "MIT LICENSE file exists at project root with ALEA copyright"
    - "docker-compose.yml provides single-tenant quick start with SQLite"
    - "docker-compose.multi.yml provides multi-tenant setup with PostgreSQL"
    - "Helm chart deploys to Kubernetes with configurable values"
    - "install.sh one-liner sets up self-hosted single-tenant instance"
    - "Skills registry loads bundled skills and supports community marketplace index"
    - "main.py version is 1.0.0 and all new middleware/routers/lifespan steps are wired"
    - "Dockerfile produces a working image with all dependencies"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license file"
      contains: "MIT License"
    - path: "docker-compose.yml"
      provides: "Single-tenant quick start with SQLite backend"
      contains: "ALEA_DEPLOYMENT_MODE"
    - path: "docker-compose.multi.yml"
      provides: "Multi-tenant setup with PostgreSQL"
      contains: "multi_tenant"
    - path: "helm/alea-intake/Chart.yaml"
      provides: "Helm chart metadata"
      contains: "apiVersion: v2"
    - path: "helm/alea-intake/values.yaml"
      provides: "Helm chart defaults"
      contains: "deploymentMode"
    - path: "scripts/install.sh"
      provides: "One-liner self-hosted setup script"
      contains: "docker compose"
    - path: "backend/app/skills/registry.py"
      provides: "Skills registry for bundled + community skills"
      exports: ["SkillsRegistry"]
    - path: "backend/app/main.py"
      provides: "Final application entry point with all Phase 11 wiring"
      contains: "version.*1.0.0"
  key_links:
    - from: "scripts/install.sh"
      to: "docker-compose.yml"
      via: "install.sh downloads and runs docker-compose.yml"
      pattern: "docker compose up"
    - from: "helm/alea-intake/templates/deployment.yaml"
      to: "helm/alea-intake/values.yaml"
      via: "Helm templating references values"
      pattern: "\\.Values\\."
---

<objective>
Create distribution packaging: MIT license, enhanced Docker Compose files (single-tenant and multi-tenant), Helm chart for Kubernetes, install.sh one-liner, skills registry, and final main.py wiring that integrates all Phase 11 components. Version bump to 1.0.0.

Purpose: This is the final capstone -- packaging everything for open-source distribution so organizations can deploy ALEA via Docker, Kubernetes, or a simple install script.
Output: LICENSE, Docker/Helm packaging, install script, skills registry, fully wired application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-integration-production-deployment/11-RESEARCH.md
@.planning/phases/11-integration-production-deployment/11-01-SUMMARY.md
@.planning/phases/11-integration-production-deployment/11-02-SUMMARY.md
@.planning/phases/11-integration-production-deployment/11-03-SUMMARY.md

@Dockerfile
@docker-compose.yml
@backend/app/main.py

<interfaces>
<!-- Key artifacts from Plans 01-03 -->

From Plan 01 (observability + security):
```python
# backend/app/observability/telemetry.py
def setup_telemetry(app: FastAPI) -> None: ...

# backend/app/observability/logging.py
def setup_logging() -> None: ...

# backend/app/middleware/security.py
class SecurityHeadersMiddleware(BaseHTTPMiddleware): ...

# backend/app/middleware/rate_limit.py
def setup_rate_limiting(app: FastAPI) -> None: ...
```

From Plan 02 (CMS connectors):
```python
# backend/app/routers/cms_admin.py
router  # /api/v1/admin/cms

# backend/app/integrations/cms/sync_queue.py
class CMSSyncQueue: ...
```

From Plan 03 (deployment + persistence):
```python
# backend/app/deployment/migration_runner.py
async def run_startup_migrations() -> None: ...

# backend/app/deployment/persistence.py
class PersistenceManager: ...

# backend/app/deployment/mode.py
def get_deployment_mode() -> DeploymentMode: ...
def is_multi_tenant() -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MIT license, Docker packaging, Helm chart, and install script</name>
  <files>
    LICENSE,
    Dockerfile,
    docker-compose.yml,
    docker-compose.multi.yml,
    helm/alea-intake/Chart.yaml,
    helm/alea-intake/values.yaml,
    helm/alea-intake/templates/_helpers.tpl,
    helm/alea-intake/templates/deployment.yaml,
    helm/alea-intake/templates/service.yaml,
    helm/alea-intake/templates/ingress.yaml,
    helm/alea-intake/templates/configmap.yaml,
    helm/alea-intake/templates/secret.yaml,
    scripts/install.sh
  </files>
  <read_first>
    Dockerfile,
    docker-compose.yml,
    docker-compose.dev.yml,
    backend/app/config.py
  </read_first>
  <behavior>
    - Test 1: LICENSE file exists and contains "MIT License" and "ALEA" and current year
    - Test 2: docker-compose.yml sets ALEA_DEPLOYMENT_MODE=single_tenant and ALEA_DATABASE_BACKEND=sqlite
    - Test 3: docker-compose.multi.yml sets ALEA_DEPLOYMENT_MODE=multi_tenant and includes PostgreSQL service
    - Test 4: Dockerfile builds successfully (docker build --target backend)
    - Test 5: Helm Chart.yaml has apiVersion v2, name alea-intake, version 1.0.0
    - Test 6: Helm values.yaml has deploymentMode, persistence, observability, security sections with sane defaults
    - Test 7: Helm deployment.yaml references values for env vars, resource limits, probes
    - Test 8: install.sh is executable and downloads docker-compose.yml then runs docker compose up
    - Test 9: Helm secret.yaml uses secretKeyRef not inline values (Pitfall 6)
  </behavior>
  <action>
    1. Create LICENSE at project root per D-12:
       - MIT License
       - Copyright (c) 2026 ALEA
       - Standard MIT text

    2. Update Dockerfile per D-03:
       - Keep existing multi-stage build (frontend-build + backend)
       - Add labels: org.opencontainers.image.title=alea-intake, version=1.0.0, source, description, licenses=MIT
       - Add HEALTHCHECK using curl to /health (replace httpx python check with simpler curl)
       - Ensure all new Python dependencies are included (OTel, structlog, slowapi, prometheus-client)
       - Add an ENTRYPOINT that runs Alembic migrations before uvicorn start (via a small entrypoint.sh):
         ```bash
         #!/bin/sh
         # Run migrations unless skipped
         if [ "$ALEA_SKIP_MIGRATIONS" != "true" ]; then
           cd /app/backend && uv run alembic upgrade head
         fi
         exec uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
         ```

    3. Update docker-compose.yml for single-tenant quick start per D-03:
       - Remove PostgreSQL service (single-tenant uses SQLite by default)
       - Set environment: ALEA_DEPLOYMENT_MODE=single_tenant, ALEA_DATABASE_BACKEND=sqlite
       - Map ./data volume for SQLite persistence and uploads
       - Add ALEA_SECRET_KEY with instruction to change
       - Expose port 8000
       - Simple: one service, one volume, docker compose up and go

    4. Create docker-compose.multi.yml for multi-tenant per D-03:
       - PostgreSQL service (pgvector/pgvector:pg17) with health check
       - Backend service with ALEA_DEPLOYMENT_MODE=multi_tenant, ALEA_DATABASE_BACKEND=postgresql
       - Optional Redis service for distributed rate limiting
       - Optional OTEL collector service (commented out, operator brings their own)
       - All config via env vars with sensible defaults and comments

    5. Create Helm chart at helm/alea-intake/ per D-03:
       - Chart.yaml: apiVersion v2, name alea-intake, version 1.0.0, appVersion 1.0.0, description "ALEA Intake - AI-powered legal intake system"
       - values.yaml with sections:
         - replicaCount: 1
         - image: {repository: ghcr.io/alea/alea-intake, tag: 1.0.0, pullPolicy: IfNotPresent}
         - deploymentMode: multi_tenant
         - database: {backend: postgresql, host: "", port: 5432, name: alea_intake, existingSecret: ""}
         - observability: {otelEndpoint: "", logLevel: INFO, metricsEnabled: true}
         - security: {rateLimitDefault: "100/minute", hstsMaxAge: 31536000}
         - persistence: {defaultMode: persistent}
         - ingress: {enabled: false, className: "", hosts: [], tls: []}
         - resources: {limits: {cpu: 1000m, memory: 1Gi}, requests: {cpu: 250m, memory: 512Mi}}
         - autoscaling: {enabled: false, minReplicas: 1, maxReplicas: 5}
       - templates/_helpers.tpl: standard Helm name/label helpers
       - templates/deployment.yaml: Pod spec with env from configmap + secret refs, health/readiness probes to /health, resource limits from values
       - templates/service.yaml: ClusterIP service on port 8000
       - templates/ingress.yaml: Optional ingress with TLS
       - templates/configmap.yaml: Non-sensitive env vars from values
       - templates/secret.yaml: Reference to external Kubernetes Secret (Pitfall 6 -- never inline secrets in values.yaml). Template creates a Secret only if existingSecret is empty, with placeholder values and a comment to replace.

    6. Create scripts/install.sh per D-03:
       - Shebang #!/bin/bash with set -euo pipefail
       - Print banner with ALEA logo/name
       - Check prerequisites: docker, docker compose
       - Generate random SECRET_KEY and MASTER_KEY
       - Download docker-compose.yml from GitHub raw URL (or copy if local)
       - Create .env with generated secrets
       - Create data directory
       - Run docker compose up -d
       - Print success message with URL and first-login instructions
       - chmod +x on the script

    7. Verify: Run `docker build --target backend -t alea-test .` to confirm Dockerfile builds (if docker available). If docker not available, verify Dockerfile syntax is valid.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake" && test -f LICENSE && grep -q "MIT License" LICENSE && test -f docker-compose.yml && grep -q "ALEA_DEPLOYMENT_MODE" docker-compose.yml && test -f docker-compose.multi.yml && grep -q "multi_tenant" docker-compose.multi.yml && test -f helm/alea-intake/Chart.yaml && grep -q "apiVersion: v2" helm/alea-intake/Chart.yaml && test -f scripts/install.sh && test -x scripts/install.sh && echo "All distribution artifacts present and valid"</automated>
  </verify>
  <done>
    MIT LICENSE at project root. docker-compose.yml for single-tenant quick start (SQLite). docker-compose.multi.yml for multi-tenant (PostgreSQL). Helm chart with configurable values for Kubernetes deployment. install.sh one-liner for self-hosted setup. Dockerfile enhanced with labels, migration entrypoint, and health check.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skills registry, final main.py wiring, and version 1.0.0</name>
  <files>
    backend/app/skills/__init__.py,
    backend/app/skills/registry.py,
    backend/app/skills/marketplace.py,
    backend/app/main.py,
    backend/app/models/__init__.py
  </files>
  <read_first>
    backend/app/main.py,
    backend/app/models/__init__.py,
    backend/app/config.py,
    backend/app/observability/telemetry.py,
    backend/app/observability/logging.py,
    backend/app/middleware/security.py,
    backend/app/middleware/rate_limit.py,
    backend/app/deployment/migration_runner.py,
    backend/app/deployment/persistence.py,
    backend/app/routers/cms_admin.py,
    backend/app/integrations/cms/sync_queue.py
  </read_first>
  <behavior>
    - Test 1: SkillsRegistry.load_bundled() loads DV screening and general intake templates from bundled directory
    - Test 2: SkillsRegistry.list_skills() returns list of Skill objects with name, description, type, bundled flag
    - Test 3: SkillsRegistry.get_skill(name) returns a specific skill's Markdown content
    - Test 4: MarketplaceIndex.fetch_index() loads community skills index (JSON) from configured URL
    - Test 5: MarketplaceIndex supports offline mode (returns empty index when URL unreachable)
    - Test 6: main.py version is "1.0.0"
    - Test 7: main.py includes SecurityHeadersMiddleware in middleware stack
    - Test 8: main.py includes setup_rate_limiting call
    - Test 9: main.py includes cms_admin_router
    - Test 10: main.py lifespan calls setup_telemetry, setup_logging, run_startup_migrations
  </behavior>
  <action>
    1. Create backend/app/skills/__init__.py (empty).

    2. Create backend/app/skills/registry.py per D-13:
       - Skill dataclass: name (str), description (str), skill_type (str: "screening"|"intake_template"|"workflow"), content (str -- Markdown), bundled (bool), author (str | None)
       - SkillsRegistry class:
         - _skills: dict[str, Skill]
         - load_bundled() -> None: Scan backend/app/skills/bundled/ directory for .md files. Parse frontmatter (YAML between --- delimiters) for name, description, type. Load content. Mark bundled=True.
         - list_skills(skill_type: str | None = None) -> list[Skill]: Filter by type if specified
         - get_skill(name: str) -> Skill | None
         - register_skill(skill: Skill) -> None: Add to registry (for org-private skills)
         - Bundled skills directory: backend/app/skills/bundled/ with at least:
           - dv_screening.md (DV screening protocol -- reference existing screening protocol content)
           - general_intake.md (general intake template)

    3. Create backend/app/skills/marketplace.py per D-13:
       - MarketplaceIndex class:
         - __init__(index_url: str = "https://raw.githubusercontent.com/alea/skills-registry/main/index.json")
         - async fetch_index() -> list[dict]: GET the index URL, parse JSON. Returns list of {name, description, type, author, url}. Graceful on network error (return []).
         - async fetch_skill(url: str) -> str: GET the skill Markdown content from URL.
         - Git-based registry: skills are Markdown files in a GitHub repo, index.json lists them.

    4. Create bundled skills directory and files:
       - backend/app/skills/bundled/dv_screening.md:
         ```
         ---
         name: DV Screening Protocol
         description: Default domestic violence screening protocol for family law matters
         type: screening
         author: ALEA
         ---
         [Reference existing DV protocol content from screening seed data]
         ```
       - backend/app/skills/bundled/general_intake.md:
         ```
         ---
         name: General Intake Template
         description: Standard legal intake conversation template
         type: intake_template
         author: ALEA
         ---
         [General intake conversation flow template]
         ```

    5. Final main.py wiring -- update backend/app/main.py to integrate ALL Phase 11 components:
       - Version: Change version="0.1.0" to version="1.0.0"
       - Imports: Add observability, security, rate_limit, cms_admin, deployment imports
       - Lifespan (add new steps before existing yield):
         - Step 0 (new, before Step 1): setup_logging()
         - Step 0b: run_startup_migrations() (from deployment.migration_runner)
         - After app creation: setup_telemetry(app)
         - After Step 9: If settings.cms_enabled, start CMSSyncQueue worker background task
         - Store PersistenceManager on app.state
         - Load SkillsRegistry and store on app.state
       - Middleware: Add SecurityHeadersMiddleware (after CORS, before Session)
       - Rate limiting: Call setup_rate_limiting(app) after app creation
       - Router: Include cms_admin_router
       - /health endpoint: Replace inline handler with check_health(app) from observability.health
       - Middleware order: CORS -> SecurityHeaders -> RateLimit -> Session -> Audit -> Tenant -> Consent
       - Ensure all new imports are at top of file, not inline

    6. Update backend/app/models/__init__.py to register CMSSyncRecord and CMSConnectorConfig models.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv run python -c "
from app.main import app
assert '1.0.0' in app.version, f'Expected 1.0.0, got {app.version}'
# Verify middleware stack includes SecurityHeaders
middleware_names = [m.cls.__name__ if hasattr(m, 'cls') else str(m) for m in app.user_middleware]
print(f'Middleware: {middleware_names}')
# Verify routers include cms_admin
route_paths = [r.path for r in app.routes if hasattr(r, 'path')]
assert any('cms' in p for p in route_paths), f'CMS routes missing. Routes: {route_paths[:20]}'
print('All wiring verified')
" && uv run pytest tests/ -x -q --timeout=60 -k "not test_e2e"</automated>
  </verify>
  <done>
    Skills registry loads bundled DV screening and general intake skills. Marketplace index supports fetching community skills from Git-based registry. main.py version is 1.0.0. All Phase 11 components wired: observability (OTel, structlog, /metrics, extended /health), security (headers, rate limiting), CMS admin router, deployment (migrations, persistence manager), skills registry. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
```bash
cd "/home/damienriehl/Coding Projects/alea-intake" && test -f LICENSE && grep -q "MIT" LICENSE && cd backend && uv run pytest tests/ -x -q --timeout=60 -k "not test_e2e"
```
</verification>

<success_criteria>
- MIT LICENSE file at project root
- docker-compose.yml for single-tenant (SQLite) quick start
- docker-compose.multi.yml for multi-tenant (PostgreSQL + optional Redis)
- Helm chart at helm/alea-intake/ with configurable values.yaml
- install.sh one-liner for self-hosted setup
- Skills registry loads bundled skills, marketplace index supports community skills
- main.py version is 1.0.0
- All Phase 11 middleware, routers, lifespan steps wired into main.py
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-production-deployment/11-04-SUMMARY.md`
</output>
