---
phase: 11-integration-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/observability/__init__.py
  - backend/app/observability/telemetry.py
  - backend/app/observability/metrics.py
  - backend/app/observability/health.py
  - backend/app/observability/logging.py
  - backend/app/middleware/security.py
  - backend/app/middleware/rate_limit.py
  - backend/app/main.py
  - backend/tests/test_monitoring.py
  - backend/tests/test_security_headers.py
  - backend/tests/test_rate_limiting.py
autonomous: true
requirements:
  - DEPLOY-06
must_haves:
  truths:
    - "GET /health returns component-level status for DB, FOLIO OWL, folio-mcp, and LLM provider"
    - "GET /metrics returns Prometheus text format with request latency and count metrics"
    - "All responses include security headers (CSP, HSTS, X-Content-Type-Options, X-Frame-Options)"
    - "Rate limiting returns 429 when threshold exceeded"
    - "Structured JSON logs include OTel trace_id and span_id when collector configured"
    - "OTel is a no-op when ALEA_OTEL_ENDPOINT is empty"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Extended Settings with deployment_mode, persistence_mode, otel, security, rate_limit, CMS fields"
      contains: "deployment_mode"
    - path: "backend/app/observability/telemetry.py"
      provides: "OTel TracerProvider + MeterProvider setup"
      exports: ["setup_telemetry"]
    - path: "backend/app/observability/metrics.py"
      provides: "Custom Prometheus metrics (intake counts, LLM costs, screening triggers)"
      exports: ["INTAKE_COUNTER", "LLM_COST_HISTOGRAM"]
    - path: "backend/app/observability/health.py"
      provides: "Extended /health logic with component checks"
      exports: ["check_health"]
    - path: "backend/app/observability/logging.py"
      provides: "structlog configuration with OTel correlation processor"
      exports: ["setup_logging"]
    - path: "backend/app/middleware/security.py"
      provides: "SecurityHeadersMiddleware"
      exports: ["SecurityHeadersMiddleware"]
    - path: "backend/app/middleware/rate_limit.py"
      provides: "Rate limiting setup using slowapi"
      exports: ["setup_rate_limiting"]
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/observability/telemetry.py"
      via: "setup_telemetry(app) in lifespan"
      pattern: "setup_telemetry"
    - from: "backend/app/main.py"
      to: "backend/app/middleware/security.py"
      via: "app.add_middleware(SecurityHeadersMiddleware)"
      pattern: "SecurityHeadersMiddleware"
    - from: "backend/app/observability/logging.py"
      to: "backend/app/observability/telemetry.py"
      via: "OTel trace context injection into structlog processors"
      pattern: "add_otel_context"
---

<objective>
Add production observability (OpenTelemetry tracing, Prometheus metrics, structlog JSON logging), security hardening (CSP/HSTS headers, rate limiting), and extended health checks. Also extend Settings with ALL Phase 11 configuration fields (deployment, persistence, OTel, security, rate limiting, CMS) so downstream plans can reference them.

Purpose: Production-ready monitoring and security are prerequisites for deployment. Settings extensions unblock all subsequent plans.
Output: Observability stack, security middleware, extended /health, and complete Phase 11 Settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-integration-production-deployment/11-RESEARCH.md

@backend/app/config.py
@backend/app/main.py
@backend/app/middleware/tenant.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/config.py:
```python
class DatabaseBackend(str, Enum):
    POSTGRESQL = "postgresql"
    SQLITE = "sqlite"

class Settings(BaseSettings):
    # ... existing fields ...
    cors_origins: list[str] = ["http://localhost:5173"]
    model_config = SettingsConfigDict(env_file=".env", env_prefix="ALEA_")
```

From backend/app/main.py:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # Steps 1-9 already exist
    yield
    # Shutdown

app = FastAPI(title="ALEA Intake API", version="0.1.0", lifespan=lifespan)
# Middleware order: CORS -> Session -> Audit -> Tenant -> Consent -> handler
# Health at @app.get("/health")
```

From backend/app/middleware/tenant.py:
```python
PUBLIC_ROUTES = {"/health", "/docs", "/openapi.json", "/redoc", ...}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Settings and build observability stack</name>
  <files>
    backend/app/config.py,
    backend/app/observability/__init__.py,
    backend/app/observability/telemetry.py,
    backend/app/observability/metrics.py,
    backend/app/observability/health.py,
    backend/app/observability/logging.py,
    backend/tests/test_monitoring.py
  </files>
  <read_first>
    backend/app/config.py,
    backend/app/main.py,
    backend/app/services/folio/owl_cache.py (for get_owl_status import in health)
  </read_first>
  <behavior>
    - Test 1: Settings loads DeploymentMode, PersistenceMode enums with defaults (single_tenant, persistent)
    - Test 2: Settings loads all new fields (otel_endpoint, log_level, rate_limit_default, csp_script_src, hsts_max_age, cms_enabled, etc.) from env with ALEA_ prefix
    - Test 3: setup_telemetry with empty otel_endpoint is a no-op (no exporter added, no crash)
    - Test 4: setup_telemetry with otel_endpoint creates TracerProvider with BatchSpanProcessor + OTLPSpanExporter
    - Test 5: setup_logging configures structlog with JSON renderer and OTel correlation processor
    - Test 6: add_otel_context processor injects trace_id and span_id from active span context
    - Test 7: check_health returns {"status": "healthy", "version": "1.0.0", "database": {"status": "up"}, "folio_owl": ..., "folio_mcp": ..., "llm_provider": ...}
    - Test 8: check_health returns {"status": "degraded"} when DB connection fails
    - Test 9: Custom Prometheus counters/histograms (INTAKE_COUNTER, LLM_COST_HISTOGRAM, SCREENING_TRIGGER_COUNTER) are importable and increment correctly
    - Test 10: /metrics endpoint returns Prometheus text format (via prometheus-fastapi-instrumentator)
    - Test 11: /health endpoint returns extended component status JSON
  </behavior>
  <action>
    1. Install new dependencies: `cd backend && uv add opentelemetry-api opentelemetry-sdk opentelemetry-exporter-otlp-proto-http opentelemetry-instrumentation-fastapi structlog slowapi prometheus-client prometheus-fastapi-instrumentator`

    2. Extend backend/app/config.py per D-02, D-09, D-11:
       - Add DeploymentMode enum (MULTI_TENANT, SINGLE_TENANT) with default SINGLE_TENANT
       - Add PersistenceMode enum (EPHEMERAL, PERSISTENT, CMS_INTEGRATED) with default PERSISTENT
       - Add to Settings class:
         - deployment_mode: DeploymentMode = DeploymentMode.SINGLE_TENANT
         - tenant_signup_mode: str = "admin_approval" (D-10)
         - otel_endpoint: str = "" (empty = disabled per D-09)
         - otel_service_name: str = "alea-intake"
         - log_level: str = "INFO"
         - log_format: str = "json" (json or console)
         - rate_limit_default: str = "100/minute"
         - rate_limit_key_header: str = "" (empty = use client IP; Pitfall 3)
         - rate_limit_storage: str = "memory" (memory or redis://...)
         - csp_script_src: str = "'self'" (D-11)
         - hsts_max_age: int = 31536000 (D-11)
         - max_request_size_mb: int = 50 (D-11)
         - cms_enabled: bool = False
         - cms_sync_interval_seconds: int = 300

    3. Create backend/app/observability/__init__.py (empty).

    4. Create backend/app/observability/telemetry.py per D-09:
       - setup_telemetry(app: FastAPI) function
       - If settings.otel_endpoint is non-empty: create TracerProvider with BatchSpanProcessor + OTLPSpanExporter, set as global. Call FastAPIInstrumentor.instrument_app(app).
       - If settings.otel_endpoint is empty: do nothing (no-op). OTel is opt-in.
       - Set up prometheus-fastapi-instrumentator: Instrumentator().instrument(app).expose(app, endpoint="/metrics")
       - Add "/metrics" to PUBLIC_ROUTES in tenant middleware so it bypasses tenant resolution.

    5. Create backend/app/observability/metrics.py per D-09:
       - INTAKE_COUNTER = Counter("alea_intakes_total", "Total intakes started", ["org_slug", "mode"])
       - LLM_COST_HISTOGRAM = Histogram("alea_llm_cost_dollars", "LLM API cost per call", ["provider", "model"])
       - SCREENING_TRIGGER_COUNTER = Counter("alea_screening_triggers_total", "Screening protocol triggers", ["protocol_name", "trigger_type"])
       - ANALYSIS_STAGE_DURATION = Histogram("alea_analysis_stage_seconds", "Analysis stage duration", ["stage_name"])

    6. Create backend/app/observability/health.py per D-09:
       - async def check_health(app: FastAPI) -> dict
       - Checks: DB connectivity (SELECT 1), FOLIO OWL status (get_owl_status), folio-mcp connectivity (app.state.folio_mcp_client), LLM provider status
       - Returns {"status": "healthy"|"degraded", "version": "1.0.0", "database": {...}, "folio_owl": {...}, "folio_mcp": {...}, "llm_provider": {...}}
       - Any component failure sets status to "degraded" (not "unhealthy" -- partial functionality still available)

    7. Create backend/app/observability/logging.py per D-09:
       - setup_logging() configures structlog with processors: merge_contextvars, add_otel_context, add_log_level, TimeStamper(fmt="iso"), JSONRenderer (or ConsoleRenderer if log_format=="console")
       - add_otel_context processor: reads trace.get_current_span().get_span_context(), injects trace_id (032x) and span_id (016x) if valid
       - Uses settings.log_level for filtering bound logger

    8. Write tests/test_monitoring.py covering behaviors 1-11:
       - Use monkeypatch for Settings overrides
       - Mock DB engine for health check tests
       - Verify /metrics returns text/plain with prometheus content-type
       - Verify /health returns extended JSON

    9. Wire into main.py:
       - In lifespan, before yield: call setup_telemetry(app) and setup_logging()
       - Replace inline /health handler with: `from app.observability.health import check_health; return await check_health(app)`
       - Update version to "1.0.0" in FastAPI constructor
       - Add "/metrics" to PUBLIC_ROUTES in tenant.py
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv run pytest tests/test_monitoring.py -x -q --timeout=30</automated>
  </verify>
  <done>
    Settings includes all Phase 11 fields. OTel setup is no-op when endpoint empty. /health returns component-level status. /metrics returns Prometheus format. structlog configured with OTel correlation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Security headers middleware and rate limiting</name>
  <files>
    backend/app/middleware/security.py,
    backend/app/middleware/rate_limit.py,
    backend/app/middleware/tenant.py,
    backend/app/main.py,
    backend/tests/test_security_headers.py,
    backend/tests/test_rate_limiting.py
  </files>
  <read_first>
    backend/app/main.py,
    backend/app/middleware/tenant.py,
    backend/app/config.py
  </read_first>
  <behavior>
    - Test 1: Response includes Content-Security-Policy header with script-src from settings
    - Test 2: Response includes Strict-Transport-Security with max-age from settings
    - Test 3: Response includes X-Content-Type-Options: nosniff
    - Test 4: Response includes X-Frame-Options: DENY
    - Test 5: Response includes Referrer-Policy: strict-origin-when-cross-origin
    - Test 6: Rate limiter returns 429 after threshold exceeded for same IP
    - Test 7: Rate limiter reads X-Forwarded-For when rate_limit_key_header is set (Pitfall 3)
    - Test 8: Rate limiter allows requests under threshold
    - Test 9: /health, /docs, /metrics bypass rate limiting
  </behavior>
  <action>
    1. Create backend/app/middleware/security.py per D-11:
       - SecurityHeadersMiddleware(BaseHTTPMiddleware)
       - dispatch reads settings for configurable values
       - Sets: Content-Security-Policy (script-src {csp_script_src}), Strict-Transport-Security (max-age={hsts_max_age}; includeSubDomains), X-Content-Type-Options: nosniff, X-Frame-Options: DENY, X-XSS-Protection: 0, Referrer-Policy: strict-origin-when-cross-origin

    2. Create backend/app/middleware/rate_limit.py per D-11:
       - setup_rate_limiting(app: FastAPI) function
       - Uses slowapi.Limiter with custom key_func that reads X-Forwarded-For/X-Real-IP when settings.rate_limit_key_header is set, otherwise uses request.client.host (Pitfall 3)
       - Default limit from settings.rate_limit_default ("100/minute")
       - Storage from settings.rate_limit_storage ("memory" or "redis://...")
       - Exempt paths: /health, /docs, /openapi.json, /redoc, /metrics
       - Attach limiter.state to app and add SlowAPIMiddleware + SlowAPIASGIMiddleware

    3. Update backend/app/middleware/tenant.py:
       - Add "/metrics" to PUBLIC_ROUTES set

    4. Wire into backend/app/main.py:
       - Add SecurityHeadersMiddleware to middleware stack (outermost, after CORS)
       - Call setup_rate_limiting(app) after app creation
       - Middleware order becomes: CORS -> SecurityHeaders -> RateLimit -> Session -> Audit -> Tenant -> Consent

    5. Write tests/test_security_headers.py:
       - Use httpx.AsyncClient with app for response header assertions
       - Verify all 5 security headers present on normal endpoint

    6. Write tests/test_rate_limiting.py:
       - Verify 429 after exceeding rate limit
       - Verify X-Forwarded-For key function with rate_limit_key_header set
       - Verify exempt paths not rate limited
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv run pytest tests/test_security_headers.py tests/test_rate_limiting.py -x -q --timeout=30</automated>
  </verify>
  <done>
    All API responses include security headers (CSP, HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy). Rate limiting returns 429 above threshold. X-Forwarded-For supported for reverse proxy deployments. Health/docs/metrics exempt from rate limiting.
  </done>
</task>

</tasks>

<verification>
```bash
cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv run pytest tests/test_monitoring.py tests/test_security_headers.py tests/test_rate_limiting.py -x -q --timeout=30
```
</verification>

<success_criteria>
- Extended Settings loads all Phase 11 env vars with ALEA_ prefix
- /health returns component-level checks (DB, FOLIO, MCP, LLM) -- degraded on failure
- /metrics returns Prometheus text format with RED metrics
- OTel is no-op when ALEA_OTEL_ENDPOINT is empty
- structlog JSON output includes trace_id/span_id when OTel active
- All responses carry CSP, HSTS, X-Frame-Options, X-Content-Type-Options headers
- Rate limiting returns 429 above threshold, supports proxy headers
- All new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-production-deployment/11-01-SUMMARY.md`
</output>
