---
phase: 05-pre-research-exploration-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/screening.py
  - backend/app/models/__init__.py
  - backend/app/services/screening/__init__.py
  - backend/app/services/screening/protocol_service.py
  - backend/app/services/screening/seed_protocols.py
  - backend/app/services/screening/trigger_matcher.py
  - backend/app/services/exploration/__init__.py
  - backend/app/services/exploration/schemas.py
  - backend/app/services/analysis/schemas.py
  - backend/app/routers/screening_admin.py
  - backend/app/main.py
  - backend/tests/test_screening_protocols.py
  - backend/tests/test_seed_protocols.py
autonomous: true
requirements:
  - EXPLORE-03
  - EXPLORE-05
  - EXPLORE-06
  - EXPLORE-07
  - EXPLORE-08
  - EXPLORE-09

must_haves:
  truths:
    - "Organizations can create, read, update, and delete screening protocols via admin API"
    - "16 seed protocols load idempotently at startup across three severity tiers (5 Critical, 5 Elevated, 6 Advisory)"
    - "Organizations can activate protocols as mandatory, optional, or disabled"
    - "Organizations can pin to a specific protocol version and running intakes use the pinned version"
    - "Organizations can share protocols to the community pool or keep them private"
    - "Exploration depth (min/max rounds) and question transparency are configurable per org"
  artifacts:
    - path: "backend/app/models/screening.py"
      provides: "ScreeningProtocol, ProtocolVersion, OrgProtocolActivation, ScreeningEvent DB models"
      contains: "class ScreeningProtocol"
    - path: "backend/app/services/screening/seed_protocols.py"
      provides: "16 seed protocol definitions and idempotent DB loader"
      contains: "SEED_PROTOCOLS"
    - path: "backend/app/services/screening/protocol_service.py"
      provides: "Protocol CRUD, activation management, version pinning"
      contains: "class ProtocolService"
    - path: "backend/app/services/screening/trigger_matcher.py"
      provides: "Fast keyword/regex/FOLIO-concept trigger matching"
      contains: "class TriggerMatcher"
    - path: "backend/app/services/exploration/schemas.py"
      provides: "Pydantic schemas for exploration I/O and ExplorationConfig"
      contains: "class ExplorationConfig"
    - path: "backend/app/routers/screening_admin.py"
      provides: "Admin CRUD endpoints for protocols and activations"
      contains: "router = APIRouter"
  key_links:
    - from: "backend/app/services/screening/seed_protocols.py"
      to: "backend/app/models/screening.py"
      via: "ScreeningProtocol + ProtocolVersion creation"
      pattern: "ScreeningProtocol\\("
    - from: "backend/app/routers/screening_admin.py"
      to: "backend/app/services/screening/protocol_service.py"
      via: "ProtocolService method calls"
      pattern: "ProtocolService"
    - from: "backend/app/services/analysis/schemas.py"
      to: "backend/app/services/exploration/schemas.py"
      via: "AnalysisConfig.exploration field referencing ExplorationConfig"
      pattern: "ExplorationConfig"
---

<objective>
Screening protocol data layer, seed library, admin API, and exploration Pydantic schemas.

Purpose: Establishes the DB models, 16 seed protocols, trigger matching engine, protocol CRUD service, admin API, and Pydantic schemas that both the exploration engine (Plan 02) and screening middleware (Plan 03) depend on. Without this foundation, neither exploration nor screening can reference protocols, match triggers, or store screening events.

Output: 4 DB models (ScreeningProtocol, ProtocolVersion, OrgProtocolActivation, ScreeningEvent), ProtocolService with CRUD + activation + version pinning, TriggerMatcher for fast pattern checks, 16 seed protocols, ExplorationConfig extending AnalysisConfig, and admin REST endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pre-research-exploration-safety/05-CONTEXT.md
@.planning/phases/05-pre-research-exploration-safety/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/db/base.py:
```python
class TenantBase(DeclarativeBase):
    """Base class for models that live in per-tenant schemas."""
    metadata = tenant_metadata

class SharedBase(DeclarativeBase):
    """Base class for models that live in the shared schema."""
    metadata = shared_metadata
```

From backend/app/models/organization.py:
```python
class OrganizationConfig(TenantBase):
    __tablename__ = "organization_config"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    org_id: Mapped[int] = mapped_column(Integer, nullable=False, unique=True)
    analysis_config_json: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    # ... other fields
```

From backend/app/services/analysis/schemas.py:
```python
class AnalysisConfig(BaseModel):
    auto_trigger_enabled: bool = True
    auto_trigger_fact_threshold: int = 5
    max_iterations: int = 10
    convergence_threshold: float = 0.75
    convergence_weights: ConvergenceWeights = Field(default_factory=ConvergenceWeights)
    confidence_weights: ConfidenceWeights = Field(default_factory=ConfidenceWeights)
    question_transparency: bool = True
```

From backend/app/routers/folio_admin.py (pattern for admin router):
```python
router = APIRouter(
    prefix="/api/v1/admin/folio",
    tags=["folio-admin"],
    dependencies=[Depends(require_role(Role.ADMIN))],
)
```

From backend/app/models/__init__.py:
```python
# Re-exports all models -- new screening models must be added here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Screening DB models, Pydantic schemas, TriggerMatcher, and seed protocols</name>
  <files>
    backend/app/models/screening.py
    backend/app/models/__init__.py
    backend/app/services/screening/__init__.py
    backend/app/services/screening/seed_protocols.py
    backend/app/services/screening/trigger_matcher.py
    backend/app/services/exploration/__init__.py
    backend/app/services/exploration/schemas.py
    backend/app/services/analysis/schemas.py
    backend/tests/test_screening_protocols.py
    backend/tests/test_seed_protocols.py
  </files>
  <read_first>
    backend/app/db/base.py
    backend/app/models/analysis.py
    backend/app/models/__init__.py
    backend/app/services/analysis/schemas.py
    backend/app/routers/folio_admin.py
    backend/tests/conftest.py
  </read_first>
  <behavior>
    - Test: ScreeningProtocol model has columns id, name, slug (unique), description, severity_tier, owner_org_id (nullable), is_shared, is_seed, created_at
    - Test: ProtocolVersion model has columns id, protocol_id, version (semver string), trigger_conditions_json (JSON), questions_json (JSON), escalation_actions_json (JSON), safety_resources_json (JSON nullable), is_active, created_at
    - Test: OrgProtocolActivation model (TenantBase) has columns id, protocol_id, pinned_version_id, activation_mode (mandatory/optional/disabled), config_json, created_at, updated_at
    - Test: ScreeningEvent model (TenantBase) has columns id, session_id, protocol_id, protocol_version_id, severity_tier, trigger_details_json, action_taken, created_at
    - Test: TriggerMatcher.match_fast returns triggered protocols for keyword match in <50ms
    - Test: TriggerMatcher.match_fast returns triggered protocols for regex pattern match
    - Test: TriggerMatcher.match_fast returns empty list when no triggers match
    - Test: TriggerMatcher pre-compiles regex patterns on initialization
    - Test: SEED_PROTOCOLS contains exactly 16 protocols with correct severity distribution (5 Critical, 5 Elevated, 6 Advisory)
    - Test: seed_protocols_to_db creates all 16 protocols idempotently (second call produces no duplicates)
    - Test: DV/IPV seed protocol has slug "dv-ipv", severity "critical", keywords including "domestic violence", "afraid of partner", safety resources with National DV Hotline
    - Test: Each critical-tier seed protocol includes "Are you safe right now?" as first mandatory question per D-13
    - Test: ExplorationConfig has min_rounds (default 1), max_rounds (default 3), stability_threshold (default 0), exploration_confidence_threshold (default 0.4), question_transparency (default True)
    - Test: AnalysisConfig now has exploration field of type ExplorationConfig
    - Test: ExplorationResult schema has fields: description, folio_iri (optional), source_layer, confidence, is_new_issue, protocol_id (optional)
    - Test: ExplorationRoundResult schema has fields: round_number, results list, new_issues_count, is_stable
  </behavior>
  <action>
    **DB Models** (backend/app/models/screening.py):
    Create four models per D-01, D-02, D-03, D-04:

    1. `ScreeningProtocol(SharedBase)` -- community protocol library. Columns: id (PK auto), name (String 255), slug (String 100, unique), description (Text, nullable), severity_tier (String 20: "critical"/"elevated"/"advisory"), owner_org_id (Integer nullable -- NULL means system seed per D-02), is_shared (Boolean default False -- orgs opt-in to share per D-02), is_seed (Boolean default False -- True for the 16 shipped protocols), created_at (server_default func.now()).

    2. `ProtocolVersion(SharedBase)` -- versioned content per D-04. Columns: id (PK auto), protocol_id (Integer, FK to screening_protocols.id), version (String 20, semver like "1.0.0"), trigger_conditions_json (JSON, not null -- schema: {keywords, keyword_match_mode, folio_concept_iris, area_of_law_iris, regex_patterns, exclude_keywords, min_confidence}), questions_json (JSON, not null -- list of question objects with question_id, text, text_transparent, priority, is_mandatory, follow_up_if_yes, follow_up_if_no, trauma_informed_framing), escalation_actions_json (JSON, not null -- {immediate_resources, mandated_reporting_flag, mandated_reporting_note, flag_for_attorney_review, pause_analysis}), safety_resources_json (JSON nullable -- hotline numbers, shelter info, emergency contacts), is_active (Boolean default True), created_at (server_default func.now()). Add unique constraint on (protocol_id, version).

    3. `OrgProtocolActivation(TenantBase)` -- per-org activation per D-03. Columns: id (PK auto), protocol_id (Integer), pinned_version_id (Integer), activation_mode (String 20: "mandatory"/"optional"/"disabled"), config_json (JSON nullable -- org-specific overrides), created_at (server_default func.now()), updated_at (server_default func.now(), onupdate func.now()). Add unique constraint on (protocol_id,) within tenant schema.

    4. `ScreeningEvent(TenantBase)` -- audit trail for triggered screenings. Columns: id (PK auto), session_id (Integer), protocol_id (Integer), protocol_version_id (Integer), severity_tier (String 20), trigger_details_json (JSON -- what triggered: matched keywords, patterns, etc.), action_taken (String 50 -- "immediate_alert", "queued", "folded_to_exploration"), created_at (server_default func.now()).

    Update `backend/app/models/__init__.py` to import and re-export all four new models.

    **Pydantic Schemas** (backend/app/services/exploration/schemas.py):
    Create exploration I/O schemas following the pattern from backend/app/services/analysis/schemas.py:

    - `ExplorationConfig(BaseModel)`: min_rounds (int, default=1, ge=1, le=10), max_rounds (int, default=3, ge=1, le=10), cheap_llm_provider (str|None), cheap_llm_model (str|None), stability_threshold (int, default=0), exploration_confidence_threshold (float, default=0.4), question_transparency (bool, default=True) per D-06/D-15.
    - `ExplorationResult(BaseModel)`: description (str), folio_iri (str|None), source_layer (Literal["folio_adjacency", "protocol_match", "cheap_llm", "expensive_llm"]), confidence (float, ge=0, le=1), is_new_issue (bool), protocol_id (int|None), claim_name (str|None), rationale (str|None).
    - `ExplorationRoundResult(BaseModel)`: round_number (int), results (list[ExplorationResult]), new_issues_count (int), is_stable (bool).
    - `ExplorationStageResult(BaseModel)`: rounds (list[ExplorationRoundResult]), total_new_issues (int), new_claims (list of dicts with claim_name, folio_iri, confidence, source_layer), triggered_protocols (list of dicts with protocol_id, protocol_name, severity_tier).
    - `ScreeningResult(BaseModel)`: triggered_protocols (list of dicts), has_critical (bool), has_elevated (bool), has_advisory (bool), safety_resources (list of dicts), mandatory_questions (list of dicts), needs_deep_scan (bool).

    **Extend AnalysisConfig** (backend/app/services/analysis/schemas.py):
    Add `exploration: ExplorationConfig = Field(default_factory=ExplorationConfig)` to the existing AnalysisConfig class. Import ExplorationConfig from app.services.exploration.schemas. This covers EXPLORE-05 (configurable depth) and EXPLORE-06 (question transparency).

    **TriggerMatcher** (backend/app/services/screening/trigger_matcher.py):
    Class that pre-compiles regex patterns from protocol trigger_conditions_json on initialization for <50ms matching per D-08:

    - `__init__(self, protocols: list[tuple[OrgProtocolActivation, ProtocolVersion]])`: Pre-compile all regex patterns from each protocol version's trigger_conditions_json. Build keyword sets (lowercased). Store protocol metadata for fast lookup.
    - `match_fast(self, content: str) -> list[TriggeredProtocol]`: Lowercase content once. For each active protocol: (1) check keyword sets via set intersection with content words, (2) check compiled regex patterns against content, (3) check FOLIO concept IRIs if provided (simple set membership -- actual FOLIO matching deferred to deep scan). Return list of TriggeredProtocol(protocol_id, protocol_name, severity_tier, trigger_type, matched_terms).
    - `TriggeredProtocol` dataclass: protocol_id, protocol_name, severity_tier, version_id, trigger_type ("keyword"/"regex"/"folio_concept"), matched_terms (list[str]).

    **Seed Protocols** (backend/app/services/screening/seed_protocols.py):
    Define SEED_PROTOCOLS as a list of 16 dicts, each with: name, slug, severity_tier, description, version ("1.0.0"), trigger_conditions (keywords, regex_patterns, folio_concept_iris, area_of_law_iris as empty list for universal per D-11), questions (list with text, text_transparent per D-14, is_mandatory), escalation_actions (immediate_resources per D-13 with real hotline numbers, mandated_reporting_flag, flag_for_attorney_review), safety_resources.

    The 16 protocols per D-12:
    - **Critical (5):** (1) DV/IPV (slug: "dv-ipv"), (2) Child abuse/neglect ("child-abuse"), (3) Elder/dependent adult abuse ("elder-abuse"), (4) Self-harm/suicidal ideation ("self-harm"), (5) Human trafficking ("human-trafficking")
    - **Elevated (5):** (6) Stalking/harassment ("stalking"), (7) Sexual assault ("sexual-assault"), (8) Substance abuse ("substance-abuse"), (9) Mental health crisis ("mental-health-crisis"), (10) Immigration detention risk ("immigration-detention")
    - **Advisory (6):** (11) Housing instability ("housing-instability"), (12) Employment retaliation ("employment-retaliation"), (13) Financial exploitation ("financial-exploitation"), (14) Firearms access ("firearms-access"), (15) Custody/parental alienation ("custody-alienation"), (16) Medical neglect ("medical-neglect")

    Per D-13, every critical-tier protocol MUST include "Are you safe right now?" as the first mandatory question (is_mandatory=True, priority=1) with text_transparent variant per D-14. Per D-13, all critical protocols include safety planning resources (hotlines, shelters, emergency contacts) and mandated_reporting_flag=True.

    Per D-14, all questions use trauma-informed framing: normalize the inquiry ("Many people in your situation experience..."), offer opt-out ("You don't have to answer this"), explain purpose when transparency enabled (text_transparent field). Never use clinical or legal jargon.

    Function `async def seed_protocols_to_db(session: AsyncSession) -> int`: Insert all 16 protocols + their v1.0.0 versions. Use INSERT ... ON CONFLICT (slug) DO NOTHING for idempotency per Pitfall 6 in RESEARCH.md. Return count of newly inserted protocols.

    **Create `__init__.py` files** for backend/app/services/screening/ and backend/app/services/exploration/ (empty or minimal imports).

    **Tests** (backend/tests/test_screening_protocols.py):
    Test ProtocolService and model creation using the async_session fixture pattern from conftest.py. Create the screening tables in the test engine alongside existing tables.

    **Tests** (backend/tests/test_seed_protocols.py):
    Test SEED_PROTOCOLS list structure (count, severity distribution), seed_protocols_to_db idempotency, DV protocol content validation.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && .venv/bin/python -m pytest tests/test_screening_protocols.py tests/test_seed_protocols.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    Four DB models created in screening.py (SharedBase for protocol library, TenantBase for activations/events). 16 seed protocols defined with correct severity distribution, trauma-informed questions, safety resources, and "Are you safe right now?" universal opener on all critical protocols. TriggerMatcher pre-compiles regex and keyword sets for <50ms matching. ExplorationConfig extends AnalysisConfig with min_rounds/max_rounds/stability_threshold/question_transparency. All Pydantic schemas for exploration I/O defined. Models re-exported from __init__.py. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protocol CRUD service, admin API, and lifespan seed loading</name>
  <files>
    backend/app/services/screening/protocol_service.py
    backend/app/routers/screening_admin.py
    backend/app/main.py
    backend/tests/test_screening_protocols.py
  </files>
  <read_first>
    backend/app/routers/folio_admin.py
    backend/app/routers/organizations.py
    backend/app/services/screening/seed_protocols.py
    backend/app/models/screening.py
    backend/app/main.py
    backend/tests/test_folio_admin.py
  </read_first>
  <behavior>
    - Test: ProtocolService.create_protocol creates a ScreeningProtocol + initial ProtocolVersion, returns protocol with version
    - Test: ProtocolService.create_protocol with owner_org_id sets the org as owner (private by default per D-02)
    - Test: ProtocolService.create_protocol with is_shared=True makes it visible in community pool
    - Test: ProtocolService.list_protocols returns seed + community shared + own org's private protocols
    - Test: ProtocolService.list_protocols does NOT return other org's private protocols (EXPLORE-08)
    - Test: ProtocolService.create_version creates a new ProtocolVersion with incremented semver
    - Test: ProtocolService.activate_protocol creates OrgProtocolActivation with pinned_version_id per D-04
    - Test: ProtocolService.activate_protocol with mode="mandatory" makes protocol always run per D-03
    - Test: ProtocolService.deactivate sets activation_mode to "disabled"
    - Test: ProtocolService.get_active_protocols returns only non-disabled protocols for org with their pinned versions
    - Test: POST /api/v1/admin/screening/protocols creates protocol (admin only)
    - Test: GET /api/v1/admin/screening/protocols lists protocols visible to org
    - Test: POST /api/v1/admin/screening/protocols/{id}/activate activates with mode and pins version
    - Test: Seed protocols load during application lifespan (idempotent)
    - Test: New orgs get critical-tier protocols activated as mandatory by default per D-03
  </behavior>
  <action>
    **ProtocolService** (backend/app/services/screening/protocol_service.py):
    Class providing full protocol lifecycle management:

    - `__init__(self, db_session: AsyncSession)`: Store session.
    - `async def create_protocol(self, name, slug, severity_tier, description, owner_org_id, is_shared, trigger_conditions, questions, escalation_actions, safety_resources, version="1.0.0") -> tuple[ScreeningProtocol, ProtocolVersion]`: Create ScreeningProtocol record + initial ProtocolVersion. Validate severity_tier in ("critical", "elevated", "advisory"). If owner_org_id is None and not is_seed, raise ValueError (only seeds have no owner). Return (protocol, version).
    - `async def list_protocols(self, org_id: int | None = None) -> list[dict]`: Return all seed protocols (is_seed=True) + all community shared (is_shared=True) + org's own private protocols (owner_org_id=org_id). Exclude other orgs' private protocols per EXPLORE-07/EXPLORE-08. Include latest active version for each.
    - `async def get_protocol(self, protocol_id: int) -> dict | None`: Get single protocol with versions.
    - `async def create_version(self, protocol_id, trigger_conditions, questions, escalation_actions, safety_resources, version) -> ProtocolVersion`: Create new version for existing protocol. Validate version is semver and greater than previous.
    - `async def activate_protocol(self, protocol_id, pinned_version_id, activation_mode, config=None) -> OrgProtocolActivation`: Create or update OrgProtocolActivation for current tenant. Per D-04, pin to specific version_id. Per D-03, validate activation_mode in ("mandatory", "optional", "disabled").
    - `async def deactivate(self, protocol_id) -> None`: Set activation_mode="disabled" on OrgProtocolActivation.
    - `async def get_active_protocols(self, include_versions=True) -> list[tuple[OrgProtocolActivation, ProtocolVersion]]`: Return all non-disabled protocol activations with their pinned ProtocolVersion records. This is the primary query used by both TriggerMatcher (Plan 03) and ExplorationEngine (Plan 02).
    - `async def activate_defaults_for_org(self) -> int`: Auto-activate critical-tier seed protocols as mandatory and elevated-tier as optional for a new org per D-03. Returns count of activated protocols.

    **Admin Router** (backend/app/routers/screening_admin.py):
    Follow the folio_admin.py pattern exactly:

    ```python
    router = APIRouter(
        prefix="/api/v1/admin/screening",
        tags=["screening-admin"],
        dependencies=[Depends(require_role(Role.ADMIN))],
    )
    ```

    Endpoints:
    - `POST /protocols` -- create protocol (body: name, slug, severity_tier, description, trigger_conditions, questions, escalation_actions, safety_resources, is_shared)
    - `GET /protocols` -- list visible protocols (seed + shared + own)
    - `GET /protocols/{protocol_id}` -- get protocol with versions
    - `POST /protocols/{protocol_id}/versions` -- create new version
    - `POST /protocols/{protocol_id}/activate` -- activate with {pinned_version_id, activation_mode, config}
    - `POST /protocols/{protocol_id}/deactivate` -- set to disabled
    - `GET /activations` -- list org's active protocol activations

    **Lifespan Integration** (backend/app/main.py):
    In the existing lifespan function, after DB engine initialization and FOLIO loading, add seed protocol loading. Use a shared session (not tenant session) since protocols are SharedBase. Call `seed_protocols_to_db(session)`. This ensures the 16 seed protocols exist before any org tries to activate them.

    Register the screening_admin router on the app alongside the existing folio_admin router.

    **Extend Tests** (backend/tests/test_screening_protocols.py):
    Add tests for ProtocolService CRUD, activation, visibility rules, and admin API endpoints using async_client fixture pattern.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && .venv/bin/python -m pytest tests/test_screening_protocols.py tests/test_seed_protocols.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    ProtocolService provides full CRUD + activation lifecycle. Admin API at /api/v1/admin/screening/ follows established folio_admin pattern with require_role(Role.ADMIN). Community sharing visibility rules enforced: seed protocols visible to all, shared protocols visible to all, private protocols visible only to owning org. Critical-tier protocols auto-activate as mandatory for new orgs per D-03. Seed protocols load idempotently during application lifespan. All tests pass including API endpoint tests.
  </done>
</task>

</tasks>

<verification>
- 4 DB models created and registered in models/__init__.py
- 16 seed protocols with correct severity distribution
- TriggerMatcher matches keywords and regex patterns in <50ms
- ProtocolService CRUD enforces visibility rules (seed + shared + own)
- Admin API requires ADMIN role
- ExplorationConfig extends AnalysisConfig with round config
- Seed protocols load idempotently during app lifespan
- `cd backend && .venv/bin/python -m pytest tests/test_screening_protocols.py tests/test_seed_protocols.py -x -q` passes
</verification>

<success_criteria>
- All 4 DB models (ScreeningProtocol, ProtocolVersion, OrgProtocolActivation, ScreeningEvent) exist and have correct columns
- 16 seed protocols defined with DV/IPV as first critical protocol, all critical protocols have "Are you safe right now?" opener
- ProtocolService.list_protocols enforces visibility: seed + shared visible to all, private visible only to owner org
- OrgProtocolActivation pins to specific version_id (not "latest")
- ExplorationConfig has min_rounds/max_rounds/stability_threshold/question_transparency
- Admin router registered at /api/v1/admin/screening/ with ADMIN role guard
- Seed loading is idempotent (no duplicates on restart)
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-research-exploration-safety/05-01-SUMMARY.md`
</output>
