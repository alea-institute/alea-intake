---
phase: 09-frontend-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/analysis.py
  - backend/app/schemas/visualization.py
  - backend/tests/test_visualization_api.py
  - frontend/src/features/visualization/types.ts
  - frontend/src/features/visualization/api.ts
  - frontend/src/features/visualization/store.ts
  - frontend/src/features/visualization/store.test.ts
  - frontend/src/features/visualization/components/ViewTabs.tsx
  - frontend/src/features/visualization/components/ViewTabs.test.tsx
  - frontend/src/features/visualization/components/FilterBar.tsx
  - frontend/src/features/visualization/components/DetailPanel.tsx
  - frontend/src/features/visualization/components/SourceSpanViewer.tsx
  - frontend/src/features/visualization/palette.ts
  - frontend/src/test/msw/handlers.ts
  - frontend/src/test/fixtures/visualization.ts
autonomous: true
requirements: [FRONTEND-03, FRONTEND-04, FRONTEND-05]

must_haves:
  truths:
    - "API returns facts, claims, elements, mappings, gaps, and messages in a single visualization payload"
    - "Zustand store holds shared filters (jurisdiction, claim, confidence threshold) and per-view state"
    - "Tab bar switches between Graph, Matrix, and Narrative views without resetting state"
    - "Filter changes propagate to all views simultaneously"
    - "Colorblind-safe Okabe-Ito categorical palette is defined and exported"
  artifacts:
    - path: "backend/app/routers/analysis.py"
      provides: "GET /api/v1/analysis/{intake_id}/visualization endpoint"
      contains: "visualization"
    - path: "backend/app/schemas/visualization.py"
      provides: "Pydantic response schemas for visualization data"
      exports: ["VisualizationResponse"]
    - path: "frontend/src/features/visualization/types.ts"
      provides: "TypeScript types for graph nodes, links, matrix cells, annotation ranges"
      exports: ["GraphNode", "GraphLink", "MatrixCell", "AnnotationRange", "VisualizationData"]
    - path: "frontend/src/features/visualization/store.ts"
      provides: "Zustand store with shared filters and per-view state slices"
      exports: ["useVisualizationStore"]
    - path: "frontend/src/features/visualization/api.ts"
      provides: "React Query hook for fetching visualization data"
      exports: ["useVisualizationData"]
    - path: "frontend/src/features/visualization/palette.ts"
      provides: "Okabe-Ito colorblind-safe palette and confidence scale"
      exports: ["CATEGORICAL_PALETTE", "NODE_TYPE_COLORS", "CONFIDENCE_SCALE"]
  key_links:
    - from: "frontend/src/features/visualization/api.ts"
      to: "/api/v1/analysis/{intake_id}/visualization"
      via: "apiFetch in React Query queryFn"
      pattern: "apiFetch.*visualization"
    - from: "frontend/src/features/visualization/store.ts"
      to: "frontend/src/features/visualization/components/FilterBar.tsx"
      via: "useVisualizationStore selectors"
      pattern: "useVisualizationStore"
---

<objective>
Backend visualization API endpoint and frontend shared infrastructure for all three visualization views.

Purpose: The existing analysis results API does NOT return facts, mappings, or source spans needed by visualizations. This plan creates a dedicated visualization endpoint returning the full data payload, plus the shared frontend foundation (types, Zustand store, filter bar, detail panel, view tabs, palette) that all three views consume.

Output: Working API endpoint, typed data layer, shared filter/tab UI, colorblind-safe palette, MSW test fixtures, and detail panel component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-visualization/09-CONTEXT.md
@.planning/phases/09-frontend-visualization/09-RESEARCH.md

@backend/app/routers/analysis.py
@backend/app/models/analysis.py
@backend/app/models/fact.py
@backend/app/models/intake.py
@frontend/src/features/auth/api.ts
@frontend/src/features/chat/store.ts
@frontend/src/features/output/api.ts
@frontend/src/test/msw/handlers.ts
@frontend/src/test/utils.tsx

<interfaces>
<!-- Backend models the visualization endpoint must query -->
From backend/app/models/fact.py:
```python
class ExtractedFact(TenantBase):
    id, intake_id, message_id, party_id, assertion_text, fact_type,
    entity_type, confidence, is_active, superseded_by_id, visibility,
    metadata_json, created_at

class FactSourceSpan(TenantBase):
    id, fact_id, message_id, start_char, end_char,
    timestamp_start_sec, timestamp_end_sec, page_number, paragraph_index
```

From backend/app/models/analysis.py:
```python
class AnalysisRun(TenantBase):
    id, intake_id, status, current_iteration_number, max_iterations,
    convergence_score, created_at

class AnalysisClaim(TenantBase):
    id, run_id, claim_name, claim_type, folio_iri, jurisdiction,
    confidence, rationale, is_potential, iteration_discovered

class ClaimElement(TenantBase):
    id, claim_id, element_name, element_description, is_satisfied,
    satisfaction_confidence, jurisdiction

class FactClaimMapping(TenantBase):
    id, fact_id, claim_id, element_id, confidence,
    llm_confidence, concept_confidence, fact_confidence,
    mapping_rationale, iteration_number

class AnalysisGap(TenantBase):
    id, run_id, gap_type, claim_id, element_id, description,
    priority, status, iteration_found
```

From backend/app/models/intake.py:
```python
class Message(TenantBase):
    id, session_id, party_id, sender_type, modality,
    content_encrypted, normalized_text, metadata_json,
    sequence_number, created_at
```

From frontend/src/features/auth/api.ts:
```typescript
export async function apiFetch(input: RequestInfo, init?: RequestInit): Promise<Response>
```

From frontend/src/features/chat/store.ts (Zustand pattern):
```typescript
export const useWSStore = create<WSState>((set) => ({ ... }))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend visualization API endpoint with Pydantic schemas</name>
  <files>
    backend/app/schemas/visualization.py,
    backend/app/routers/analysis.py,
    backend/tests/test_visualization_api.py
  </files>
  <read_first>
    backend/app/routers/analysis.py,
    backend/app/models/analysis.py,
    backend/app/models/fact.py,
    backend/app/models/intake.py,
    backend/app/db/session.py,
    backend/app/core/permissions.py
  </read_first>
  <behavior>
    - Test 1: GET /api/v1/analysis/{intake_id}/visualization returns 200 with facts, claims, mappings, gaps, messages arrays
    - Test 2: Each fact includes source_spans array with start_char, end_char, message_id, page_number, paragraph_index, timestamp fields
    - Test 3: Each claim includes elements array with id, element_name, element_description, is_satisfied, satisfaction_confidence
    - Test 4: Mappings include fact_id, claim_id, element_id, confidence, mapping_rationale
    - Test 5: Returns 404 when no analysis run exists for intake
    - Test 6: Messages include id, content (decrypted normalized_text or content_encrypted), sender_type
  </behavior>
  <action>
    Create backend/app/schemas/visualization.py with Pydantic response models:
    - VisualizationFact(id, assertion_text, fact_type, confidence, source_spans: list[VisualizationSourceSpan])
    - VisualizationSourceSpan(message_id, start_char, end_char, page_number, paragraph_index, timestamp_start_sec, timestamp_end_sec)
    - VisualizationClaim(id, claim_name, claim_type, jurisdiction, confidence, rationale, elements: list[VisualizationElement])
    - VisualizationElement(id, element_name, element_description, is_satisfied, satisfaction_confidence)
    - VisualizationMapping(id, fact_id, claim_id, element_id, confidence, mapping_rationale)
    - VisualizationGap(id, gap_type, claim_id, element_id, description, priority, status)
    - VisualizationMessage(id, content, sender_type)
    - VisualizationResponse(run_id, status, facts, claims, mappings, gaps, messages)

    Add GET endpoint to backend/app/routers/analysis.py at path "/{intake_id}/visualization". Query:
    1. Latest AnalysisRun for intake_id (404 if none)
    2. ExtractedFact rows where intake_id matches and is_active=True
    3. FactSourceSpan rows for those fact IDs
    4. AnalysisClaim rows for the run
    5. ClaimElement rows for those claim IDs
    6. FactClaimMapping rows for those fact IDs
    7. AnalysisGap rows for the run
    8. Message rows for sessions belonging to this intake (join via IntakeSession.intake_id), with content from normalized_text (try decrypt, fallback to empty string) or content_encrypted (same). Only consumer/professional sender_type messages.

    For message content: since content is LargeBinary (encrypted), read normalized_text bytes, decode as UTF-8. If both are None, use empty string. Note: full decryption requires EncryptionContext per Phase 01-03, but for MVP visualization just decode raw bytes (the field may store plaintext in dev/test). Add a comment noting production should use EncryptionContext.

    Write tests in backend/tests/test_visualization_api.py using the async_client fixture pattern from existing tests. Seed an AnalysisRun, ExtractedFact, FactSourceSpan, AnalysisClaim, ClaimElement, FactClaimMapping, AnalysisGap, and Message with known data, then assert the response structure matches expectations.
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/alea-intake && python -m pytest backend/tests/test_visualization_api.py -x -v</automated>
  </verify>
  <done>
    GET /api/v1/analysis/{intake_id}/visualization returns 200 with complete visualization payload (facts with source_spans, claims with elements, mappings, gaps, messages). Returns 404 when no run exists. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend shared infrastructure -- types, store, API hook, palette, shared components, test fixtures</name>
  <files>
    frontend/src/features/visualization/types.ts,
    frontend/src/features/visualization/api.ts,
    frontend/src/features/visualization/store.ts,
    frontend/src/features/visualization/store.test.ts,
    frontend/src/features/visualization/palette.ts,
    frontend/src/features/visualization/components/ViewTabs.tsx,
    frontend/src/features/visualization/components/ViewTabs.test.tsx,
    frontend/src/features/visualization/components/FilterBar.tsx,
    frontend/src/features/visualization/components/DetailPanel.tsx,
    frontend/src/features/visualization/components/SourceSpanViewer.tsx,
    frontend/src/test/msw/handlers.ts,
    frontend/src/test/fixtures/visualization.ts
  </files>
  <read_first>
    frontend/src/features/auth/api.ts,
    frontend/src/features/chat/store.ts,
    frontend/src/features/chat/types.ts,
    frontend/src/features/output/api.ts,
    frontend/src/test/msw/handlers.ts,
    frontend/src/test/utils.tsx,
    frontend/src/components/ui/sheet.tsx,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/slider.tsx
  </read_first>
  <behavior>
    - Test 1: useVisualizationStore initializes with default filters (confidenceThreshold: 0, activeView: 'graph', empty claim/jurisdiction filters)
    - Test 2: setActiveView changes view and preserves per-view state of other views
    - Test 3: setConfidenceThreshold updates shared filter, available to all views
    - Test 4: setJurisdiction and setClaims update shared filters
    - Test 5: Per-view state (graphState.selectedNodeId, matrixState.sortBy, narrativeState.activeLayers) persists across view switches
    - Test 6: ViewTabs renders three tabs (Graph, Matrix, Narrative) and calls setActiveView on click
  </behavior>
  <action>
    **types.ts** -- per D-01/D-06/D-09, define:
    - VisualizationData matching the API response shape (run_id, status, facts[], claims[], mappings[], gaps[], messages[])
    - GraphNode extends SimulationNodeDatum with id, type ('fact'|'claim'|'element'|'gap'), label, confidence, factId/claimId/elementId/gapId (original DB IDs)
    - GraphLink extends SimulationLinkDatum<GraphNode> with confidence
    - MatrixCell with factId, elementId, claimId, confidence, rationale
    - AnnotationRange with start, end, claimIds[], factIds[], colors[]
    - ViewType = 'graph' | 'matrix' | 'narrative'

    **palette.ts** -- per D-15, Okabe-Ito colorblind-safe palette:
    - CATEGORICAL_PALETTE: ['#E69F00','#56B4E9','#009E73','#D55E00','#0072B2','#CC79A7','#F0E442','#000000']
    - NODE_TYPE_COLORS: fact=#E69F00, claim=#56B4E9, element=#009E73, gap=#D55E00
    - CONFIDENCE_SCALE: 5 levels (strong>=0.8=#009E73, good>=0.6=#56B4E9, partial>=0.4=#E69F00, weak>=0.2=#D55E00, none>=0=#cccccc)
    - getConfidenceLevel(score: number) utility function

    **store.ts** -- per D-12/D-13, Zustand store:
    - Shared filters: activeView, jurisdictionFilter, claimFilter[], confidenceThreshold, showGapsOnly
    - Per-view state: graphState (selectedNodeId, zoom, panX, panY), matrixState (sortBy, selectedCell), narrativeState (activeLayers[], selectedSpanId)
    - Actions: setActiveView, setJurisdiction, setClaims, setConfidenceThreshold, toggleGapsOnly, and per-view setters
    - URL sync: activeView reflected in ?view= param (use window.history.replaceState, not router navigation)

    **api.ts** -- React Query hook:
    - useVisualizationData(intakeId: string) wrapping apiFetch to GET /api/v1/analysis/{intakeId}/visualization
    - Returns UseQueryResult<VisualizationData>
    - queryKey: ['visualization', intakeId]
    - staleTime: 30_000 (30s -- analysis data doesn't change frequently)

    **ViewTabs.tsx** -- per D-12, tab bar:
    - Three tabs: Graph | Matrix | Narrative using shadcn Tabs component
    - Reads activeView from store, calls setActiveView on tab change
    - Renders children (the active view component) in TabsContent
    - ARIA: role="tablist" with proper tab/tabpanel association

    **FilterBar.tsx** -- per D-04/D-13, shared filter controls:
    - Jurisdiction dropdown (populated from claims' jurisdictions, includes "All")
    - Claim multi-select checkboxes
    - Confidence threshold slider (0-100, maps to 0-1)
    - Gap-status highlight toggle
    - Uses store selectors for current values, dispatches store actions
    - ARIA: live region announces filter changes per D-14

    **DetailPanel.tsx** -- per D-03, slide-out panel using shadcn Sheet:
    - Opens from right side (side="right")
    - Receives selectedItem prop (fact | claim | element | gap with full data)
    - Shows: type badge, title/text, confidence score, connected edges list, source spans (via SourceSpanViewer), authorities if claim
    - Panel stays open while exploring (controlled by store's selectedNodeId or similar)

    **SourceSpanViewer.tsx** -- per D-11, "trust but verify" source links:
    - Receives sourceSpans[] and messages[] from visualization data
    - Renders each span as a link that highlights the relevant text excerpt
    - Shows message content with the span range highlighted (substring with <mark>)
    - Displays page/paragraph info if from document, timestamp if from voice

    **Test fixtures** (frontend/src/test/fixtures/visualization.ts):
    - mockVisualizationData: 3 facts, 2 claims, 4 elements, 5 mappings, 2 gaps, 3 messages
    - Realistic data: fact assertions about a landlord-tenant dispute, claims for breach of warranty of habitability and wrongful eviction

    **MSW handler**: Add GET /api/v1/analysis/:id/visualization to handlers.ts returning mockVisualizationData.

    Install dependencies: d3-force, d3-selection, d3-zoom, d3-drag, d3-scale, html-to-image, jspdf and their @types/* packages. Add 'd3-vendor' and 'export-vendor' manual chunks to vite.config.ts.
  </action>
  <verify>
    <automated>cd /home/damienriehl/Coding\ Projects/alea-intake/frontend && npx vitest run src/features/visualization/store.test.ts src/features/visualization/components/ViewTabs.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    Frontend visualization feature directory exists with types.ts (all shared types exported), palette.ts (Okabe-Ito colors), store.ts (Zustand with shared+per-view state), api.ts (React Query hook), ViewTabs.tsx (3-tab switcher), FilterBar.tsx (shared filters), DetailPanel.tsx (shadcn Sheet), SourceSpanViewer.tsx (source linking), MSW handler for visualization endpoint, and test fixtures. D3 + export dependencies installed. Vite manual chunks configured. All tests pass.
  </done>
</task>

</tasks>

<verification>
- Backend: `cd backend && python -m pytest tests/test_visualization_api.py -x -v` passes
- Frontend: `cd frontend && npx vitest run src/features/visualization/ --reporter=verbose` passes
- API response contains all required fields (facts.source_spans, claims.elements, mappings, gaps, messages)
- Zustand store preserves per-view state across tab switches
- D3 vendor chunk created in Vite config
</verification>

<success_criteria>
- GET /api/v1/analysis/{intake_id}/visualization returns complete payload with facts, source_spans, claims, elements, mappings, gaps, messages
- Zustand store manages shared filters and per-view state independently
- ViewTabs switches views without state reset
- FilterBar controls update store state
- DetailPanel renders as slide-out Sheet with source span viewer
- Colorblind-safe palette defined and exported
- MSW mock handler and test fixtures available for downstream plans
- All 7+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-visualization/09-01-SUMMARY.md`
</output>
