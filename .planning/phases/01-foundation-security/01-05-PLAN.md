---
phase: 01-foundation-security
plan: 05
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - backend/app/services/llm_service.py
  - backend/app/routers/organizations.py
  - backend/tests/test_llm_service.py
  - backend/tests/test_tenancy.py
  - backend/tests/test_db_backend.py
  - Dockerfile
  - docker-compose.yml
  - docker-compose.dev.yml
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/index.html
autonomous: true
requirements:
  - SECURITY-09
  - INTEGRATE-04
  - DEPLOY-04

must_haves:
  truths:
    - "LLM service initializes with per-org configuration from alea-llm-client"
    - "LLM data policy enforcement: cloud_optout uses API-tier access, local_only never hits cloud"
    - "No case data is sent to LLM training endpoints"
    - "Docker build succeeds for backend (multi-stage Dockerfile)"
    - "docker-compose up starts backend + postgres and /health returns 200"
    - "Frontend scaffold builds with vite build"
    - "Organization CRUD endpoints allow admin to create/list/update orgs"
    - "Tenant isolation test: tenant A cannot read tenant B data"
  artifacts:
    - path: "backend/app/services/llm_service.py"
      provides: "LLM client wrapper with per-org config and training opt-out"
      contains: "class LLMService"
      exports: ["LLMService", "get_llm_service"]
    - path: "backend/app/routers/organizations.py"
      provides: "Organization CRUD endpoints"
      contains: "APIRouter"
    - path: "Dockerfile"
      provides: "Multi-stage Docker build for backend"
      contains: "FROM python"
    - path: "docker-compose.yml"
      provides: "Production-like compose with backend + postgres"
      contains: "pgvector/pgvector"
    - path: "frontend/package.json"
      provides: "Frontend scaffold with React, Vite, TypeScript, Tailwind"
      contains: "react"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind config with design tokens from UI-SPEC"
      contains: "theme"
    - path: "backend/tests/test_tenancy.py"
      provides: "Cross-tenant isolation tests"
      contains: "test_tenant_isolation"
    - path: "backend/tests/test_llm_service.py"
      provides: "LLM service configuration tests"
      contains: "test_llm_service_init"
  key_links:
    - from: "backend/app/services/llm_service.py"
      to: "alea-llm-client"
      via: "import alea_llm_client"
      pattern: "alea_llm_client"
    - from: "backend/app/services/llm_service.py"
      to: "backend/app/models/organization.py"
      via: "reads OrganizationConfig for LLM settings"
      pattern: "OrganizationConfig"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context"
      pattern: "build:"
---

<objective>
Implement the LLM service wrapper with per-org configuration and training opt-out enforcement, organization CRUD endpoints, cross-tenant isolation integration tests, Docker infrastructure (Dockerfile + compose), and the frontend scaffold (React/Vite/TypeScript/Tailwind with design tokens).

Purpose: Complete the foundational infrastructure -- LLM integration ensures the AI pipeline has a secure client, Docker enables deployment, the frontend scaffold prepares for Phase 8, and tenant isolation tests prove data security.
Output: Working LLM service, org management endpoints, Docker build, frontend scaffold, and green isolation tests.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-UI-SPEC.md
@.planning/phases/01-foundation-security/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From backend/app/config.py:
```python
class LLMDataPolicy(str, Enum):
    CLOUD_WITH_OPTOUT = "cloud_optout"
    CLOUD_WITH_BAA = "cloud_baa"
    LOCAL_ONLY = "local_only"

class Settings(BaseSettings):
    # ...all config fields
```

From backend/app/models/organization.py:
```python
class OrganizationConfig(TenantBase):
    __tablename__ = "organization_config"
    llm_api_key_encrypted: Mapped[bytes | None]  # LargeBinary
    llm_provider: Mapped[str | None]  # "openai", "anthropic"
    llm_model: Mapped[str | None]
    llm_data_policy: Mapped[str]  # "cloud_optout", "cloud_baa", "local_only"
```

From backend/app/models/shared.py:
```python
class Organization(SharedBase):
    __tablename__ = "organizations"
    name, slug, auth_mode, llm_data_policy, consent_mode, deletion_policy, is_active
```

From backend/app/schemas/organization.py:
```python
class OrganizationResponse(BaseModel): id, name, slug, auth_mode, is_active
class OrganizationCreate(BaseModel): name (min 2, max 255), slug (pattern ^[a-z0-9_-]+$)
```

From backend/app/db/session.py:
```python
async def get_shared_session() -> AsyncGenerator[AsyncSession, None]
```

From frontend UI-SPEC:
- Spacing scale: xs=4px, sm=8px, md=16px, lg=24px, xl=32px, 2xl=48px, 3xl=64px
- Frontend scaffold: Vite + React + TypeScript + Tailwind, no components
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLM service, organization endpoints, and tenant isolation tests</name>
  <files>
    backend/app/services/llm_service.py,
    backend/app/routers/organizations.py,
    backend/app/main.py,
    backend/tests/test_llm_service.py,
    backend/tests/test_tenancy.py,
    backend/tests/test_db_backend.py
  </files>
  <read_first>
    backend/app/config.py (LLMDataPolicy enum, Settings)
    backend/app/models/organization.py (OrganizationConfig with llm fields)
    backend/app/models/shared.py (Organization model)
    backend/app/schemas/organization.py (OrganizationCreate, OrganizationResponse)
    backend/app/db/session.py (get_shared_session, get_tenant_session)
    backend/app/db/tenant.py (ensure_tenant_schema_exists)
    backend/app/core/permissions.py (require_role)
    backend/app/services/tenant_service.py (TenantService)
    backend/tests/conftest.py (fixtures)
    .planning/phases/01-foundation-security/01-RESEARCH.md (INTEGRATE-04 details, alea-llm-client v0.3.3, Open Question 2 on training controls)
    .planning/phases/01-foundation-security/01-CONTEXT.md (LLM API keys per org, LLM training opt-out org-configurable)
  </read_first>
  <behavior>
    - Test: LLMService.__init__ with org config containing provider="openai", model="gpt-4" initializes correctly
    - Test: LLMService with llm_data_policy="local_only" raises error if no local endpoint configured
    - Test: LLMService.get_client_config() returns provider-appropriate config dict
    - Test: POST /api/v1/organizations with admin token creates org and tenant schema
    - Test: GET /api/v1/organizations with admin token returns list of orgs
    - Test: Consumer/professional cannot create organizations (403)
    - Test: Tenant A user cannot read Tenant B user data via API
    - Test: Creating a user in Tenant A's schema doesn't appear in Tenant B's queries
    - Test: App engine factory creates SQLite engine when database_backend="sqlite"
    - Test: App engine factory creates PostgreSQL URL when database_backend="postgresql"
  </behavior>
  <action>
    **backend/app/services/llm_service.py:**
    - `class LLMService`:
      - `__init__(self, org_config: OrganizationConfig | None = None, platform_settings: Settings | None = None)`:
        - Determine LLM provider: org_config.llm_provider if set, else platform default
        - Determine data policy: org_config.llm_data_policy if org_config, else platform_settings.llm_data_policy if available, else "cloud_optout"
        - If data_policy == "local_only": verify provider endpoint is local (not OpenAI/Anthropic cloud URLs)
        - Store config for client initialization
      - `def get_client_config(self) -> dict`:
        - Return config dict suitable for `alea_llm_client` initialization:
          ```python
          {
              "provider": self.provider,  # "openai", "anthropic", "google", "vllm"
              "model": self.model,
              "api_key": self.api_key,  # org key or platform key
              # Provider-specific headers for training opt-out
              # OpenAI: Organization header (API tier = no training by default)
              # Anthropic: API tier = no training by default
              # VLLM: local, no training concern
          }
          ```
      - `async def check_connection(self) -> dict`:
        - Try to initialize alea-llm-client with config
        - Return `{"status": "connected", "provider": self.provider, "model": self.model}` on success
        - Return `{"status": "error", "detail": str(e)}` on failure
        - Do NOT send any case data in connection check

    - `def get_llm_service(org_config: OrganizationConfig | None = None) -> LLMService`:
      - Factory function for dependency injection

    **IMPORTANT: Training opt-out enforcement:**
    - Level 1: Use API-tier access (not consumer-tier) for all cloud providers
    - Level 2: Include provider-specific organization headers when available
    - Level 3: Respect org data_policy -- if "local_only", only allow VLLM/local endpoints
    - The service MUST NOT expose methods that send arbitrary data to training-eligible endpoints

    **backend/app/routers/organizations.py:**
    - `router = APIRouter(prefix="/api/v1/organizations", tags=["organizations"])`
    - `POST /`: requires `require_role(Role.ADMIN)`, accepts `OrganizationCreate`, calls `tenant_service.create_tenant()`, returns `OrganizationResponse` with 201
    - `GET /`: requires `require_role(Role.ADMIN)`, calls `tenant_service.list_tenants()`, returns `list[OrganizationResponse]`
    - `GET /{org_id}`: requires `require_role(Role.ADMIN)`, returns single `OrganizationResponse`
    - `PATCH /{org_id}`: requires `require_role(Role.ADMIN)`, accepts partial update, returns `OrganizationResponse`

    **backend/app/main.py updates:**
    - Add `app.include_router(organizations_router)`

    **backend/tests/test_llm_service.py:**
    - `test_llm_service_init_with_org_config`: create LLMService with mock org config, verify provider and model set
    - `test_llm_service_local_only_policy`: set policy="local_only" with cloud provider -> raises ValueError or similar
    - `test_llm_service_cloud_optout_config`: verify get_client_config() includes appropriate headers
    - `test_llm_service_check_connection`: mock the alea-llm-client, verify check_connection returns status dict
    - `test_llm_service_no_case_data_in_check`: verify check_connection does not send PII

    **backend/tests/test_tenancy.py:**
    - `test_tenant_schema_creation`: create org -> verify schema `tenant_{slug}` exists (SQLite: skip schema check)
    - `test_tenant_isolation_users`: create user in tenant_a, query users in tenant_b -> empty results
    - `test_cross_tenant_api_blocked`: authenticate as tenant_a user, try to access tenant_b data -> denied
    - `test_shared_schema_org_accessible`: Organization records visible across tenants (shared schema)

    **backend/tests/test_db_backend.py:**
    - `test_sqlite_engine_creation`: with database_backend="sqlite", engine URL starts with "sqlite+aiosqlite"
    - `test_postgresql_engine_url_format`: with database_backend="postgresql", engine URL starts with "postgresql+asyncpg"
    - `test_unsupported_backend_raises`: with database_backend="mysql" -> ValueError
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv run python -m pytest tests/test_llm_service.py tests/test_tenancy.py tests/test_db_backend.py -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/llm_service.py contains `class LLMService` with `__init__`, `get_client_config`, `check_connection`
    - backend/app/services/llm_service.py contains `alea_llm_client` import or reference
    - backend/app/services/llm_service.py contains check for `local_only` policy blocking cloud providers
    - backend/app/services/llm_service.py contains `def get_llm_service(` factory function
    - backend/app/routers/organizations.py contains `APIRouter(prefix="/api/v1/organizations")`
    - backend/app/routers/organizations.py contains `require_role(Role.ADMIN)` on POST endpoint
    - backend/app/main.py contains `include_router` for organizations router
    - backend/tests/test_llm_service.py contains at least 3 test functions including `test_llm_service_init_with_org_config`
    - backend/tests/test_tenancy.py contains `test_tenant_isolation_users`
    - backend/tests/test_db_backend.py contains `test_sqlite_engine_creation` and `test_postgresql_engine_url_format`
    - `pytest tests/test_llm_service.py tests/test_tenancy.py tests/test_db_backend.py -x` exits 0
  </acceptance_criteria>
  <done>
    LLM service wraps alea-llm-client with per-org configuration and three-level training opt-out enforcement. Organization CRUD endpoints work with admin-only access. Cross-tenant isolation tests prove data doesn't leak. Database backend abstraction tests verify both pg and sqlite engine creation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Docker infrastructure and frontend scaffold</name>
  <files>
    Dockerfile,
    docker-compose.yml,
    docker-compose.dev.yml,
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/tailwind.config.ts,
    frontend/postcss.config.js,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/index.html
  </files>
  <read_first>
    backend/pyproject.toml (Python dependencies for Dockerfile)
    backend/app/main.py (FastAPI app entry point for uvicorn command)
    .planning/phases/01-foundation-security/01-RESEARCH.md (Docker compose example, project structure)
    .planning/phases/01-foundation-security/01-UI-SPEC.md (frontend scaffold contract, spacing scale, Tailwind config)
    .planning/phases/01-foundation-security/01-CONTEXT.md (Docker is Claude's discretion)
    pnpm-workspace.yaml (workspace config)
  </read_first>
  <action>
    **Dockerfile (multi-stage build):**
    ```dockerfile
    # Stage 1: Frontend build
    FROM node:22-slim AS frontend-build
    RUN corepack enable && corepack prepare pnpm@latest --activate
    WORKDIR /app/frontend
    COPY frontend/package.json frontend/pnpm-lock.yaml* ./
    RUN pnpm install --frozen-lockfile || pnpm install
    COPY frontend/ ./
    RUN pnpm build

    # Stage 2: Backend
    FROM python:3.12-slim AS backend
    WORKDIR /app
    RUN pip install uv
    COPY backend/pyproject.toml backend/uv.lock* ./backend/
    WORKDIR /app/backend
    RUN uv sync --no-dev
    COPY backend/ ./
    COPY --from=frontend-build /app/frontend/dist /app/frontend/dist

    EXPOSE 8000

    HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
      CMD python -c "import httpx; r=httpx.get('http://localhost:8000/health'); exit(0 if r.status_code==200 else 1)"

    CMD ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ```

    **docker-compose.yml:**
    ```yaml
    services:
      db:
        image: pgvector/pgvector:pg17
        environment:
          POSTGRES_DB: alea_intake
          POSTGRES_USER: alea
          POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
        volumes:
          - pgdata:/var/lib/postgresql/data
        ports:
          - "5432:5432"
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U alea -d alea_intake"]
          interval: 5s
          timeout: 3s
          retries: 5

      backend:
        build:
          context: .
          dockerfile: Dockerfile
          target: backend
        environment:
          ALEA_DATABASE_BACKEND: postgresql
          ALEA_DB_HOST: db
          ALEA_DB_PORT: "5432"
          ALEA_DB_NAME: alea_intake
          ALEA_DB_USER: alea
          ALEA_DB_PASSWORD: ${DB_PASSWORD:-changeme}
          ALEA_SECRET_KEY: ${SECRET_KEY:-dev-secret-key-change-in-production}
          ALEA_MASTER_KEY_PATH: /app/data/master.key
        volumes:
          - app_data:/app/data
        ports:
          - "8000:8000"
        depends_on:
          db:
            condition: service_healthy

    volumes:
      pgdata:
      app_data:
    ```

    **docker-compose.dev.yml:**
    ```yaml
    services:
      db:
        image: pgvector/pgvector:pg17
        environment:
          POSTGRES_DB: alea_intake
          POSTGRES_USER: alea
          POSTGRES_PASSWORD: devpassword
        ports:
          - "5432:5432"
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U alea -d alea_intake"]
          interval: 5s
          timeout: 3s
          retries: 5
    ```
    (Dev compose is lightweight -- just the DB. Backend and frontend run locally.)

    **frontend/package.json:**
    ```json
    {
      "name": "alea-intake-frontend",
      "private": true,
      "version": "0.1.0",
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "preview": "vite preview"
      },
      "dependencies": {
        "react": "^19.0.0",
        "react-dom": "^19.0.0"
      },
      "devDependencies": {
        "@types/react": "^19.0.0",
        "@types/react-dom": "^19.0.0",
        "@vitejs/plugin-react": "^4.4.0",
        "autoprefixer": "^10.4.0",
        "postcss": "^8.5.0",
        "tailwindcss": "^3.4.0",
        "typescript": "~5.7.0",
        "vite": "^6.0.0"
      }
    }
    ```

    **frontend/vite.config.ts:**
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      plugins: [react()],
      server: {
        port: 5173,
        proxy: {
          '/api': {
            target: 'http://localhost:8000',
            changeOrigin: true,
          },
        },
      },
    })
    ```

    **frontend/tsconfig.json:**
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["ES2022", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "moduleResolution": "bundler",
        "jsx": "react-jsx",
        "strict": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "noFallthroughCasesInSwitch": true,
        "skipLibCheck": true,
        "isolatedModules": true,
        "outDir": "dist",
        "rootDir": "src"
      },
      "include": ["src"]
    }
    ```

    **frontend/tailwind.config.ts:**
    Per UI-SPEC, pre-seed design tokens in theme.extend.spacing:
    ```typescript
    import type { Config } from 'tailwindcss'

    export default {
      content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
      theme: {
        extend: {
          spacing: {
            'xs': '4px',
            'sm-custom': '8px',
            'md-custom': '16px',
            'lg-custom': '24px',
            'xl-custom': '32px',
            '2xl-custom': '48px',
            '3xl-custom': '64px',
          },
        },
      },
      plugins: [],
    } satisfies Config
    ```
    Note: Use suffixed names to avoid colliding with Tailwind's default spacing scale. Phase 8 will refine these.

    **frontend/postcss.config.js:**
    ```javascript
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    }
    ```

    **frontend/index.html:**
    ```html
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>ALEA Intake</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    **frontend/src/main.tsx:**
    ```typescript
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import './index.css'
    import App from './App'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>,
    )
    ```

    **frontend/src/App.tsx:**
    ```typescript
    function App() {
      return (
        <div className="min-h-screen flex items-center justify-center">
          <h1 className="text-2xl font-semibold text-gray-700">
            ALEA Intake — Frontend scaffold ready
          </h1>
        </div>
      )
    }

    export default App
    ```

    **frontend/src/index.css:**
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/frontend" && (command -v pnpm >/dev/null 2>&1 && pnpm install && pnpm build || npm install && npx vite build) && echo "Frontend build OK" && cd "/home/damienriehl/Coding Projects/alea-intake" && test -f Dockerfile && test -f docker-compose.yml && echo "Docker files exist"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile contains `FROM python:3.12-slim` and `FROM node:22-slim`
    - Dockerfile contains `HEALTHCHECK` with `/health` endpoint check
    - Dockerfile contains `CMD` with `uvicorn app.main:app`
    - docker-compose.yml contains `pgvector/pgvector:pg17` as db image
    - docker-compose.yml contains `ALEA_DATABASE_BACKEND: postgresql` in backend environment
    - docker-compose.yml contains `healthcheck` for db service with `pg_isready`
    - docker-compose.yml contains `depends_on` with `condition: service_healthy`
    - docker-compose.dev.yml exists with db service only
    - frontend/package.json contains `react` and `vite` and `tailwindcss` as dependencies
    - frontend/vite.config.ts contains proxy config for `/api` to `http://localhost:8000`
    - frontend/tailwind.config.ts contains spacing tokens (`xs`, `sm-custom`, etc.)
    - frontend/src/App.tsx contains `ALEA Intake`
    - frontend/src/index.css contains `@tailwind base`
    - `vite build` (or equivalent) exits 0 in frontend/
  </acceptance_criteria>
  <done>
    Docker infrastructure is complete: multi-stage Dockerfile builds frontend + backend, docker-compose orchestrates backend + pgvector database with health checks. Frontend scaffold has React 19, Vite 6, TypeScript 5.7, Tailwind 3.4 with design tokens from UI-SPEC pre-seeded. Scaffold builds cleanly and is ready for Phase 8 component development.
  </done>
</task>

</tasks>

<verification>
- `cd backend && uv run python -m pytest tests/test_llm_service.py tests/test_tenancy.py tests/test_db_backend.py -v --timeout=30` all pass
- `cd frontend && pnpm build` (or npm build) succeeds
- Dockerfile exists with HEALTHCHECK
- docker-compose.yml has backend + db services with health checks
- `grep -r "Fernet" backend/` returns nothing (no Fernet usage anywhere)
- LLM service enforces data policy correctly
</verification>

<success_criteria>
- LLM service wraps alea-llm-client with per-org config and training opt-out at three levels
- Organization CRUD endpoints work for admin users
- Cross-tenant isolation tests prove data separation
- Database backend abstraction tests verify both pg and sqlite engine creation
- Docker build works (Dockerfile, docker-compose.yml, docker-compose.dev.yml)
- Frontend scaffold builds: React 19, Vite, TypeScript, Tailwind with design tokens
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-05-SUMMARY.md`
</output>
