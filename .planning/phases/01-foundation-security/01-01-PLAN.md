---
phase: 01-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/core/__init__.py
  - backend/app/core/exceptions.py
  - backend/app/db/__init__.py
  - backend/app/db/engine.py
  - backend/app/db/base.py
  - backend/app/db/session.py
  - backend/app/db/tenant.py
  - backend/app/models/__init__.py
  - backend/app/models/shared.py
  - backend/app/models/user.py
  - backend/app/models/organization.py
  - backend/app/models/audit.py
  - backend/app/models/consent.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/auth.py
  - backend/app/schemas/user.py
  - backend/app/schemas/organization.py
  - backend/app/schemas/audit.py
  - backend/app/schemas/consent.py
  - backend/app/middleware/__init__.py
  - backend/app/middleware/tenant.py
  - backend/app/routers/__init__.py
  - backend/app/services/__init__.py
  - backend/app/services/tenant_service.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/alembic/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - pnpm-workspace.yaml
  - package.json
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - DEPLOY-01
  - SECURITY-10

must_haves:
  truths:
    - "FastAPI app starts and responds to GET /health with 200"
    - "App connects to PostgreSQL via asyncpg when ALEA_DATABASE_BACKEND=postgresql"
    - "App connects to SQLite via aiosqlite when ALEA_DATABASE_BACKEND=sqlite"
    - "Schema-per-tenant isolation routes queries to tenant_{org_slug} schemas"
    - "Shared schema holds tenant registry (Organization model)"
    - "All SQLAlchemy models use async 2.0 select() style"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI app with lifespan and /health endpoint"
      contains: "FastAPI"
    - path: "backend/app/config.py"
      provides: "Pydantic Settings with all config fields"
      contains: "class Settings"
    - path: "backend/app/db/engine.py"
      provides: "Async engine factory for pg and sqlite"
      contains: "create_engine"
    - path: "backend/app/db/base.py"
      provides: "TenantBase and SharedBase declarative bases"
      exports: ["TenantBase", "SharedBase"]
    - path: "backend/app/db/session.py"
      provides: "Per-request session with schema_translate_map"
      contains: "schema_translate_map"
    - path: "backend/app/db/tenant.py"
      provides: "Tenant resolution and schema management"
      contains: "resolve_tenant"
    - path: "backend/app/models/shared.py"
      provides: "Organization model in shared schema"
      contains: "class Organization"
    - path: "backend/app/models/user.py"
      provides: "User model with role enum"
      contains: "class User"
    - path: "backend/tests/conftest.py"
      provides: "Test fixtures for async DB and FastAPI test client"
      contains: "async_client"
  key_links:
    - from: "backend/app/db/session.py"
      to: "backend/app/db/engine.py"
      via: "get_engine() call"
      pattern: "get_engine"
    - from: "backend/app/db/session.py"
      to: "backend/app/db/tenant.py"
      via: "schema_translate_map from tenant resolution"
      pattern: "schema_translate_map"
    - from: "backend/app/main.py"
      to: "backend/app/db/engine.py"
      via: "lifespan engine initialization"
      pattern: "lifespan"
---

<objective>
Scaffold the complete backend project structure, configuration system, database abstraction layer, all SQLAlchemy models, Pydantic schemas, Alembic migration setup, tenant isolation infrastructure, and test harness. This is the foundation that every subsequent plan builds on.

Purpose: Establish the core project skeleton and data models so Plans 02-05 can build auth, encryption, audit, consent, and LLM features against a working, testable codebase.
Output: A running FastAPI app with /health endpoint, configurable Postgres/SQLite backend, schema-per-tenant isolation, all data models, and a green test suite.
</objective>

<execution_context>
@/home/damienriehl/.claude/get-shit-done/workflows/execute-plan.md
@/home/damienriehl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding, config, and database engine</name>
  <files>
    backend/pyproject.toml,
    backend/app/__init__.py,
    backend/app/main.py,
    backend/app/config.py,
    backend/app/core/__init__.py,
    backend/app/core/exceptions.py,
    backend/app/db/__init__.py,
    backend/app/db/engine.py,
    backend/app/db/base.py,
    pnpm-workspace.yaml,
    package.json,
    .env.example,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation-security/01-RESEARCH.md (architecture patterns, standard stack, config.py example, project structure)
    .planning/phases/01-foundation-security/01-CONTEXT.md (locked decisions on DB backends, encryption, tenant isolation)
    .planning/PROJECT.md (tech stack constraints, FOLIO ecosystem context)
  </read_first>
  <action>
    Create the project root and backend directory structure from scratch (greenfield).

    **Root level:**
    - `package.json`: `{"name": "alea-intake", "private": true}` with no dependencies (workspace root)
    - `pnpm-workspace.yaml`: `packages: ["frontend"]`
    - `.gitignore`: Python (__pycache__, *.pyc, .venv, *.egg-info), Node (node_modules), env files (.env, .env.local), IDE (.idea, .vscode), data/ directory, alembic versions except __init__
    - `.env.example`: All ALEA_ prefixed vars with placeholder values:
      ```
      ALEA_SECRET_KEY=change-me-to-random-64-char-string
      ALEA_DATABASE_BACKEND=postgresql
      ALEA_DB_HOST=localhost
      ALEA_DB_PORT=5432
      ALEA_DB_NAME=alea_intake
      ALEA_DB_USER=alea
      ALEA_DB_PASSWORD=change-me
      ALEA_SQLITE_PATH=./data/alea_intake.db
      ALEA_MASTER_KEY_PATH=
      ALEA_KMS_PROVIDER=
      ALEA_KMS_KEY_ID=
      ALEA_ACCESS_TOKEN_EXPIRE_MINUTES=30
      ALEA_REFRESH_TOKEN_EXPIRE_DAYS=7
      ALEA_DEBUG=false
      ALEA_CORS_ORIGINS=["http://localhost:5173"]
      ```

    **backend/pyproject.toml:**
    - Project name: `alea-intake-backend`
    - Python requires: `>=3.12`
    - Dependencies (exact versions from RESEARCH.md standard stack):
      - `fastapi>=0.135.0,<1.0`
      - `uvicorn[standard]>=0.42.0`
      - `sqlalchemy[asyncio]>=2.0.48`
      - `alembic>=1.18.0`
      - `pydantic>=2.12.0`
      - `pydantic-settings>=2.13.0`
      - `pyjwt>=2.12.0`
      - `pwdlib[argon2]>=0.3.0`
      - `cryptography>=46.0.0`
      - `python-multipart>=0.0.22`
      - `python-dotenv>=1.2.0`
      - `alea-llm-client>=0.3.0`
      - `httpx>=0.28.0`
      - `asyncpg>=0.31.0`
      - `psycopg[binary]>=3.3.0`
      - `aiosqlite>=0.22.0`
      - `pgvector>=0.4.0`
    - Dev dependencies group:
      - `pytest>=8.0`
      - `pytest-asyncio>=0.24`
      - `factory-boy>=3.0`
      - `ruff>=0.9`
      - `pytest-timeout>=2.0`
    - Tool config: `[tool.pytest.ini_options]` with `asyncio_mode = "auto"`, `timeout = 30`, `testpaths = ["tests"]`
    - Ruff config: `[tool.ruff]` with `target-version = "py312"`, `line-length = 120`

    **backend/app/config.py:**
    - Import `BaseSettings` from `pydantic_settings`
    - Define enums: `DatabaseBackend(str, Enum)` with POSTGRESQL="postgresql" and SQLITE="sqlite", `LLMDataPolicy(str, Enum)` with CLOUD_WITH_OPTOUT="cloud_optout", CLOUD_WITH_BAA="cloud_baa", LOCAL_ONLY="local_only"
    - `class Settings(BaseSettings)`:
      - `app_name: str = "alea-intake"`
      - `debug: bool = False`
      - `secret_key: str` (no default -- required)
      - `database_backend: DatabaseBackend = DatabaseBackend.POSTGRESQL`
      - `db_host: str = "localhost"`, `db_port: int = 5432`, `db_name: str = "alea_intake"`, `db_user: str = "alea"`, `db_password: str = ""`
      - `sqlite_path: str = "./data/alea_intake.db"`
      - `master_key_path: str = ""`, `kms_provider: str = ""`, `kms_key_id: str = ""`
      - `access_token_expire_minutes: int = 30`, `refresh_token_expire_days: int = 7`
      - `cors_origins: list[str] = ["http://localhost:5173"]`
      - `model_config = SettingsConfigDict(env_file=".env", env_prefix="ALEA_")`
    - Module-level `settings = Settings()` instance (but wrap in a function `get_settings()` that returns cached instance for testability)

    **backend/app/db/engine.py:**
    - `create_engine() -> AsyncEngine` factory function per RESEARCH.md Pattern 5
    - PostgreSQL: `postgresql+asyncpg://{user}:{password}@{host}:{port}/{name}` with `pool_size=20, max_overflow=10`
    - SQLite: `sqlite+aiosqlite:///{path}` with `echo=False`
    - Cache the engine in a module-level `_engine: AsyncEngine | None` variable
    - `get_engine() -> AsyncEngine` that creates on first call
    - `async def dispose_engine()` for cleanup

    **backend/app/db/base.py:**
    - Two `MetaData` objects: `tenant_metadata = MetaData(schema="tenant")`, `shared_metadata = MetaData(schema="shared")`
    - `class TenantBase(DeclarativeBase)` with `metadata = tenant_metadata`
    - `class SharedBase(DeclarativeBase)` with `metadata = shared_metadata`
    - Import naming convention for constraints: `convention = {"ix": "ix_%(column_0_label)s", "uq": "uq_%(table_name)s_%(column_0_name)s", "ck": "ck_%(table_name)s_%(constraint_name)s", "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s", "pk": "pk_%(table_name)s"}`

    **backend/app/main.py:**
    - FastAPI app with `title="ALEA Intake API"`, `version="0.1.0"`
    - `@asynccontextmanager async def lifespan(app)` that calls engine initialization on startup and `dispose_engine()` on shutdown
    - `GET /health` endpoint returning `{"status": "healthy", "version": "0.1.0"}`
    - CORSMiddleware added with `allow_origins=settings.cors_origins`, `allow_credentials=True`, `allow_methods=["*"]`, `allow_headers=["*"]`

    **backend/app/core/exceptions.py:**
    - `class TenantNotFoundError(Exception)` with `tenant_id` attribute
    - `class EncryptionError(Exception)`
    - `class ConsentRequiredError(Exception)`
    - `class InsufficientPermissionsError(Exception)`
    - FastAPI exception handlers registered in main.py for each, returning appropriate HTTP status codes and the exact error messages from UI-SPEC copywriting contract

    All `__init__.py` files should be empty (or contain `__all__` exports where useful).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv sync && uv run python -c "from app.config import Settings; from app.db.engine import create_engine; from app.db.base import TenantBase, SharedBase; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains `name = "alea-intake-backend"` and `python = ">=3.12"` (or requires-python)
    - backend/pyproject.toml contains `fastapi`, `sqlalchemy`, `pyjwt`, `pwdlib`, `cryptography`, `alea-llm-client` as dependencies
    - backend/pyproject.toml contains `pytest`, `pytest-asyncio`, `ruff` in dev dependencies
    - backend/pyproject.toml contains `asyncio_mode = "auto"` in pytest config
    - backend/app/config.py contains `class Settings(BaseSettings)` with `env_prefix="ALEA_"`
    - backend/app/config.py contains `class DatabaseBackend(str, Enum)` with `POSTGRESQL` and `SQLITE`
    - backend/app/config.py contains `class LLMDataPolicy(str, Enum)`
    - backend/app/db/engine.py contains `def create_engine()` and `def get_engine()`
    - backend/app/db/engine.py contains `postgresql+asyncpg://` and `sqlite+aiosqlite:///`
    - backend/app/db/base.py contains `class TenantBase(DeclarativeBase)` and `class SharedBase(DeclarativeBase)`
    - backend/app/db/base.py contains `schema="tenant"` and `schema="shared"`
    - backend/app/main.py contains `FastAPI(title="ALEA Intake API")`
    - backend/app/main.py contains `GET` and `/health`
    - backend/app/main.py contains `CORSMiddleware`
    - backend/app/core/exceptions.py contains `class TenantNotFoundError` and `class EncryptionError`
    - .env.example contains `ALEA_SECRET_KEY` and `ALEA_DATABASE_BACKEND`
    - .gitignore contains `__pycache__` and `node_modules` and `.env`
    - `uv run python -c "from app.config import Settings"` exits 0 from backend/
  </acceptance_criteria>
  <done>
    FastAPI app skeleton imports cleanly. Config loads from environment. Database engine factory supports both PostgreSQL and SQLite backends. All directory structure matches RESEARCH.md architecture. Dependencies install via uv.
  </done>
</task>

<task type="auto">
  <name>Task 2: Data models, schemas, tenant isolation, and test harness</name>
  <files>
    backend/app/db/session.py,
    backend/app/db/tenant.py,
    backend/app/models/__init__.py,
    backend/app/models/shared.py,
    backend/app/models/user.py,
    backend/app/models/organization.py,
    backend/app/models/audit.py,
    backend/app/models/consent.py,
    backend/app/schemas/__init__.py,
    backend/app/schemas/auth.py,
    backend/app/schemas/user.py,
    backend/app/schemas/organization.py,
    backend/app/schemas/audit.py,
    backend/app/schemas/consent.py,
    backend/app/middleware/__init__.py,
    backend/app/middleware/tenant.py,
    backend/app/routers/__init__.py,
    backend/app/services/__init__.py,
    backend/app/services/tenant_service.py,
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/alembic/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/script.py.mako
  </files>
  <read_first>
    backend/app/db/base.py (TenantBase and SharedBase classes just created)
    backend/app/db/engine.py (engine factory just created)
    backend/app/config.py (Settings class just created)
    backend/app/main.py (FastAPI app just created)
    .planning/phases/01-foundation-security/01-RESEARCH.md (Pattern 1 schema-per-tenant, Pattern 3 audit model, consent model, RBAC permissions)
    .planning/phases/01-foundation-security/01-CONTEXT.md (locked decisions on tenant isolation, consent granularity, audit log design)
  </read_first>
  <action>
    Create all SQLAlchemy models, Pydantic schemas, tenant isolation middleware, session management, Alembic setup, tenant service, and test infrastructure.

    **backend/app/models/shared.py (SharedBase -- lives in `shared` schema):**
    - `class Organization(SharedBase)`: `__tablename__ = "organizations"`
      - `id: Mapped[int]` primary_key, autoincrement
      - `name: Mapped[str]` String(255), not null
      - `slug: Mapped[str]` String(100), unique, not null -- used for schema name `tenant_{slug}`
      - `settings: Mapped[dict]` JSON, nullable, default `{}` -- org-configurable settings
      - `auth_mode: Mapped[str]` String(20), default `"email_password"` -- enum: `email_password`, `oauth_sso`, `kiosk`
      - `llm_data_policy: Mapped[str]` String(20), default `"cloud_optout"`
      - `consent_mode: Mapped[str]` String(20), default `"granular"` -- `all_or_nothing`, `granular`
      - `deletion_policy: Mapped[str]` String(20), default `"anonymize"` -- `full_delete`, `anonymize`, `time_based`
      - `is_active: Mapped[bool]` default True
      - `created_at: Mapped[datetime]` server_default=func.now()
      - `updated_at: Mapped[datetime]` server_default=func.now(), onupdate=func.now()

    **backend/app/models/user.py (TenantBase -- lives in tenant schema):**
    - `class Role(str, Enum)`: `ADMIN = "admin"`, `PROFESSIONAL = "professional"`, `CONSUMER = "consumer"`
    - `class User(TenantBase)`: `__tablename__ = "users"`
      - `id: Mapped[int]` primary_key, autoincrement
      - `email: Mapped[str | None]` String(255), nullable (null for kiosk users)
      - `hashed_password: Mapped[str | None]` String(255), nullable (null for OAuth/kiosk)
      - `full_name: Mapped[bytes | None]` LargeBinary, nullable -- ENCRYPTED PII field (stored as bytes)
      - `role: Mapped[str]` String(20), not null, default `"consumer"`
      - `is_active: Mapped[bool]` default True
      - `org_id: Mapped[int]` not null -- denormalized from org context for queries
      - `created_at: Mapped[datetime]` server_default=func.now()
      - `updated_at: Mapped[datetime]` server_default=func.now(), onupdate=func.now()
      - Add unique constraint on `(email, org_id)` -- one account per org per email

    **backend/app/models/audit.py (TenantBase):**
    - `class AuditLog(TenantBase)`: `__tablename__ = "audit_log"`
      - `id: Mapped[int]` primary_key, autoincrement
      - `timestamp: Mapped[datetime]` server_default=func.now(), not null
      - `actor_id: Mapped[int | None]` nullable (null for system/anonymous)
      - `actor_role: Mapped[str | None]` String(20), nullable
      - `action: Mapped[str]` String(100), not null -- e.g., "user.login", "case.view", "ai.decision"
      - `resource_type: Mapped[str | None]` String(50), nullable
      - `resource_id: Mapped[int | None]` nullable
      - `details: Mapped[dict | None]` JSON, nullable
      - `ip_address: Mapped[str | None]` String(45), nullable
      - `request_id: Mapped[str | None]` String(36), nullable -- correlation ID
      - Index on `(timestamp, action)` for query performance

    **backend/app/models/consent.py (TenantBase):**
    - `class ConsentRecord(TenantBase)`: `__tablename__ = "consent_records"`
      - `id: Mapped[int]` primary_key
      - `user_id: Mapped[int | None]` ForeignKey("users.id"), nullable (null for kiosk)
      - `session_id: Mapped[str | None]` String(36), nullable (for kiosk sessions)
      - `consent_version: Mapped[str]` String(20), not null
      - `granted_at: Mapped[datetime]` server_default=func.now()
      - `revoked_at: Mapped[datetime | None]` nullable
      - `consent_items: Mapped[dict]` JSON, not null -- e.g., `{"ai_processing": true, "data_storage": true}`
      - `ip_address: Mapped[str | None]` String(45), nullable
    - `class ConsentTemplate(TenantBase)`: `__tablename__ = "consent_templates"`
      - `id: Mapped[int]` primary_key
      - `org_id: Mapped[int]` not null
      - `version: Mapped[str]` String(20), not null
      - `items: Mapped[list]` JSON, not null -- `[{"key": "ai_processing", "label": "Allow AI analysis", "required": true}, ...]`
      - `is_active: Mapped[bool]` default True

    **backend/app/models/organization.py (TenantBase -- org config that lives in tenant schema):**
    - `class OrganizationConfig(TenantBase)`: `__tablename__ = "organization_config"`
      - `id: Mapped[int]` primary_key
      - `org_id: Mapped[int]` not null, unique
      - `llm_api_key_encrypted: Mapped[bytes | None]` LargeBinary, nullable -- org's own LLM API key (encrypted)
      - `llm_provider: Mapped[str | None]` String(50), nullable -- e.g., "openai", "anthropic"
      - `llm_model: Mapped[str | None]` String(100), nullable
      - `llm_data_policy: Mapped[str]` String(20), default `"cloud_optout"`
      - `kiosk_audit_enabled: Mapped[bool]` default True
      - `kiosk_consent_required: Mapped[bool]` default True
      - `kiosk_session_ttl_hours: Mapped[int]` default 24
      - `created_at: Mapped[datetime]` server_default=func.now()
      - `updated_at: Mapped[datetime]` server_default=func.now(), onupdate=func.now()

    **backend/app/models/__init__.py:**
    - Import and re-export all models: `User, Role, Organization, OrganizationConfig, AuditLog, ConsentRecord, ConsentTemplate`

    **Pydantic Schemas (backend/app/schemas/):**

    `auth.py`:
    - `class LoginRequest(BaseModel)`: `email: EmailStr`, `password: str`
    - `class TokenResponse(BaseModel)`: `access_token: str`, `refresh_token: str`, `token_type: str = "bearer"`
    - `class RegisterRequest(BaseModel)`: `email: EmailStr`, `password: str` (min_length=8), `full_name: str`
    - `class RefreshRequest(BaseModel)`: `refresh_token: str`

    `user.py`:
    - `class UserResponse(BaseModel)`: `id: int`, `email: str | None`, `full_name: str | None`, `role: str`, `is_active: bool`, `created_at: datetime`, with `model_config = ConfigDict(from_attributes=True)`
    - `class UserCreate(BaseModel)`: `email: EmailStr`, `password: str`, `full_name: str`, `role: str = "consumer"`

    `organization.py`:
    - `class OrganizationResponse(BaseModel)`: `id: int`, `name: str`, `slug: str`, `auth_mode: str`, `is_active: bool`
    - `class OrganizationCreate(BaseModel)`: `name: str` (min_length=2, max_length=255), `slug: str` (pattern=`^[a-z0-9_-]+$`, min_length=2, max_length=100)

    `audit.py`:
    - `class AuditLogResponse(BaseModel)`: all fields from model, with `model_config = ConfigDict(from_attributes=True)`
    - `class AuditLogQuery(BaseModel)`: `action: str | None = None`, `actor_id: int | None = None`, `start_date: datetime | None = None`, `end_date: datetime | None = None`, `limit: int = 50`, `offset: int = 0`

    `consent.py`:
    - `class ConsentGrantRequest(BaseModel)`: `consent_version: str`, `consent_items: dict[str, bool]`
    - `class ConsentResponse(BaseModel)`: all fields from model with `from_attributes=True`
    - `class ConsentTemplateResponse(BaseModel)`: all fields from model with `from_attributes=True`

    **backend/app/db/session.py:**
    - `async def get_tenant_session(request: Request) -> AsyncGenerator[AsyncSession, None]`: reads `request.state.tenant_schema`, creates session with `schema_translate_map={"tenant": tenant_schema}`, yields session
    - `async def get_shared_session() -> AsyncGenerator[AsyncSession, None]`: session against shared schema (no translation needed), for Organization CRUD
    - Use `async_sessionmaker` with `expire_on_commit=False`

    **backend/app/db/tenant.py:**
    - `async def resolve_tenant_schema(org_slug: str) -> str`: returns `f"tenant_{org_slug}"`
    - `async def ensure_tenant_schema_exists(engine: AsyncEngine, org_slug: str) -> None`: creates schema `tenant_{org_slug}` if not exists, creates all TenantBase tables in that schema
    - `async def ensure_shared_schema_exists(engine: AsyncEngine) -> None`: creates `shared` schema and SharedBase tables

    **backend/app/middleware/tenant.py:**
    - `class TenantMiddleware`: Starlette BaseHTTPMiddleware
      - Reads `X-Tenant-Slug` header OR extracts org from JWT token `org` claim
      - Sets `request.state.tenant_schema = f"tenant_{org_slug}"`
      - Skips tenant resolution for public routes: `/health`, `/docs`, `/openapi.json`, `/api/v1/auth/login`, `/api/v1/auth/register`
      - If no tenant found and route requires it: returns 400 `{"detail": "Tenant identification required"}`

    **backend/app/services/tenant_service.py:**
    - `class TenantService`:
      - `async def create_tenant(self, session: AsyncSession, org_create: OrganizationCreate) -> Organization`: creates Organization in shared schema, then calls `ensure_tenant_schema_exists`
      - `async def get_tenant_by_slug(self, session: AsyncSession, slug: str) -> Organization | None`
      - `async def list_tenants(self, session: AsyncSession) -> list[Organization]`

    **backend/alembic/alembic.ini:**
    - `script_location = alembic`
    - `sqlalchemy.url` will be overridden in env.py

    **backend/alembic/env.py:**
    - Import both `tenant_metadata` and `shared_metadata` from `app.db.base`
    - Support `-x tenant=schema_name` flag for per-schema migrations
    - `run_migrations_online()`: set `search_path` to target schema before running
    - Use synchronous `psycopg` connection for migrations (not asyncpg)

    **backend/alembic/script.py.mako:**
    - Standard Alembic template

    **backend/tests/conftest.py:**
    - Use `aiosqlite` for test database (in-memory `:memory:` or temp file)
    - `@pytest.fixture` for `async_engine`: creates SQLite async engine, creates all tables from both TenantBase and SharedBase metadata (using default schema for SQLite)
    - `@pytest.fixture` for `async_session`: yields an AsyncSession against the test engine
    - `@pytest.fixture` for `async_client`: creates `httpx.AsyncClient` with FastAPI `app` using `ASGITransport`
    - Override `get_settings()` to return test settings with `database_backend="sqlite"` and `secret_key="test-secret-key-for-testing-only-not-production"`
    - `@pytest.fixture` for `test_org`: creates a test Organization record
    - `@pytest.fixture` for `test_user`: creates a test User record with known credentials

    **backend/tests/__init__.py:** empty file

    Wire the TenantMiddleware into `main.py` by adding `app.add_middleware(TenantMiddleware)`.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/alea-intake/backend" && uv run python -c "
from app.models import User, Role, Organization, OrganizationConfig, AuditLog, ConsentRecord, ConsentTemplate
from app.schemas.auth import LoginRequest, TokenResponse, RegisterRequest
from app.schemas.user import UserResponse, UserCreate
from app.schemas.organization import OrganizationResponse, OrganizationCreate
from app.schemas.audit import AuditLogResponse
from app.schemas.consent import ConsentGrantRequest, ConsentResponse
from app.db.session import get_tenant_session, get_shared_session
from app.db.tenant import resolve_tenant_schema
from app.middleware.tenant import TenantMiddleware
from app.services.tenant_service import TenantService
print('All models, schemas, middleware, and services import OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/shared.py contains `class Organization(SharedBase)` with `slug`, `auth_mode`, `llm_data_policy`, `consent_mode`, `deletion_policy`
    - backend/app/models/user.py contains `class User(TenantBase)` with `full_name` as `LargeBinary` (encrypted PII)
    - backend/app/models/user.py contains `class Role(str, Enum)` with `ADMIN`, `PROFESSIONAL`, `CONSUMER`
    - backend/app/models/audit.py contains `class AuditLog(TenantBase)` with `action`, `actor_id`, `details`, `request_id`
    - backend/app/models/consent.py contains `class ConsentRecord(TenantBase)` and `class ConsentTemplate(TenantBase)`
    - backend/app/models/consent.py ConsentRecord has `user_id` nullable and `session_id` for kiosk
    - backend/app/models/organization.py contains `class OrganizationConfig(TenantBase)` with `llm_api_key_encrypted` as LargeBinary
    - backend/app/db/session.py contains `schema_translate_map` and `get_tenant_session`
    - backend/app/db/tenant.py contains `resolve_tenant_schema` and `ensure_tenant_schema_exists`
    - backend/app/middleware/tenant.py contains `class TenantMiddleware` with `X-Tenant-Slug` header reading
    - backend/app/schemas/auth.py contains `LoginRequest`, `TokenResponse`, `RegisterRequest`
    - backend/app/schemas/user.py contains `UserResponse` with `from_attributes=True`
    - backend/app/services/tenant_service.py contains `class TenantService` with `create_tenant`
    - backend/tests/conftest.py contains `async_client` and `async_session` fixtures
    - backend/tests/conftest.py contains `aiosqlite` or `sqlite` for test database
    - backend/alembic/env.py exists and imports `tenant_metadata` and `shared_metadata`
    - All imports in the verify command exit 0
  </acceptance_criteria>
  <done>
    All SQLAlchemy models exist with correct column types (LargeBinary for encrypted PII fields, JSON for flexible config). Pydantic schemas cover all request/response patterns. Tenant middleware resolves org from header or JWT. Session factory applies schema_translate_map. Alembic is configured for per-schema migrations. Test conftest provides async fixtures with SQLite in-memory backend. The foundation is ready for auth, encryption, audit, and consent implementations.
  </done>
</task>

</tasks>

<verification>
- `cd backend && uv sync` exits 0 (all deps install)
- `cd backend && uv run python -c "from app.main import app; print(app.title)"` prints "ALEA Intake API"
- `cd backend && uv run python -c "from app.models import User, Organization, AuditLog, ConsentRecord"` exits 0
- `cd backend && uv run python -c "from app.db.engine import create_engine; from app.db.base import TenantBase, SharedBase"` exits 0
- `cd backend && uv run python -c "from app.middleware.tenant import TenantMiddleware"` exits 0
- `.env.example` exists with all ALEA_ variables
</verification>

<success_criteria>
- FastAPI app skeleton is complete with /health endpoint
- All 7 SQLAlchemy models exist (Organization, OrganizationConfig, User, AuditLog, ConsentRecord, ConsentTemplate, RefreshToken if added)
- All Pydantic schemas exist for auth, user, org, audit, consent
- Database engine factory supports postgresql and sqlite backends
- Schema-per-tenant session management uses schema_translate_map
- Tenant middleware resolves org from X-Tenant-Slug header or JWT
- Test conftest with async fixtures is in place
- Alembic is configured for multi-schema migrations
- All imports resolve without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-01-SUMMARY.md`
</output>
