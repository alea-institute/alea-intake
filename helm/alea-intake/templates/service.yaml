apiVersion: v1
kind: Service
metadata:
  name: {{ include "alea-intake.fullname" . }}
  labels:
    {{- include "alea-intake.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alea-intake.selectorLabels" . | nindent 4 }}
