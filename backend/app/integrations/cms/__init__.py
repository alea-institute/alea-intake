"""CMS (Case Management System) sync connectors.

Provides bidirectional sync adapters for Clio, MyCase, and LegalServer.
Each adapter implements the CMSAdapter ABC for uniform push/pull/webhook
operations against CMS-specific APIs.
"""
